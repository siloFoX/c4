# Patch 1.5.3 - auto-mgr 도구 제한 (5.1)

## Date: 2026-04-05

## Summary
auto-manager worker가 코드를 직접 수정하지 못하도록 도구 제한. c4 명령어로 하위 worker를 생성해서 작업을 위임하도록 강제.

## Changes

### src/pty-manager.js
- `_buildAutoManagerPermissions()` 수정
- allow: `Bash(c4:*)`, `Bash(MSYS_NO_PATHCONV=1 c4:*)`, `Bash(git -C:*)`, `Agent` 만 허용
- deny: `Read`, `Write`, `Edit`, `Grep`, `Glob` 추가
- 기존 광범위한 Bash 패턴(npm, node, grep, find, cat 등) 및 일반 Bash 제거

### tests/worker-settings.test.js
- mock에 `_buildAutoManagerPermissions()` 및 `_autoWorker` 분기 추가
- 5개 테스트 추가: deny 확인, allow 패턴 확인, defaultMode, Agent 허용, 일반 worker 비영향
