# 1.5.8 - Branch Auto-Delete + Worktree Prune

## Changes

### close() branch cleanup (5.25/5.31)
- `src/pty-manager.js` close(): worktree remove 후 `git branch -D` 로 c4/ 접두사 브랜치 자동 삭제
- c4/ 접두사가 아닌 브랜치는 삭제하지 않음
- branch가 이미 삭제되었거나 존재하지 않으면 무시 (try-catch)

### healthCheck() worktree prune (5.32)
- `src/pty-manager.js` healthCheck(): return 직전에 `git worktree prune` 실행
- stale worktree 참조를 주기적으로 정리
- repoRoot가 없으면 skip

### Tests
- `tests/worktree-cleanup.test.js`: branch cleanup 6개 테스트 + worktree prune 3개 테스트 추가
