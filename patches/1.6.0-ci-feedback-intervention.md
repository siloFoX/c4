# 1.6.0 — CI feedback loop, intervention immediate notification

## Changes

### 5.20 CI Feedback Loop
- `src/pty-manager.js` `_handlePostToolUse()`: `git commit` 감지 후 자동 `npm test` 실행
- 성공 시: `_lastCiResult.passed = true`, SSE `ci` 이벤트, `[CI PASS]` Slack 알림
- 실패 ��: `_lastCiResult.passed = false`, 에러 출력 스냅샷, `[CI FAIL]` Slack 알림, worker에 자동 피드백 전송
- `config.ci.enabled` (default: true), `config.ci.testCommand` (default: 'npm test'), `config.ci.timeoutMs` (default: 120000)
- `toolError`가 있는 커밋(실패한 커밋)에서는 CI 미실��

### 5.29 Intervention Immediate Notification
- question 감지 시 즉시 `notifyStall(name, 'intervention: question — ...')` 호출
- escalation 감지 시 즉시 `notifyStall(name, 'intervention: escalation — ...')` 호출 (TTY + hook 모두)
- permission prompt 감지 시 즉시 `notifyStall(name, 'awaiting approval: ...')` 호출
- `_permissionNotified` 플래그로 같은 프롬프트에 대한 중복 알림 방지, 프롬프트 사라지면 리셋
- worker 초기화에 `_permissionNotified`, `_lastCiResult` 속성 추가

## Tests
- `tests/ci-intervention.test.js`: 18 tests (11 CI + 7 intervention)
