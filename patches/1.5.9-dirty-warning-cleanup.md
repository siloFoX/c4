# 1.5.9 — Dirty worktree warning, submodule diff, c4 cleanup

## Changes

### 5.15 Dirty worktree Slack warning
- `src/pty-manager.js` healthCheck(): alive worker의 worktree dirty 상태 감지
- `_isWorktreeDirty()` 호출 후 `[DIRTY]` 알림 전송
- `_dirtyNotified` 플래그로 중복 알림 방지, 정리 시 리셋

### 5.30 Submodule diff support
- `src/cli.js` merge case: merge 완료 후 `git diff --stat --submodule=diff HEAD~1..HEAD` 실행
- 서브모듈 변경사항이 포인터만이 아닌 실제 diff로 표시

### 5.33 c4 cleanup command
- `src/cli.js`: `case 'cleanup'` 추가 (`--dry-run` 지원)
- `src/daemon.js`: `POST /cleanup` 라우트 추가
- `src/pty-manager.js`: `cleanup(dryRun)` 메서드 추가
  - LOST worker의 c4/ 브랜치 삭제
  - LOST worker의 worktree 제거
  - 고아 c4-worktree-* 디렉토리 삭제
  - `git worktree prune` 실행
  - `_saveState()` 호출

## Tests
- `tests/dirty-warning-cleanup.test.js`: 13 tests (6 dirty warning + 7 cleanup)
