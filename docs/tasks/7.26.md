# TODO 7.26: Manager 명령 halt 방지

## 배경
매니저가 복합/파이프/루프/markdown 패턴을 포함한 bash 명령을 생성하면 Claude Code 권한 매처가 안전 판정 못 해서 permission prompt에서 halt됨. 자동화 치명적 차단점.

재현 패턴 (2026-04-17 세션에서 관찰된 것들):
- halt 모드 A: `c4 send ... 2>&1 | tail -1` / `for dir in ...; do ...; done` / `cd /path && npm test | tail`
- halt 모드 B: `c4 task name "... ## 목표 ... ### 1. ..."` — quoted arg에 `\n#` 포함

## 목표
매니저가 halt 유발 패턴을 생성하지 않도록 (1) 규칙 강화, (2) 자동 파일화, (3) 선택적 감지 hook 추가.

## 수정 범위

### 1. .claude/agents/manager.md (핵심)
기존 규칙 섹션에 다음 내용을 "명령 생성 규칙" 서브섹션으로 추가:

```
## 명령 생성 규칙 (halt 방지)

### 절대 금지 패턴
- 복합: cmd1 && cmd2, cmd1; cmd2, cmd1 || cmd2
- 파이프: cmd | filter
- 리다이렉션: cmd 2>&1 | ...
- 루프: for x in ...; do ...; done, while ...; do ...; done
- cd chain: cd /path && command

### 올바른 대안
- 각 명령 개별 Bash tool call (병렬 가능)
- git -C <path> (cd 대신)
- npm --prefix <path> (cd 대신, 또는 c4 task name "cd /path && npm test" 아님 — 직접 npm --prefix)
- sleep 대신 c4 wait <name>
- 여러 파일 조회는 개별 Read tool (병렬)

### c4 task/send 메시지 규칙
- 메시지에 markdown 헤더(##, ###) 포함 금지 — halt 유발
- 긴 스펙 전달은 다음 패턴: Write로 /tmp/task-<name>.md 저장 → c4 task name "read /tmp/task-<name>.md and execute"
- 인라인 메시지는 한 문단 plain text (markdown 없음)

### 위반 시 대응
halt 발생하면: (1) 사용자에게 알림, (2) 해당 명령을 규칙 준수 형태로 재작성, (3) 같은 실수 반복 금지
```

### 2. src/cli.js c4 task 자동 파일화 검증/개선
CLAUDE.md에 "1000자+ 자동 `.c4-task.md` 파일 전달" 언급 있음. 실제 동작 확인:
- 현재 로직: `case 'task'` 블록에서 메시지 길이 체크하는지 확인
- 1000자 이상이어도 shell 인자로 그대로 전달되는지, 아니면 파일 변환되는지
- 만약 파일 변환 안 되고 있으면: 구현 (메시지 길이 > 1000 OR `\n#` 패턴 포함 시 임시 파일에 쓰고 워커에 `--file` 또는 경로 전달)
- 기존 동작 유지 (짧은 메시지는 인라인)

### 3. tests/manager-command-rules.test.js (선택, 여유 있으면)
규칙 문서가 유효한지 최소 검증:
- .claude/agents/manager.md에 "명령 생성 규칙" 섹션 존재
- "절대 금지 패턴" 서브섹션에 compound/pipe/loop 명시
- "c4 task/send 메시지 규칙" 존재

## 검증
- npm test 전체 pass (현재 53 suites)
- c4 task name "message with ## header" 전송 → 자동 파일화돼서 halt 없이 전달되는지 확인 (수동)
- manager.md 규칙이 실제 매니저 세션에 적용되는지 확인 (런타임 검증은 어려우니 문서 검증만)

## 문서
- TODO.md 7.26 상태 **todo** → **done** + 구현 요약 추가
- CHANGELOG.md 최신 섹션에 한 줄: `fix: prevent manager halt from compound/markdown commands (7.26)`
- 커밋 메시지: `fix: prevent manager halt from compound/markdown commands (7.26)`

## 주의사항
- 스스로 복합 명령 생성 금지 (dogfood). 각 Bash call은 단일 명령.
- `cd && ...` 금지. git -C, npm --prefix 사용.
- 긴 메시지 shell 인자로 박지 말고 파일로 Write 후 path만 전달.
- 불필요한 확인 질문 없이 자율 진행. 구현 → 테스트 → 문서 → 커밋 순서.
- 머지 전 working tree clean 확인.

## 완료 신호
머지 완료 후 "완료" 한 마디만 출력.
