# 5.25/5.31 브랜치 자동 정리 + 5.32 worktree prune 자동화

## 5.25/5.31: close() 시 브랜치 삭제

### 수정: src/pty-manager.js close() (약 3325번 줄)

worktree remove 후, 해당 worker의 branch도 삭제:
```js
// close() 메서드 안, worktree cleanup 블록 뒤에 추가:
// Branch cleanup (5.25/5.31): delete worker's c4/ branch after worktree removal
if (w.branch && w.branch.startsWith('c4/')) {
  const cleanupRoot = w.worktreeRepoRoot || this._detectRepoRoot();
  if (cleanupRoot) {
    try {
      execSyncSafe(`git -C "${cleanupRoot.replace(/\\/g, '/')}" branch -D "${w.branch}"`, {
        encoding: 'utf8', stdio: 'pipe', timeout: 5000
      });
    } catch {} // Branch may already be deleted or not exist
  }
}
```

## 5.32: healthCheck에서 worktree prune

### 수정: src/pty-manager.js healthCheck() (약 2958번 줄)

healthCheck 끝 부분에 worktree prune 추가:
```js
// Worktree prune (5.32): periodically clean up stale worktrees
try {
  const repoRoot = this._detectRepoRoot();
  if (repoRoot) {
    execSyncSafe('git worktree prune', {
      cwd: repoRoot, encoding: 'utf8', stdio: 'pipe', timeout: 5000
    });
  }
} catch {}
```

## 테스트
기존 worktree-cleanup.test.js에 브랜치 삭제 테스트 추가. healthCheck worktree prune 테스트 추가.

## 문서
- TODO.md: 5.25, 5.31, 5.32 done 표시
- CHANGELOG.md: 최상단 버전 추가
- patches/ 추가

npm test 후 커밋. 메시지: `feat: auto-delete worker branches on close, add worktree prune to healthCheck (5.25, 5.31, 5.32)`
