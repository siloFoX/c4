# Patch 11.896 -- UI: chart-line-vortex-cross primitive

**Version:** v1.11.914
**Queue TODO:** 11.896
**Branch:** c4/c4-mgr-auto
**Status:** done

## Summary

Added `<ChartLineVortexCross />` -- a pure-SVG, dual-panel
React/TS primitive that renders the close in the top panel and
overlays the Vortex Indicator (VI+ over VI-) in the bottom
panel, marking bullish / bearish cross trigger events for
*directional movement regime changes*. Cross markers are also
painted as arrow overlays on the price panel for direct
charting-overlay use.

Simplified close-only Vortex with sum-of-window normalisation:

```
TR[i]    = i === 0 ? 0 : |close[i] - close[i-1]|
VMp[i]   = i === 0 ? 0
                  : close[i] > close[i-1]
                      ? close[i] - close[i-1] : 0
VMn[i]   = i === 0 ? 0
                  : close[i] < close[i-1]
                      ? close[i-1] - close[i] : 0
sumTR[i] = sum(TR[i-length+1 .. i])
sumVMp[i]= sum(VMp[i-length+1 .. i])
sumVMn[i]= sum(VMn[i-length+1 .. i])
VIplus[i] = sumTR === 0 ? 0.5 : sumVMp / sumTR
VIminus[i]= sumTR === 0 ? 0.5 : sumVMn / sumTR
bullish : (VIplus - VIminus) crosses up
bearish : (VIplus - VIminus) crosses down
```

Defaults: `length = 14` (canonical Vortex). Regime classifier:
`bullish` (VIplus > VIminus), `bearish` (VIplus < VIminus),
`neutral` (VIplus === VIminus), `none` (either side null).

## Bit-exact anchors (three)

- **CONST close = K**: every TR = VMp = VMn = 0 -> sumTR = 0
  -> VIplus = VIminus = 0.5 every bar after warmup. VIplus
  === VIminus -> regime `neutral`, cross count = 0. Verified
  across multiple K including K = 0.
- **LINEAR UP step > 0**: every close > close[i-1], so VMp =
  step, VMn = 0, TR = step. sum(VMp) = length*step, sum(TR) =
  length*step -> VIplus = 1, VIminus = 0 every bar. Regime
  `bullish`, cross count = 0 (no transition since starts
  bullish at warmup).
- **LINEAR DOWN step < 0**: symmetric, VIplus = 0, VIminus =
  1, regime `bearish`, cross count = 0.

## Surface

- Helpers exported: `getLineVortexCrossFinitePoints`,
  `normalizeLineVortexCrossLength`, `computeLineVortexCross`,
  `classifyLineVortexCrossRegime`,
  `detectLineVortexCrossCrosses`, `runLineVortexCross`,
  `computeLineVortexCrossLayout`,
  `describeLineVortexCrossChart`.
- Component: `<ChartLineVortexCross />` (forwardRef on a
  `<div>` wrapping the SVG + legend + badge).
- Data attrs: `data-section="chart-line-vortex-cross"`,
  `data-length`, `data-total-points`, `data-bullish-count`,
  `data-bearish-count`, `data-neutral-count`, `data-cross-count`.
- Accessibility: `role="region"` + `aria-label` +
  `aria-describedby`, inner `<svg role="img">`, sr-only desc,
  motion-safe fade-in; oscillator cross markers carry
  `role="graphics-symbol"` + `tabIndex={0}` + descriptive
  `aria-label`. Overlay arrow markers on the price panel carry
  the same attributes for direct charting-overlay use.
- Legend: three toggleable buttons (`close`, `VI+`, `VI-`);
  controlled / uncontrolled modes; `onSeriesToggle` reports
  diffs.
- Tooltip: `x`, `close`, `VI+`, `VI-`, regime, running regime
  counts, and total cross count.
- Renderer: top panel renders the close with bullish / bearish
  arrow overlays at every cross trigger; bottom panel overlays
  VI+ and VI- with a dashed 0.5 mid-line reference and dot
  markers at the same cross indices. The oscillator axis is
  fixed to [0, 1] (VI values are normalised proportions).

## Tests (57 cases)

- `getLineVortexCrossFinitePoints` (3) -- null, NaN, null
  entries.
- `normalizeLineVortexCrossLength` (3) -- default, below-1,
  accept.
- `computeLineVortexCross` (6) -- null, CONST -> 0.5/0.5,
  LINEAR UP -> 1/0, LINEAR DOWN -> 0/1, defaults, no mutation.
- `classifyLineVortexCrossRegime` (4) -- bullish, bearish,
  neutral, none.
- `detectLineVortexCrossCrosses` (3) -- bullish, bearish, skip
  null.
- `runLineVortexCross` (8) -- ok flags, defaults, options,
  sort by x, CONST neutral / 0 crosses, LINEAR UP bullish /
  0 crosses, LINEAR DOWN bearish / 0 crosses.
- `computeLineVortexCrossLayout` (5) -- empty, ok=true, panel
  stack, osc [0,1] + midY anchored, price + plus paths.
- `describeLineVortexCrossChart` (3) -- empty, mention,
  length.
- `<ChartLineVortexCross />` (21) -- empty placeholder, region
  role, ref forward, data attrs, cross count, aria desc, three
  legend items, legend toggle, controlled hide, badge,
  midline / axis / grid / legend / crosses / overlay crosses
  on/off, className+style, animate off, plus+minus paths,
  price path, defaultHiddenSeries.
- Vortex Cross integration (1) -- three regime anchors verified
  bit-exact across multiple K, start values.

## Files

- web/src/components/ui/chart-line-vortex-cross.tsx (new)
- web/src/components/ui/chart-line-vortex-cross.test.tsx (new)
- web/src/components/ui/index.ts (barrel export added)
- package.json + package-lock.json + web/package.json +
  web/package-lock.json bumped to 1.11.914
- CHANGELOG.md prepended

## Verification

- `npx vitest run
  src/components/ui/chart-line-vortex-cross.test.tsx` -- 57/57
  pass.
- Typecheck: no new errors introduced.
