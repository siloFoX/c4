# 11.576 -- web/src/components/ui/chart-line-macd.tsx

Pure-SVG line chart with a **MACD** (Moving Average
Convergence Divergence) sub-panel. The price line sits in
the top panel; the bottom panel renders the MACD momentum
study -- the MACD line, the signal line and the convergence
histogram.

## The MACD computation

`runLineMacd(points, options)`:

- `computeLineMacdEMA(values, period)` -- an exponential
  moving average (`alpha = 2 / (period + 1)`, seeded with
  the first value, so it is defined at every index).
- The **MACD line** is the fast EMA minus the slow EMA of
  the price (default periods 12 and 26).
- The **signal line** is an EMA of the MACD line (default
  period 9).
- The **histogram** is `macd - signal` -- the convergence /
  divergence of the two lines. Positive bars (MACD above
  signal) read green; negative bars read red.

All three series are defined at every index because the
EMAs are seeded.

The component fixture (periods 1/3/3 on a 4-point series)
makes every value exact: the MACD line is `[0, 5, 7.5,
-1.25]`, the signal line `[0, 2.5, 5, 1.875]`, and the
histogram `[0, 2.5, 2.5, -3.125]`.

## Distinct from neighbouring primitives

MACD layers three EMAs into one momentum study with a
sign-tinted histogram. `chart-line-ewma` overlays a single
EMA; `chart-line-keltner` builds an EMA centre with an ATR
band; `chart-line-momentum` is a sub-panel oscillator driven
by a derivative, not a difference-of-EMAs. None compute the
fast/slow EMA spread, the signal smoothing or the
convergence histogram.

All exported helpers carry a `LineMacd` infix so the
`export *` barrel line cannot collide.

## Public surface

- `<ChartLineMacd data={...} />` -- a single
  `ChartLineMacdPoint[]` (`{x, value}`).
- `fastPeriod` / `slowPeriod` / `signalPeriod` -- the three
  EMA periods (defaults 12 / 26 / 9).
- `pricePanelRatio` -- the fraction of the height given to
  the price panel (default 0.58; clamped to [0.3, 0.8]).
- `gap` -- the pixel gap between the stacked panels.
- Toggle flags: `showAxis`, `showGrid`, `showDots` (default
  false), `showMacd`, `showSignal`, `showHistogram`,
  `showTooltip`, `showConfigBadge`, `showLegend`, `animate`.
- Controlled + uncontrolled visibility (`hiddenSeries` keys
  `price` / `macd` / `signal` / `histogram`).
- Standard sizing + format + a11y props. `formatValue` /
  `formatX`.
- `onPointClick({point})` / `onSeriesToggle({seriesId,
  hidden})`.

## Pure helpers

- `getLineMacdFinitePoints(points)`.
- `normalizeLineMacdPeriod(period, fallback)`.
- `computeLineMacdEMA(values, period)`.
- `runLineMacd(points, options?)` -- the MACD / signal /
  histogram computation.
- `computeLineMacdLayout({...})` -- stacks the price panel
  above the MACD panel and projects both.
- `describeLineMacdChart(data, options?)`.

## ARIA + test selectors

- Root `role="region"` + `aria-label` + `aria-describedby`.
- SVG `role="img"`.
- The price line and every price dot `role="graphics-symbol"`
  + `tabIndex={0}`.
- Every node carries `data-section="chart-line-macd-..."`.
- Root mirrors `data-fast-period`, `data-slow-period`,
  `data-signal-period`, `data-total-points`, `data-animate`.
- Axis / tick / grid / panel-label nodes carry
  `data-panel="price"|"macd"`. Histogram bars expose
  `data-histogram` and `data-positive`.

## Tooltip / badge / legend

Hovering a price dot or a histogram bar shows x, price, and
the MACD / signal / histogram values at that index. Config
badge: `MACD <fast>/<slow>/<signal>`. The legend has four
toggle items (Price / MACD / Signal / Histogram).

## Tests

53 vitest cases in `chart-line-macd.test.tsx`, all passing:

- Defaults, `getLineMacdFinitePoints`.
- `normalizeLineMacdPeriod`: keeps a positive integer, **a
  sub-1 period falls back**, floors a fractional period,
  non-finite falls back.
- `computeLineMacdEMA`: **a period of 1 is the identity**,
  **smooths with alpha = 2/(period+1)**, a constant series
  stays constant, empty -> [].
- `runLineMacd`: empty / single point -> ok=false, resolves
  the default periods, **the MACD line is the fast EMA minus
  the slow EMA**, **the signal line is an EMA of the MACD
  line**, **the histogram is the MACD minus the signal**,
  the samples carry every per-period value, sorts by x,
  honours custom periods.
- `computeLineMacdLayout`: empty / degenerate -> ok=false,
  **stacks a price panel above a MACD panel**, price + MACD
  + signal paths, **one histogram bar per period**,
  histogram bars carry the value + positive flag, **the
  MACD panel y range is symmetric about zero**, price dots
  carry the macd / signal / histogram values, periods +
  point count.
- `describeLineMacdChart`: No data, summary mentions MACD +
  signal + histogram.
- `<ChartLineMacd>` render: empty, price line path, MACD +
  signal lines + hide, **one histogram bar per period +
  hide**, histogram bars carry the positive flag, the MACD
  zero line, panel labels, price dots off by default + on,
  config badge periods + hide, ARIA region + img + sr-only
  desc, root data-*, tooltip on a price dot (price + macd +
  signal + histogram) + hide on leave, omit tooltip via
  prop, onPointClick, legend four toggle items + toggle, the
  legend toggle hides a line, hide legend, animate flag +
  class, ref forwarding, displayName, custom ariaLabel.

## Files

- `web/src/components/ui/chart-line-macd.tsx`
- `web/src/components/ui/chart-line-macd.test.tsx`
- `web/src/components/ui/index.ts` -- appended
  `export * from './chart-line-macd';`
- `CHANGELOG.md` -- prepended `[1.11.594]` entry.
- `package.json`, `package-lock.json`, `web/package.json`,
  `web/package-lock.json` -- bumped to `1.11.594`.
