# Patch 11.880 -- UI: chart-line-atr-cross-pct primitive

**Version:** v1.11.898
**Queue TODO:** 11.880
**Branch:** c4/c4-mgr-auto
**Status:** done

## Summary

Added `<ChartLineAtrCrossPct />` -- a pure-SVG, dual-panel
React/TS primitive that renders the close in the top panel and
overlays the simplified single-series Average True Range (ATR),
its EMA-smoothed signal, and the `atrPct = (ATR - signal) /
close * 100` deviation channel in the bottom panel. The percent
treatment surfaces the *relative volatility momentum* (how
stretched ATR is above or below its signal, expressed as a
percent of the close) so the magnitude is comparable across
instruments at different price levels.

Single-series ATR (no high/low needed):

```
TR[i]     = i === 0 ? 0 : |close[i] - close[i-1]|
ATR[i]    = Wilder smooth of TR, length
signal[i] = EMA(ATR, signalLength)
atrPct[i] = close[i] === 0 ? null
                           : (ATR[i] - signal[i]) / close[i] * 100
```

Defaults: `length = 14` (canonical), `signalLength = 9`. Regime
classifier: `above` (atrPct > 0), `below` (< 0), `at` (= 0),
`none` (null).

## Bit-exact anchors (three)

- **CONST close = K (K > 0)**: every TR = 0 -> ATR = 0 ->
  signal EMA of 0s = 0 -> atrPct = 0 / K * 100 = 0 every bar
  after warmup. Verified across multiple K. K = 0 triggers
  divide-by-zero guard -> pct = null.
- **LINEAR UP step = 1**: every TR = 1 (after the first bar)
  -> ATR = 1 via the Wilder short-circuit -> signal EMA of 1s
  = 1 -> atrPct = (1 - 1) / close * 100 = 0.
- **LINEAR DOWN step = -1**: same magnitudes, TR = 1 every bar
  -> ATR = 1 -> signal = 1 -> atrPct = 0 (with close > 0
  maintained by choosing a start value larger than the bar
  count).

## Surface

- Helpers exported: `getLineAtrCrossPctFinitePoints`,
  `normalizeLineAtrCrossPctLength`,
  `applyLineAtrCrossPctWilder`, `applyLineAtrCrossPctEma`,
  `computeLineAtrCrossPct`, `classifyLineAtrCrossPctRegime`,
  `runLineAtrCrossPct`, `computeLineAtrCrossPctLayout`,
  `describeLineAtrCrossPctChart`.
- Component: `<ChartLineAtrCrossPct />` (forwardRef on a
  `<div>` wrapping the SVG + legend + badge).
- Data attrs: `data-section="chart-line-atr-cross-pct"`,
  `data-length`, `data-signal-length`, `data-total-points`,
  `data-above-count`, `data-below-count`, `data-at-count`.
- Accessibility: `role="region"` + `aria-label` +
  `aria-describedby`, inner `<svg role="img">`, sr-only desc,
  motion-safe fade-in.
- Legend: four toggleable buttons (`close`, `atr`, `signal`,
  `atrPct`); controlled / uncontrolled modes; `onSeriesToggle`
  reports diffs.
- Tooltip: `x`, `close`, `atr`, `signal`, `atrPct`, regime,
  plus running regime counts.
- Renderer: top panel renders the close, bottom panel overlays
  ATR, signal, and atrPct over a dashed zero reference.

## Tests (52 cases)

- `getLineAtrCrossPctFinitePoints` (3) -- null, NaN, null
  entries.
- `normalizeLineAtrCrossPctLength` (3) -- default, below-1,
  accept.
- `applyLineAtrCrossPctWilder` (1) -- CONST bit-exact across K.
- `applyLineAtrCrossPctEma` (1) -- CONST bit-exact across K.
- `computeLineAtrCrossPct` (7) -- null, CONST K > 0 ->
  0/0/0, CONST K = 0 -> 0/0/null, LINEAR UP -> 1/1/0, LINEAR
  DOWN -> 1/1/0, defaults, no mutation.
- `classifyLineAtrCrossPctRegime` (4) -- above, below, at,
  none.
- `runLineAtrCrossPct` (6) -- ok flags, defaults, options,
  sort by x, CONST K > 0 regime at.
- `computeLineAtrCrossPctLayout` (5) -- empty, ok=true, panel
  stack, zero in osc axis, price + atr paths.
- `describeLineAtrCrossPctChart` (3) -- empty, mention, both
  lengths.
- `<ChartLineAtrCrossPct />` (18) -- empty placeholder, region
  role, ref forward, data attrs, aria desc, four legend items,
  legend toggle, controlled hide, badge, zero / axis / grid /
  legend on/off, className+style, animate off, atr+signal+pct
  paths, price path, defaultHiddenSeries.
- ATR Cross Pct integration (1) -- three bit-exact anchors:
  CONST K > 0 -> 0/0/0, LINEAR UP -> 1/1/0, LINEAR DOWN ->
  1/1/0 verified across multiple K / start values.

## Difference vs other volatility primitives

| primitive | volatility input | use-case |
| --- | --- | --- |
| (no existing close-only ATR) | -- | -- |
| `chart-line-atr-cross-pct` (11.880) | abs(close[i] - close[i-1]) | volatility momentum scaled to price |

The single-series simplified TR uses `abs(close[i] - close[i-
1])` because we do not carry high/low fields in the cross-pct
family. This still captures bar-to-bar movement magnitude and
gives a clean Wilder-smoothed volatility band whose deviation
from its signal can be expressed as a percent of price for
cross-instrument comparison.

## Files

- web/src/components/ui/chart-line-atr-cross-pct.tsx (new)
- web/src/components/ui/chart-line-atr-cross-pct.test.tsx (new)
- web/src/components/ui/index.ts (barrel export added)
- package.json + package-lock.json + web/package.json +
  web/package-lock.json bumped to 1.11.898
- CHANGELOG.md prepended

## Verification

- `npx vitest run
  src/components/ui/chart-line-atr-cross-pct.test.tsx` -- 52/52
  pass.
- Typecheck: no new errors introduced.
