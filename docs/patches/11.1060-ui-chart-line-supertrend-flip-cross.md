# 11.1060 -- UI: chart-line-supertrend-flip-cross primitive (v1.11.1078)

## Scope

Add a pure-SVG dual-panel `ChartLineSupertrendFlipCross`
component that renders the close in the top panel and the
Supertrend trailing-stop line in the bottom panel, marking
bullish (trend flips downtrend -> uptrend, trend stop
reversal flip up) / bearish (trend flips uptrend ->
downtrend, trend stop reversal flip down) Supertrend flip
events with bias coloring derived from the supertrend
slope at the flip bar.

This primitive watches the **trend state transitions**
rather than the supertrend line crossing a signal line. A
flip event is the canonical Supertrend trading signal:
the close has broken through the active trailing stop,
triggering a trend reversal.

## Files

- `web/src/components/ui/chart-line-supertrend-flip-cross.tsx`
  -- new primitive (HLC input, SMA-based ATR, HL2-
  centered upperBand / lowerBand, ratcheting finalUpper /
  finalLower, supertrend trend flip detection, layout,
  accessible SVG component).
- `web/src/components/ui/chart-line-supertrend-flip-cross.test.tsx`
  -- 40 vitest cases.
- `web/src/components/ui/index.ts` -- barrel export
  appended.
- `package.json` 1.11.1077 -> 1.11.1078.
- `web/package.json` 1.11.1077 -> 1.11.1078.
- `package-lock.json` root version bumped.
- `web/package-lock.json` web version bumped.

## Supertrend flip-cross pipeline

```
tr[i]         = max(high - low,
                    |high - close[i-1]|,
                    |low  - close[i-1]|)
atr[i]        = SMA(tr, period)
hl2[i]        = (high[i] + low[i]) / 2
upperBand[i]  = hl2[i] + multiplier * atr[i]
lowerBand[i]  = hl2[i] - multiplier * atr[i]

finalUpper[i] = (upperBand[i] < finalUpper[i-1] ||
                 close[i-1] > finalUpper[i-1])
                ? upperBand[i]
                : finalUpper[i-1]
finalLower[i] = (lowerBand[i] > finalLower[i-1] ||
                 close[i-1] < finalLower[i-1])
                ? lowerBand[i]
                : finalLower[i-1]

Initial trend seeded by sign(close[period] -
close[period-1]) (positive or zero means uptrend,
negative means downtrend). This keeps canonical LINEAR
anchors flip-free.

trend[i]      = +1 (uptrend) if close > prev finalUpper
                -1 (downtrend) if close < prev finalLower
                prev trend otherwise
supertrend[i] = uptrend ? finalLower[i] : finalUpper[i]

bullish (flip-up trigger) :
  prev trend === -1 && cur trend === +1
bearish (flip-down trigger) :
  prev trend === +1 && cur trend === -1

bias        : supertrend[i] vs supertrend[i-1] -> up /
              down / flat / none
```

Default `period = 10`, `multiplier = 3` -- canonical
Seban tuning.

Uses SMA-based ATR (matching the
chart-line-supertrend-mid-cross-sig v1.11.1068 /
chart-line-atr-* family convention) rather than Wilder
ATR (used by chart-line-supertrend-divergence-cross
v1.11.1062). Family-level tuning choice documented.

## Sibling family

The Supertrend signal family has three distinct
primitives:

- **chart-line-supertrend-mid-cross-sig v1.11.1068** --
  supertrend line vs its own SMA signal line cross
  (mid-cross-sig family convention)
- **chart-line-supertrend-divergence-cross v1.11.1062**
  -- price-vs-supertrend direction divergence
  (divergence-cross family convention)
- **chart-line-supertrend-flip-cross v1.11.1078** (this
  primitive) -- raw trend state flip events
  (canonical Supertrend trading signal)

Each surfaces a different signal niche. The flip-cross
variant is the most canonical -- it captures the moment
when the trailing stop is breached, which is the
original trading-rule application of Supertrend.

Warmup is `period = 10` for the default tuning: ATR and
supertrend both seed at i = period = 10. Flip detection
requires the previous bar's trend, so the first
potential flip lands at i = period + 1 = 11.

## Bit-exact anchors

All anchors use HLC input.

- **CONST band** `high = K + 1`, `low = K - 1`, `close
  = K`: tr = 2, atr = 2. hl2 = K. upperBand = K + 6,
  lowerBand = K - 6. close[period] - close[period-1]
  = 0 -> init uptrend. supertrend = finalLower = K - 6
  (constant). close = K is always > prev finalLower
  (K - 6) -> trend stays uptrend. 0 flips. Verified
  across K in {0, 1, 50, 200, 1234}.
- **LINEAR UP** `high = i + 1`, `low = i - 1`, `close
  = i`: tr = 2, atr = 2. close[period] -
  close[period-1] = +1 -> init uptrend. close = i is
  always > prev finalLower = i - 7 -> stays uptrend.
  supertrend = finalLower = i - 6 (slope +1). 0 flips.
- **LINEAR DOWN** `high = -i + 1`, `low = -i - 1`,
  `close = -i`: tr = 2, atr = 2. close[period] -
  close[period-1] = -1 -> init downtrend. close = -i
  < prev finalUpper = -i + 7 -> stays downtrend.
  supertrend = finalUpper = -i + 6 (slope -1). 0 flips.

All three anchors produce zero flip events -- the trend
state stays in its initial direction. Flips only fire
when price has enough curvature to break through the
trailing stop, which doesn't happen on monotonic LINEAR
input.

## Test coverage

40 vitest cases:

1. defaults: dimensions, period, multiplier
2. `getLineSupertrendFlipCrossFinitePoints`: filters NaN
   / Infinity / high < low
3. normalizers: floor + >= 1 length bound, > 0
   multiplier bound
4. `computeLineSupertrendFlipCross`: CONST supertrend =
   K - 6 + trend uptrend, LINEAR UP supertrend = i - 6
   + trend uptrend, LINEAR DOWN mirror, empty
5. `classifyLineSupertrendFlipCrossBias`: up / down /
   flat / none
6. `detectLineSupertrendFlipCrossCrosses`: bullish on
   `-1 -> +1` transition with bias from supertrend
   slope, bearish on `+1 -> -1` transition with bias
   from supertrend slope, no double-fire while in same
   trend state, skips null-window bars
7. `runLineSupertrendFlipCross`: CONST band x5, LINEAR
   UP (uptrend, 0 flips), LINEAR DOWN (downtrend, 0
   flips), sort, ok=false short, empty, custom tuning,
   trend + none counts sum to series length
8. `computeLineSupertrendFlipCrossLayout`: LINEAR UP
   paths, empty, custom dims, degenerate range padding
9. `describeLineSupertrendFlipCrossChart`: empty,
   count + period + trend stop reversal flip mention
10. component render: region, empty fallback,
    supertrend path, legend off, badge, aria, counters,
    hidden series

Result: 40/40 pass.

## Telemetry surface

`data-section="chart-line-supertrend-flip-cross"`
exposes:

- `data-period`, `data-multiplier`, `data-total-points`
- `data-uptrend-count`, `data-downtrend-count`,
  `data-none-count`
- `data-bullish-cross-count`, `data-bearish-cross-count`
- `data-up-bias-count`, `data-down-bias-count`,
  `data-flat-bias-count`
- `data-cross-count`

Plus a debug-audit surface on the `run` object:
`atrValues`, `supertrendValues`, `trendValues` (1 / -1 /
null) for bit-exact verification.

## Accessibility

- `role="region"`, descriptive `aria-label`, narrative
  `describeLineSupertrendFlipCrossChart` description
  that spells out the trend stop reversal flip semantics
  and mentions Seban's Supertrend formulation.
- Trigger markers are individually focusable
  (`tabIndex={0}`) with kind+bias `aria-label`.
- Hover targets respond to mouse and keyboard focus.
- Legend buttons (close / supertrend) are keyboard-
  toggleable via Enter / Space.

## Risk

- Pure-SVG, deterministic `.toFixed(2)`, no canvas, no
  chart lib dependency.
- Trend flip semantics are the canonical Supertrend
  trading signal. Many traders use flip events as the
  primary entry/exit signal, with the supertrend line
  itself as the trailing stop.
- Initial trend detection via sign(close[period] -
  close[period-1]) keeps LINEAR anchors flip-free. The
  more common "always start uptrend" convention used by
  some libraries would produce a single mid-data trend
  flip on LINEAR DOWN; this primitive avoids that
  ambiguity (matches the supertrend-mid-cross-sig
  sibling).
- SMA-based ATR (not Wilder) for bit-exact testability.
  Documented as a family-level tuning choice.
- The supertrend line itself jumps discontinuously at
  flip bars (from finalLower to finalUpper or vice
  versa). The slope-based bias at the flip bar therefore
  typically reads as a large "up" jump (bearish flip)
  or "down" jump (bullish flip) -- this is canonical
  and reflects the discontinuity.
- On steady LINEAR input the trend stays in its initial
  direction (zero flips), which is correct canonical
  behaviour. Real-world price data with reversals
  produces typical Supertrend flip patterns.
