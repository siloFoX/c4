# 11.773 chart-line-hilbert-cycle (v1.11.791)

## Summary

Pure-SVG dual-panel React/TypeScript chart primitive with a
**Hilbert Transform cycle** oscillator panel beneath the close.
The cycle is the SMA-smoothed Quadrature component of the
analytic signal derived from the detrended close. Added as the
`<ChartLineHilbertCycle />` UI primitive in
`web/src/components/ui/`.

## Algorithm

For each bar with smooth length `smoothLength` (default 4):

```
detrender[i] = 0.0962 * close[i] + 0.5769 * close[i - 2]
             - 0.5769 * close[i - 4] - 0.0962 * close[i - 6]
I[i]         = detrender[i - 3]                  (in-phase)
Q[i]         = 0.0962 * detrender[i] + 0.5769 * detrender[i - 2]
             - 0.5769 * detrender[i - 4] - 0.0962 * detrender[i - 6]
smoothedI[i] = SMA(I, smoothLength)
smoothedQ[i] = SMA(Q, smoothLength)
cycle[i]     = smoothedQ[i]
```

The 6-tap Hilbert FIR uses Ehlers' canonical coefficients
`(0.0962, 0, 0.5769, 0, -0.5769, 0, -0.0962)`. Bars before the
combined warmup `i = 12 + smoothLength` (6 FIR + 6 FIR + SMA -
1) are `null`. The component also exposes the smoothed
In-Phase as a togglable secondary series.

## Anchors

- **CONST_FLAT (close == 0) -> cycle = 0 bit-exact** past
  warmup. Verified across `smoothLength in {1, 2, 4, 6, 10}`.
- **CONST_FLAT (close == K, K != 0) -> cycle ~ 0 to numerical
  tolerance** (12 decimal places). Verified across
  `(K, smoothLength) in {1, 7, 100} x {1, 4, 6}`.
- **CONST_FLAT (close == 0) -> inPhase = 0 bit-exact** past
  inPhase warmup `12 + smoothLength - 1`.
- **SMA helper**: zeros stay zero bit-exact; constant K stays
  K bit-exact at every bar past the warmup.
- **FIR helper**: at K = 0 the FIR is exactly 0; for K != 0
  the FIR is within ULPs of zero (numerical).
- **Sinusoid**: produces a non-trivial cycle reading after the
  warmup window (sanity).
- **Translation invariance**: shifting the close by a constant
  leaves the cycle unchanged (both FIR taps and the smoothed
  Quadrature are insensitive to DC offset).

## Files

- `web/src/components/ui/chart-line-hilbert-cycle.tsx`
  -- helpers + component.
- `web/src/components/ui/chart-line-hilbert-cycle.test.tsx`
  -- 93 vitest cases.
- `web/src/components/ui/index.ts` -- barrel export.
- `CHANGELOG.md` -- v1.11.791 entry.
- `package.json`, `package-lock.json`, `web/package.json`,
  `web/package-lock.json` -- 4-manifest bump.

## Component surface

- Props: `data` (array of `{x, close}`), `smoothLength`,
  `width`, `height`, `padding`, `panelGap`, `tickCount`,
  `strokeWidth`, `dotRadius`, colour overrides, `showAxis`,
  `showGrid`, `showDots`, `showCycle`, `showInPhase`,
  `showMarkers`, `showTooltip`, `showConfigBadge`,
  `showLegend`, `showZeroLine`, `animate`, `hiddenSeries`,
  `defaultHiddenSeries`, `onSeriesToggle`, `onPointClick`,
  `formatPrice`, `formatCycle`, `formatX`, `ariaLabel`,
  `ariaDescription`, `className`, `style`.
- Helpers: `getLineHilbertCycleFinitePoints`,
  `normalizeLineHilbertCycleLength`,
  `applyLineHilbertCycleFir`, `applyLineHilbertCycleSma`,
  `computeLineHilbertCycle`, `classifyLineHilbertCycleZone`,
  `runLineHilbertCycle`, `computeLineHilbertCycleLayout`,
  `describeLineHilbertCycleChart`.
- Accessibility: `role="region"` root with `aria-label` +
  `aria-describedby`; one `role="img"` SVG; per-point
  `role="graphics-symbol"` markers with keyboard support
  (`Enter` / `Space`).
- ARPS design system tokens via inline `style={{ fontFamily:
  'var(--font-sans, ...)' }}`.

## Test plan

- `npm --prefix web test -- src/components/ui/chart-line-hilbert-cycle`
  -- 93 cases, all pass.
- `npm --prefix web run typecheck` -- clean for the two new files.
