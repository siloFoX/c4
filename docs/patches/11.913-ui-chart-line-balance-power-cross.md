# Patch 11.913 -- UI: chart-line-balance-power-cross primitive

## Summary

Added `<ChartLineBalancePowerCross />` -- pure-SVG dual-panel
React/TS primitive that renders the close (top panel) with
bullish / bearish arrow overlays at every Balance of Power
zero crossover, and the smoothed BoP oscillator (bottom panel)
with cross dot markers and a dashed zero baseline. Zero-cross
variant of the BoP family that flags buyer / seller dominance
regime change events distinct from the absolute BoP magnitude.

## Algorithm

```
delta[i]   = close[i] - close[i-1]               (close-only open proxy)
hi[i]      = max(close[i-b+1..i])                (range proxy)
lo[i]      = min(close[i-b+1..i])
range[i]   = hi - lo
raw[i]     = range > 0 ? delta / range : 0
bop[i]     = SMA(raw, smoothLength)
bullish   : bop crosses up   (prev <= 0, cur > 0)
bearish   : bop crosses down (prev >= 0, cur < 0)
```

Defaults `barLength = 5` (high / low rolling window),
`smoothLength = 3` (canonical BoP smoothing). Regime classifier
`bullish` (bop > 0), `bearish` (bop < 0), `neutral` (bop ===
0), `none` (bop null). Range -1..1 normalized.

## Bit-exact anchor

CONST close = K: every delta = 0 -> raw numerator = 0; range
= 0 -> divide-by-zero guard yields raw = 0. SMA of 0s = 0 via
the `min === max` precision short-circuit. bop = 0 -> regime
`neutral`, cross count = 0. Verified across K = 0, 1, 5, 17,
100, 1234.

LINEAR UP with step 1 has a clean bit-exact anchor: every
delta = 1, every rolling range = 4 (barLength = 5), so raw =
1/4 = 0.25 every bar, and the SMA of 0.25s = 0.25. LINEAR DOWN
yields raw = -0.25.

## API surface

- `<ChartLineBalancePowerCross />` `forwardRef<HTMLDivElement>`
- `getLineBalancePowerCrossFinitePoints(data)`
- `normalizeLineBalancePowerCrossLength(value, fallback)`
- `applyLineBalancePowerCrossSma(values, length)`
- `computeLineBalancePowerCross(series, options)`
- `classifyLineBalancePowerCrossRegime(bop)`
- `detectLineBalancePowerCrossCrosses(series, bop)`
- `runLineBalancePowerCross(data, options)`
- `computeLineBalancePowerCrossLayout(opts)`
- `describeLineBalancePowerCrossChart(data, options)`

## Visual

- Two stacked panels, ARPS design system colors via tokens.
- Top: close line + bullish/bearish polygon arrows at cross
  bars.
- Bottom: smoothed BoP line + cross dot markers + dashed zero
  baseline.
- Tooltip lists x / close / bop / regime / counts / crosses.
- Config badge + togglable legend (close / BoP).
- Cross marker is `role="graphics-symbol"` with `tabIndex={0}`.

## Test summary

`web/src/components/ui/chart-line-balance-power-cross.test.tsx`
-- 58 vitest cases, all pass.

Coverage includes:

- finite-point sanitization
- length normalization
- SMA CONST short-circuit
- compute / classify / detect helpers
- bit-exact CONST K (bop=0) anchor
- LINEAR UP (bop=0.25), LINEAR DOWN (bop=-0.25) anchors
- runLineBalancePowerCross defaults, custom lengths, x-sort
- layout panel stacking + zero line placement
- describe text contents
- component empty / region / ref forwarding
- data-attributes (lengths / counts)
- legend uncontrolled + controlled
- show* toggles (axis / grid / legend / crosses / overlay)
- animate=false class strip
- zero line render
- CONST integration anchor (K in {0, 1, 5, 17, 100, 1234})

## Touched

- `web/src/components/ui/chart-line-balance-power-cross.tsx`
  (new)
- `web/src/components/ui/chart-line-balance-power-cross.test.tsx`
  (new)
- `web/src/components/ui/index.ts` (barrel)
- `package.json`, `package-lock.json`, `web/package.json`,
  `web/package-lock.json` (1.11.930 -> 1.11.931)
- `docs/patches/11.913-ui-chart-line-balance-power-cross.md`
  (new)
- `CHANGELOG.md` (prepend)
- `/root/c4/docs/autonomous-queue-v10.md` row 11.913 -> done
