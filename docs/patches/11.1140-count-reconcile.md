# 11.1140 - reconcile 2 stale count-assertion tests to verified-grown registry entries

## Scope

2 count-assertion tests that drifted from the current registries
they verify. Each delta verified against the live source before
editing; both confirmed as legitimate registry growth, NOT bugs.

## Per-test verification

### TopTabs.test.tsx (1 failing test)

Test: `<TopTabs> > renders a tablist with one tab per TopView option`
expects `getAllByRole('tab').toHaveLength(ALL_VIEWS.length)`.

Test's local `ALL_VIEWS` array had 11 entries; component renders 12
distinct tabs (assertion received `12`, expected `11`).

Missing entry: `gallery`. Verified as a real shipped view:

- `web/src/components/layout/TopTabs.tsx:34` declares `gallery` in
  the `TopView` union.
- `TopTabs.tsx:59` registers it in the `TABS` array with label key
  `tab.gallery` and `LineChart` icon.
- `web/src/components/ChartLineGallery.tsx` exists (banner notes
  v1.11.1099, TODO 11.1081 - chart-line showcase gallery).
- `web/src/App.tsx:460` renders `<ChartLineGallery />` when
  `topView === 'gallery'`. `App.tsx:216` exposes a keyboard shortcut
  to `setTopView('gallery')`.
- i18n: `web/src/i18n/en.json:1295 "tab.gallery": "Gallery"` and the
  ko bundle mirrors it.

**Verdict: legitimate registry growth.** Fix: add `'gallery'` to the
test's `ALL_VIEWS` array (inserted between `'features'` and
`'settings'` to match component ordering).

### FeatureSidebar.test.tsx (1 failing test)

Test: `<FeatureSidebar> > filters the feature list by the typed
query (matches label, id, and description)` types `'scribe'` and
expects `toHaveLength(2)`. Filter now matches 3 features (assertion
received `3`, expected `2`).

The filter is OR-of-three across label / description / id. Verified
each match against `web/src/pages/registry.ts` (FEATURES) and the
i18n bundle:

1. `id: 'scribe'` (registry.ts:118) - matches via id + label
   (`feature.scribe.label = "Scribe"`).
2. `id: 'auto'` (registry.ts:174) - matches via description
   (`feature.auto.description = "Spawn an autonomous manager +
   scribe for a given task."`, i18n line 1127).
3. `id: 'settings-page'` (registry.ts:214) - matches via description
   (`feature.settingsPage.description = "Consolidated landing for
   theme, locale, scribe, notifications, feature flags and a link
   to the daemon config."`, i18n line 1196).

All 3 are real, registered FeatureDef entries with categories +
loaders. The 'scribe' substring entered the `feature.settingsPage.
description` text via prior settings-landing work.

**Verdict: legitimate registry growth.** Fix: update assertion to
`toHaveLength(3)`, extend the test comment to name the third match,
add a `.some(... /Settings/ ...)` assertion for the new matching
feature.

## Verification

- `tsc --noEmit -p web/tsconfig.json`: 0 errors (PROJECT-WIDE GREEN
  preserved).
- `npm run build`: green (~10s).
- `vitest run` on the 2 test files: 2/2 files pass + 54/54 tests
  pass.
- `git diff --name-only` after this patch: only the 2 dispatched
  test files (zero source files):
  - `web/src/components/layout/FeatureSidebar.test.tsx`
  - `web/src/components/layout/TopTabs.test.tsx`
