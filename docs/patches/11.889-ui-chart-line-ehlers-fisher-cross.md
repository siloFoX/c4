# Patch 11.889 -- UI: chart-line-ehlers-fisher-cross primitive

**Version:** v1.11.907
**Queue TODO:** 11.889
**Branch:** c4/c4-mgr-auto
**Status:** done

## Summary

Added `<ChartLineEhlersFisherCross />` -- a pure-SVG, dual-panel
React/TS primitive that renders the close in the top panel and
overlays the Ehlers Fisher Transform with its EMA-smoothed
signal in the bottom panel, marking bullish / bearish cross
trigger events for *gaussian-normalised momentum turning
points*. Trigger-focused cross-sig variant paired with the
existing `chart-line-fisher-cross-pct` (11.887) magnitude
channel.

Canonical Ehlers Fisher with 0.66 / 0.67 / 0.5 mixing
coefficients:

```
highest[i] = max(close[i - length + 1 .. i])
lowest[i]  = min(close[i - length + 1 .. i])
norm[i]    = highest === lowest
               ? 0.5
               : (close[i] - lowest) / (highest - lowest)
xRaw[i]    = 0.66 * 2 * (norm[i] - 0.5) + 0.67 * x[i-1]
x[i]       = clamp(xRaw[i], -0.999, 0.999)
fisher[i]  = 0.5 * ln((1 + x[i]) / (1 - x[i])) +
             0.5 * fisher[i-1]
signal[i]  = EMA(fisher, signalLength)
bullish   : (fisher - signal) crosses up    (prev <= 0, cur > 0)
bearish   : (fisher - signal) crosses down  (prev >= 0, cur < 0)
```

Defaults: `length = 10` (canonical Ehlers window), `signalLength
= 9`. Regime classifier: `bullish` (fisher > signal), `bearish`
(fisher < signal), `neutral` (fisher === signal), `none` (either
side null).

## Bit-exact anchor

- **CONST close = K**: every highest === lowest = K so `norm =
  0.5` (the midpoint fallback) -> `2 * (norm - 0.5) = 0` ->
  `x[i] = 0.67 * x[i-1] = 0` (seeded at 0) -> `fisher[i] = 0.5
  * ln(1) + 0.5 * fisher[i-1] = 0`. signal EMA of 0s = 0.
  fisher === signal everywhere -> regime `neutral`, cross count
  = 0. Verified across K = 0..1234.

## Surface

- Helpers exported: `getLineEhlersFisherCrossFinitePoints`,
  `normalizeLineEhlersFisherCrossLength`,
  `applyLineEhlersFisherCrossEma`,
  `computeLineEhlersFisherCross`,
  `classifyLineEhlersFisherCrossRegime`,
  `detectLineEhlersFisherCrossCrosses`,
  `runLineEhlersFisherCross`,
  `computeLineEhlersFisherCrossLayout`,
  `describeLineEhlersFisherCrossChart`.
- Component: `<ChartLineEhlersFisherCross />` (forwardRef on a
  `<div>` wrapping the SVG + legend + badge).
- Data attrs: `data-section="chart-line-ehlers-fisher-cross"`,
  `data-length`, `data-signal-length`, `data-total-points`,
  `data-bullish-count`, `data-bearish-count`,
  `data-neutral-count`, `data-cross-count`.
- Accessibility: `role="region"` + `aria-label` +
  `aria-describedby`, inner `<svg role="img">`, sr-only desc,
  motion-safe fade-in; oscillator cross markers carry
  `role="graphics-symbol"` + `tabIndex={0}` + descriptive
  `aria-label`. Overlay arrow markers on the price panel carry
  the same attributes for direct charting-overlay use.
- Legend: three toggleable buttons (`close`, `fisher`,
  `signal`); controlled / uncontrolled modes; `onSeriesToggle`
  reports diffs.
- Tooltip: `x`, `close`, `fisher`, `signal`, regime, running
  regime counts, and total cross count.
- Renderer: top panel renders the close with bullish / bearish
  arrow overlays at every cross trigger; bottom panel overlays
  fisher with the signal line, dashed zero reference, and dot
  markers at the same cross indices.

## Tests (54 cases)

- `getLineEhlersFisherCrossFinitePoints` (3) -- null, NaN, null
  entries.
- `normalizeLineEhlersFisherCrossLength` (3) -- default,
  below-2, accept.
- `applyLineEhlersFisherCrossEma` (1) -- CONST bit-exact across
  K.
- `computeLineEhlersFisherCross` (4) -- null, CONST -> 0/0,
  defaults, no mutation.
- `classifyLineEhlersFisherCrossRegime` (4) -- bullish, bearish,
  neutral, none.
- `detectLineEhlersFisherCrossCrosses` (3) -- bullish, bearish,
  skip null.
- `runLineEhlersFisherCross` (6) -- ok flags, defaults, options,
  sort by x, CONST regime neutral / 0 crosses.
- `computeLineEhlersFisherCrossLayout` (5) -- empty, ok=true,
  panel stack, zero in osc axis, price + fisher paths.
- `describeLineEhlersFisherCrossChart` (3) -- empty, mention,
  both lengths.
- `<ChartLineEhlersFisherCross />` (21) -- empty placeholder,
  region role, ref forward, data attrs, cross count, aria desc,
  three legend items, legend toggle, controlled hide, badge,
  zero / axis / grid / legend / crosses / overlay crosses
  on/off, className+style, animate off, fisher+signal paths,
  price path, defaultHiddenSeries.
- Ehlers Fisher Cross integration (1) -- CONST K bit-exact
  fisher = 0, signal = 0, regime neutral, 0 crosses across K =
  0..1234.

## Relationship to existing Fisher primitives

| primitive | bottom panel | use-case |
| --- | --- | --- |
| `chart-line-fisher-cross` (11.864) | Fisher + cross dots | basic Fisher crossings |
| `chart-line-fisher-cross-pct` (11.887) | Fisher + signal + (fisher-signal) magnitude channel | momentum strength magnitude |
| `chart-line-ehlers-fisher-cross` (11.889) | Fisher + EMA signal + cross dots + price arrows | trigger-focused with overlay arrows on price |

11.889 completes the Ehlers Fisher family by pairing the percent
magnitude (11.887) with a dedicated trigger-focused variant that
includes the canonical price-panel arrow overlays used by the
cross-sig family (MACD, ATR, RSI, MFI, OBV, KVO, CCI, TRIX,
StochRSI).

## Files

- web/src/components/ui/chart-line-ehlers-fisher-cross.tsx (new)
- web/src/components/ui/chart-line-ehlers-fisher-cross.test.tsx
  (new)
- web/src/components/ui/index.ts (barrel export added)
- package.json + package-lock.json + web/package.json +
  web/package-lock.json bumped to 1.11.907
- CHANGELOG.md prepended

## Verification

- `npx vitest run
  src/components/ui/chart-line-ehlers-fisher-cross.test.tsx` --
  54/54 pass.
- Typecheck: no new errors introduced.
