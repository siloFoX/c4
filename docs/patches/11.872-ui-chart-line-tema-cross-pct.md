# Patch 11.872 -- UI: chart-line-tema-cross-pct primitive

**Version:** v1.11.890
**Queue TODO:** 11.872
**Branch:** c4/c4-mgr-auto
**Status:** done

## Summary

Added `<ChartLineTemaCrossPct />` -- a pure-SVG, dual-panel
React/TS primitive that overlays the close with the Triple
Exponential Moving Average (TEMA) in the top panel and renders
the `close - TEMA` deviation scaled to the close as a percent
in the bottom panel. The percent normalisation makes the
indicator comparable across instruments at different price
magnitudes:

```
ema1[i]    = EMA(close, length)
ema2[i]    = EMA(ema1,  length)
ema3[i]    = EMA(ema2,  length)
TEMA[i]    = 3 * ema1[i] - 3 * ema2[i] + ema3[i]
temaPct[i] = close[i] === 0 ? null
                            : (close[i] - TEMA[i]) / close[i] * 100
```

Regime classifier: `above` (temaPct > 0), `below` (< 0),
`at` (= 0), `none` (null). Defaults: `length = 14`.

## Bit-exact anchor

- **CONST close = K (K > 0)**: every EMA in the chain collapses
  to K via the SMA-seeded `min === max` precision fix.
  `TEMA = 3K - 3K + K = K`, deviation = 0, `temaPct = 0 / K *
  100 = 0`. Regime `at` everywhere after warmup. Verified across
  multiple K and length tuples. K = 0 triggers the divide-by-
  zero guard -> temaPct = null.

## Surface

- Helpers exported: `getLineTemaCrossPctFinitePoints`,
  `normalizeLineTemaCrossPctLength`, `applyLineTemaCrossPctEma`,
  `computeLineTemaCrossPct`, `classifyLineTemaCrossPctRegime`,
  `runLineTemaCrossPct`, `computeLineTemaCrossPctLayout`,
  `describeLineTemaCrossPctChart`.
- Component: `<ChartLineTemaCrossPct />` (forwardRef on a
  `<div>` wrapping the SVG + legend + badge).
- Data attrs: `data-section="chart-line-tema-cross-pct"`,
  `data-length`, `data-total-points`, `data-above-count`,
  `data-below-count`, `data-at-count`.
- Accessibility: `role="region"` + `aria-label` +
  `aria-describedby`, inner `<svg role="img">`, sr-only desc,
  motion-safe fade-in.
- Legend: three toggleable buttons (`close`, `tema`, `temaPct`);
  controlled / uncontrolled modes; `onSeriesToggle` reports
  diffs.
- Tooltip: `x`, `close`, `tema`, `temaPct`, regime, plus running
  regime counts.
- Renderer: top panel overlays close + TEMA, bottom panel
  renders the percent deviation with a dashed zero reference.

## Tests (52 cases)

- `getLineTemaCrossPctFinitePoints` (3) -- null, NaN, null
  entries.
- `normalizeLineTemaCrossPctLength` (3) -- default, below-2,
  accept.
- `applyLineTemaCrossPctEma` (1) -- CONST bit-exact across K.
- `computeLineTemaCrossPct` (5) -- null, CONST K > 0 bit-exact,
  CONST K = 0 null, length parity, no mutation.
- `classifyLineTemaCrossPctRegime` (4) -- above, below, at,
  none.
- `runLineTemaCrossPct` (7) -- ok flags, defaults, options,
  sort by x, CONST K > 0 regime at, CONST K = 0 regime none.
- `computeLineTemaCrossPctLayout` (5) -- empty, ok=true, panel
  stack, zero in pct axis, price + tema paths.
- `describeLineTemaCrossPctChart` (3) -- empty, mention, length.
- `<ChartLineTemaCrossPct />` (21) -- empty placeholder, region
  role, ref forward, data attrs, regime counts, aria desc, three
  legend items, legend toggle, controlled hide, badge, zero /
  axis / grid / legend on/off, className+style, animate off,
  tema+pct paths, price path, defaultHiddenSeries.
- TEMA Pct integration (1) -- CONST K > 0 TEMA = K bit-exact
  and temaPct = 0 across multiple K and length tuples.

## Files

- web/src/components/ui/chart-line-tema-cross-pct.tsx (new)
- web/src/components/ui/chart-line-tema-cross-pct.test.tsx (new)
- web/src/components/ui/index.ts (barrel export added)
- package.json + package-lock.json + web/package.json +
  web/package-lock.json bumped to 1.11.890
- CHANGELOG.md prepended

## Verification

- `npx vitest run
  src/components/ui/chart-line-tema-cross-pct.test.tsx` -- 52/52
  pass.
- Typecheck: no new errors introduced (pre-existing errors in
  unrelated components remain).
