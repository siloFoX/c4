# Patch 11.857 -- UI: chart-line-stoch-rsi-cross primitive

**Version:** v1.11.875
**Queue TODO:** 11.857
**Branch:** c4/c4-mgr-auto
**Status:** done

## Summary

Added `<ChartLineStochRsiCross />` -- a pure-SVG, dual-panel
React/TS primitive that renders the close on top and the
Stochastic RSI %K / %D pair on the bottom. Markers fire at every
K-vs-D crossover and carry a trigger tag derived from %K at the
cross bar:

- `oversold-exit`    -- `up` cross while %K <= oversoldLevel
- `overbought-exit`  -- `down` cross while %K >= overboughtLevel
- `neutral`          -- cross in the mid-zone

Defaults: `rsiLength = 14`, `stochLength = 14`, `kSmooth = 3`,
`dSmooth = 3`, `overboughtLevel = 80`, `oversoldLevel = 20`,
kColor `#0ea5e9` (sky), dColor `#f97316` (orange), overbought
line `#dc2626`, oversold line `#16a34a`, trigger markers green
/ red / slate.

## Formula

```
gain[i]   = max(0, close[i] - close[i-1])
loss[i]   = max(0, close[i-1] - close[i])
avgGain   = Wilder(gain, rsiLength)
avgLoss   = Wilder(loss, rsiLength)
RSI       = (avgGain === 0 && avgLoss === 0) ? 50
          : avgLoss === 0                    ? 100
          : avgGain === 0                    ? 0
          : 100 - 100 / (1 + avgGain/avgLoss)
stochRsi  = max(RSI, stochLength) === min(RSI, stochLength)
              ? null
              : (RSI - min) / (max - min) * 100
%K        = SMA(stochRsi, kSmooth)
%D        = SMA(%K, dSmooth)
```

Cross detection uses the standard prev-relation + strict-
inequality guard.

## Bit-exact anchors

The Wilder + SMA helpers carry the `min === max` precision fix:

- **CONST close = K**: every gain/loss = 0 -> RSI = 50 (neutral
  fallback) on every bar. Stoch lookback window over the constant
  RSI has range = 0 -> `stochRsi = null` forever. `%K = null,
  %D = null`. Zero crosses, regime `none` for every bar.
- **LINEAR UP close = i + 1**: every gain = 1, loss = 0 -> Wilder
  of constant 1 = 1 (min===max seed), avgLoss = 0 -> RSI = 100
  for every bar. Stoch range = 0 -> null forever -> zero crosses.
- **LINEAR DOWN close = N - i**: mirror image. RSI = 0 forever ->
  range = 0 -> null -> zero crosses.

Verified across multiple `(rsiLength, stochLength, kSmooth,
dSmooth)` tuples in the integration suite.

## Surface

- Helpers exported: `getLineStochRsiCrossFinitePoints`,
  `normalizeLineStochRsiCrossLength`, `normalizeLineStochRsiCross
  Smooth`, `normalizeLineStochRsiCrossLevel`,
  `applyLineStochRsiCrossWilder`, `applyLineStochRsiCrossSma`,
  `applyLineStochRsiCrossGainLoss`, `applyLineStochRsiCrossRsi`,
  `applyLineStochRsiCrossStoch`, `computeLineStochRsiCross`,
  `classifyLineStochRsiCrossRelation`,
  `classifyLineStochRsiCrossZone`,
  `classifyLineStochRsiCrossTrigger`,
  `detectLineStochRsiCrossCrosses`, `runLineStochRsiCross`,
  `computeLineStochRsiCrossLayout`,
  `describeLineStochRsiCrossChart`.
- Component: `<ChartLineStochRsiCross />` (forwardRef on a
  `<div>` wrapping the SVG + legend + badge).
- Data attrs: `data-section="chart-line-stoch-rsi-cross"`,
  `data-rsi-length`, `data-stoch-length`, `data-k-smooth`,
  `data-d-smooth`, `data-overbought-level`,
  `data-oversold-level`, `data-total-points`,
  `data-up-cross-count`, `data-down-cross-count`,
  `data-oversold-exit-count`, `data-overbought-exit-count`.
- Accessibility: `role="region"` + `aria-label` +
  `aria-describedby`, inner `<svg role="img">`, sr-only desc,
  `role="graphics-symbol"` + `tabIndex={0}` on each cross marker,
  motion-safe fade-in.
- Legend: three toggleable buttons (`close`, `%K`, `%D`);
  controlled (`hiddenSeries`) and uncontrolled
  (`defaultHiddenSeries`); `onSeriesToggle` reports diffs.
- Tooltip: `x`, `close`, `rsi`, `stochRsi`, `%K`, `%D`,
  `relation`, `zone`, `cross`, `trigger`, plus running
  oversold-exit / overbought-exit counts.
- Renderer: top panel for close (with optional dots), bottom
  panel for %K and %D lines inside a fixed [0, 100] axis with
  dashed overbought / oversold / midline references.

## Tests (83 cases)

- `getLineStochRsiCrossFinitePoints` (3) -- empty, NaN, null
  entries.
- Normalize helpers (3) -- length / smooth / level.
- `applyLineStochRsiCrossWilder` (1) -- CONST bit-exact across K.
- `applyLineStochRsiCrossSma` (2) -- CONST bit-exact, null
  breaks window.
- `applyLineStochRsiCrossGainLoss` (3) -- first bar null, gain
  only, loss only.
- `applyLineStochRsiCrossRsi` (4) -- neutral fallback, all gain,
  all loss, gain == loss.
- `applyLineStochRsiCrossStoch` (4) -- range = 0 null, 100 at
  high, 0 at low, null in window.
- `computeLineStochRsiCross` (10) -- null input, three anchors
  produce k=d=null, RSI = 100 / 0 / 50 bit-exact, length parity,
  no mutation.
- `classifyLineStochRsiCrossRelation` (4) -- bullish, bearish,
  equal, none.
- `classifyLineStochRsiCrossZone` (4) -- overbought, oversold,
  neutral, none.
- `classifyLineStochRsiCrossTrigger` (4) -- null without cross,
  oversold-exit, overbought-exit, neutral.
- `detectLineStochRsiCrossCrosses` (4) -- up, down, warmup,
  no second cross.
- `runLineStochRsiCross` (8) -- ok flags, defaults, options,
  sort by x, three zero-cross anchors.
- `computeLineStochRsiCrossLayout` (6) -- empty, ok=true, panel
  stack, [0, 100] osc axis, price path + dots, CONST K/D paths
  empty.
- `describeLineStochRsiCrossChart` (3) -- empty, mention,
  parameter reporting.
- `<ChartLineStochRsiCross />` (21) -- empty placeholder,
  region role, ref forward, data attrs, cross counts, aria
  description, three legend items, legend toggle, controlled
  hide, badge, overbought/oversold/midline visibility, axis/
  grid/legend on/off, className+style, animate off, price path,
  defaultHiddenSeries.
- Stoch RSI Cross integration (2) -- all three anchors zero
  crosses across multiple (rsiLength, stochLength, kSmooth,
  dSmooth) tuples; CONST RSI = 50 bit-exact across K and
  rsiLength.

## Files

- web/src/components/ui/chart-line-stoch-rsi-cross.tsx (new)
- web/src/components/ui/chart-line-stoch-rsi-cross.test.tsx (new)
- web/src/components/ui/index.ts (barrel export added)
- package.json + package-lock.json + web/package.json +
  web/package-lock.json bumped to 1.11.875
- CHANGELOG.md prepended

## Verification

- `npx vitest run
  src/components/ui/chart-line-stoch-rsi-cross.test.tsx`
  -- 83/83 pass.
- Typecheck: no new errors introduced (pre-existing errors in
  unrelated components remain).
