# 11.558 -- web/src/components/ui/chart-line-spearman.tsx

Pure-SVG **dual-axis line chart** with a **Spearman
rank-correlation badge**. A `primary` series renders against
the left y-axis and a `secondary` series against the right
y-axis; a top-left badge reports Spearman's rho between the
two, classified into a strength bucket and a direction.

## Math

**Spearman's rho** is the Pearson correlation coefficient
applied to the *ranks* of the two variables, not their raw
values:

1. Pair the two series by exact x match.
2. Convert each variable's paired values to ranks
   (`computeRanks`).
3. Compute the Pearson correlation of the rank pairs.

### Ranking with ties

`computeRanks` assigns rank 1 to the smallest value, 2 to
the next, and so on. **Tied values receive the average
(midrank) of the ranks they span** -- e.g. `[10, 20, 20, 30]`
ranks to `[1, 2.5, 2.5, 4]` because the two 20s occupy ranks
2 and 3. Using midranks makes the resulting coefficient the
correct tie-corrected Spearman's rho.

For tie-free data the rank-Pearson result equals the classic
shortcut formula `rho = 1 - 6 * sum(d^2) / (n * (n^2 - 1))`,
where `d` is the per-pair rank difference. The component
verifies this against the shortcut formula in tests (one
swapped pair -> `rho = 0.9`; a fully reversed series ->
`rho = -1`).

### Why Spearman, not Pearson

Because it works on ranks, Spearman measures **monotonic**
association of *any shape* -- not just linear -- and is
robust to outliers (ranks compress extreme values). The
test suite makes the distinction explicit: the pair
`([1,2,3,4], [1,10,100,1000])` is a perfectly monotonic but
strongly *non-linear* relationship. Pearson would score it
below 1 (about 0.91); Spearman scores it **exactly 1**,
because the ranks `[1,2,3,4]` line up perfectly.

`ok = false` (rho NaN) is returned when fewer than 2 pairs
exist, or when either variable is constant (zero rank
variance).

## Distinct from `<ChartLineCorrelation>`

`<ChartLineCorrelation>` is the **Pearson** dual-axis chart
-- it correlates the raw values and measures *linear*
association. ChartLineSpearman is the **rank-correlation**
counterpart: it correlates the ranks and measures *monotonic*
association. The two answer different questions -- "do these
move together linearly?" vs "do these move together in the
same order, however non-linearly?" -- and ChartLineSpearman
additionally reports whether ties are present (which affect
the interpretation of the coefficient).

## Public surface

- `<ChartLineSpearman primary={...} secondary={...} />`.
- `primary` / `secondary`: `ChartLineSpearmanSeries | null`
  -- `{id, label, data, color?}`. Primary draws on the left
  axis, secondary on the right.
- `strongThreshold` / `moderateThreshold` / `weakThreshold`
  (defaults 0.7 / 0.4 / 0.2) -- strength buckets for `|rho|`.
- `primaryColor`, `secondaryColor`, `positiveColor`,
  `negativeColor`, `neutralColor`.
- Bounds overrides: `xMin` / `xMax`, `primaryYMin` /
  `primaryYMax`, `secondaryYMin` / `secondaryYMax`.
- Toggle flags: `showAxis`, `showGrid`, `showDots`,
  `showLegend`, `showTooltip`, `showBadge`, `animate`.
- Standard sizing + format + a11y props.
- `formatValue` / `formatX` / `formatRho`.
- `xLabel` / `primaryYLabel` / `secondaryYLabel`.
- `onPointClick({series, point})`.

## Pure helpers

- `getLineSpearmanFinitePoints(points)`.
- `computeRanks(values)` -- ranks with midranks for ties;
  original order; empty -> `[]`.
- `pairLineSpearmanByX(a, b)` -- pairs two series by exact x
  match.
- `computeSpearmanCorrelation(pairs)` -- the rank-Pearson
  coefficient; returns `{rho, ok, hasTies}`; `ok = false`
  for fewer than 2 pairs or a constant variable.
- `classifyLineSpearmanStrength(rho, ...)` -- `'strong' |
  'moderate' | 'weak' | 'none'`.
- `classifyLineSpearmanDirection(rho)` -- `'positive' |
  'negative' | 'neutral'`.
- `computeLineSpearman(primary, secondary, ...)` -- the full
  `ChartLineSpearmanResult` (`rho`, `ok`, `pairCount`,
  `strength`, `direction`, `hasTies`).
- `computeLineSpearmanLayout({...})` -- dual-axis layout:
  primary on left, secondary on right, x / left / right tick
  arrays, the Spearman result.
- `describeLineSpearmanChart(primary, secondary, formatRho?)`.

## ARIA + test selectors

- Root `role="region"` + `aria-label` + `aria-describedby`.
- SVG `role="img"`.
- Each series path + every dot `role="graphics-symbol"` +
  `tabIndex={0}`.
- Every node carries `data-section="chart-line-spearman-..."`.
- Root mirrors `data-total-points`, `data-pair-count`,
  `data-spearman-ok`, `data-rho`, `data-strength`,
  `data-direction`, `data-has-ties`, `data-animate`.
- Series groups expose `data-role` (`primary` / `secondary`),
  `data-axis` (`left` / `right`), `data-series-color`,
  `data-series-finite-count`, `data-series-total-count`.
- Paths carry `data-role`; dots carry `data-role`,
  `data-point-index`, `data-x`, `data-y`. Axes / ticks carry
  `data-axis` (`x` / `left` / `right`).

## Badge

Top-left badge shows
`SR rho=<rho> <strength> <direction> n=<pairCount>`, plus a
`ties` marker when tied ranks are present. The badge text is
tinted by the correlation direction (green positive / red
negative / slate neutral).

## Tooltip

Hovering a dot shows the series label, x and y.

## Tests

65 vitest cases in `chart-line-spearman.test.tsx`, all
passing:

- Defaults: positive sizes.
- `getLineSpearmanFinitePoints`: drops non-finite, null.
- `computeRanks`: empty, single, sorted, reversed,
  unsorted-order preserved, **ties get the midrank
  ([10,20,20,30] -> [1,2.5,2.5,4])**, all-equal -> the
  average rank.
- `pairLineSpearmanByX`: exact x match, no overlap, null.
- `computeSpearmanCorrelation`: fewer than 2 pairs ->
  ok=false, **perfect monotonic increasing -> rho 1**,
  **perfect monotonic decreasing -> rho -1**, **non-linear
  monotonic data still scores rho 1**, **one swapped pair ->
  the d^2 formula result 0.9**, **anti-monotonic matches the
  d^2 formula (-1)**, constant series -> ok=false, **detects
  ties and computes rho with midranks**, no-ties ->
  hasTies false, rho clamped to [-1, 1].
- `classifyLineSpearmanStrength` / `Direction`: buckets,
  absolute value, non-finite.
- `computeLineSpearman`: strong positive, strong negative,
  missing series -> ok=false.
- `computeLineSpearmanLayout`: degenerate canvas, both null,
  primary-left / secondary-right axes, line paths, Spearman
  result exposed, primary-only render, bounds overrides,
  totalPoints, x / left / right tick arrays.
- `describeLineSpearmanChart`: No data, summary mentions
  Spearman rho + strength + direction, no-correlation
  message.
- `<ChartLineSpearman>` render: empty state, a path per
  series, series groups with role + axis, badge with rho +
  strength + direction + pair count, ties marker, hide
  badge, left + right + x axes, dots via showDots, ARIA
  region + img + sr-only desc, root data-*, tooltip with x
  + y rows + hide on leave + omit via prop, onPointClick
  payload, legend lists both series, omit legend,
  primary-only render, animate flag + class, ref forwarding,
  displayName, custom ariaLabel, xLabel + y-axis labels,
  anti-monotonic data renders a strong negative badge.

## Files

- `web/src/components/ui/chart-line-spearman.tsx`
- `web/src/components/ui/chart-line-spearman.test.tsx`
- `web/src/components/ui/index.ts` -- appended
  `export * from './chart-line-spearman';`
- `CHANGELOG.md` -- prepended `[1.11.576]` entry.
- `package.json`, `package-lock.json`, `web/package.json`,
  `web/package-lock.json` -- bumped to `1.11.576`.
