# Patch 11.870 -- UI: chart-line-macd-cross-pct primitive

**Version:** v1.11.888
**Queue TODO:** 11.870
**Branch:** c4/c4-mgr-auto
**Status:** done

## Summary

Added `<ChartLineMacdCrossPct />` -- a pure-SVG, dual-panel
React/TS primitive that renders the close on top and a MACD
percent oscillator on the bottom. The MACD difference is scaled
to the close so the resulting series is comparable across
instruments at different price magnitudes:

```
ema1[i]    = EMA(close, fastLength)
ema2[i]    = EMA(close, slowLength)
macd[i]    = ema1[i] - ema2[i]
macdPct[i] = close[i] === 0 ? null
                            : macd[i] / close[i] * 100
```

Regime classifier: `above` (macdPct > 0), `below` (< 0),
`at` (= 0), `none` (null). Defaults: classic MACD periods
`fastLength = 12`, `slowLength = 26`.

## Bit-exact anchor

- **CONST close = K (K > 0)**: every EMA in the chain collapses
  to K via the SMA-seeded `min === max` precision fix.
  `macd = K - K = 0`, `macdPct = 0 / K * 100 = 0`. Regime `at`
  everywhere after warmup. Verified across multiple K and
  `(fastLength, slowLength)` tuples. K = 0 triggers the
  divide-by-zero guard -> macdPct = null.

## Soft anchors

- **LINEAR UP close = i + 1**: macd > 0 (fast EMA leads).
- **LINEAR DOWN close = N - i**: macd < 0 (fast EMA trails).

## Surface

- Helpers exported: `getLineMacdCrossPctFinitePoints`,
  `normalizeLineMacdCrossPctLength`, `applyLineMacdCrossPctEma`,
  `computeLineMacdCrossPct`, `classifyLineMacdCrossPctRegime`,
  `runLineMacdCrossPct`, `computeLineMacdCrossPctLayout`,
  `describeLineMacdCrossPctChart`.
- Component: `<ChartLineMacdCrossPct />` (forwardRef on a `<div>`
  wrapping the SVG + legend + badge).
- Data attrs: `data-section="chart-line-macd-cross-pct"`,
  `data-fast-length`, `data-slow-length`, `data-total-points`,
  `data-above-count`, `data-below-count`, `data-at-count`.
- Accessibility: `role="region"` + `aria-label` +
  `aria-describedby`, inner `<svg role="img">`, sr-only desc,
  motion-safe fade-in.
- Legend: two toggleable buttons (`close`, `macdPct`);
  controlled / uncontrolled modes; `onSeriesToggle` reports
  diffs.
- Tooltip: `x`, `close`, `macd`, `macdPct`, regime, plus running
  above / below / at / none counts.
- Renderer: top panel for close, bottom panel for macdPct with a
  dashed zero reference.

## Tests (54 cases)

- `getLineMacdCrossPctFinitePoints` (3) -- null, NaN, null
  entries.
- `normalizeLineMacdCrossPctLength` (3) -- default, below-2,
  accept.
- `applyLineMacdCrossPctEma` (1) -- CONST bit-exact across K.
- `computeLineMacdCrossPct` (7) -- null, CONST K > 0 macd=0 and
  macdPct=0, CONST K = 0 macdPct=null, LINEAR UP macd > 0, LINEAR
  DOWN macd < 0, length parity, no mutation.
- `classifyLineMacdCrossPctRegime` (4) -- above, below, at, none.
- `runLineMacdCrossPct` (8) -- ok flags, defaults, options, sort
  by x, CONST K > 0 regime at, CONST K = 0 regime none.
- `computeLineMacdCrossPctLayout` (5) -- empty, ok=true, panel
  stack, macdPct path, zero in osc axis.
- `describeLineMacdCrossPctChart` (3) -- empty, mention, params.
- `<ChartLineMacdCrossPct />` (20) -- empty placeholder, region
  role, ref forward, data attrs, regime counts, aria description,
  two legend items, legend toggle, controlled hide, badge, zero /
  axis / grid / legend on/off, className+style, animate off,
  macdPct path, price path, defaultHiddenSeries.
- MACD Pct integration (1) -- CONST K > 0 macd=macdPct=0
  bit-exact across multiple K and (fastLength, slowLength)
  tuples.

## Files

- web/src/components/ui/chart-line-macd-cross-pct.tsx (new)
- web/src/components/ui/chart-line-macd-cross-pct.test.tsx (new)
- web/src/components/ui/index.ts (barrel export added)
- package.json + package-lock.json + web/package.json +
  web/package-lock.json bumped to 1.11.888
- CHANGELOG.md prepended

## Verification

- `npx vitest run
  src/components/ui/chart-line-macd-cross-pct.test.tsx` -- 54/54
  pass.
- Typecheck: no new errors introduced (pre-existing errors in
  unrelated components remain).
