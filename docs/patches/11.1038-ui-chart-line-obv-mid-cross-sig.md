# 11.1038 -- UI: chart-line-obv-mid-cross-sig primitive (v1.11.1056)

## Scope

Add a pure-SVG dual-panel `ChartLineObvMidCrossSig`
component that renders the close in the top panel and the
On Balance Volume (OBV) centerline plus its smoothed SMA
signal line in the bottom panel, marking bullish (OBV
crosses up through signal -- accumulation distribution
trend trigger up) / bearish (OBV crosses down through
signal -- accumulation distribution trend trigger down)
OBV-over-signal crossover trigger events with bias
coloring derived from the OBV slope at the trigger bar.

This is the cumulative-volume sibling of the close-only
MA cross-sig primitives -- OBV is the canonical Joseph
Granville cumulative-volume momentum indicator. Unlike
SMA/EMA/HMA centerlines, OBV is unbounded (a running sum)
and incorporates volume directly: every bar's volume
either adds to (close up), subtracts from (close down),
or leaves unchanged (close flat) the cumulative running
total.

## Files

- `web/src/components/ui/chart-line-obv-mid-cross-sig.tsx`
  -- new primitive (close + volume input, OBV cumulative
  computation, SMA signal helper with CONST short-circuit,
  regime / bias classifiers, cross detector, layout,
  accessible SVG component).
- `web/src/components/ui/chart-line-obv-mid-cross-sig.test.tsx`
  -- 48 vitest cases.
- `web/src/components/ui/index.ts` -- barrel export appended.
- `package.json` 1.11.1055 -> 1.11.1056.
- `web/package.json` 1.11.1055 -> 1.11.1056.
- `package-lock.json` root version bumped.
- `web/package-lock.json` web version bumped.

## OBV centerline-over-signal pipeline

```
sign(d)     = +1 when d > 0
              -1 when d < 0
               0 when d == 0
obv[0]      = 0
obv[i]      = obv[i-1] + sign(close[i] - close[i-1])
                       * volume[i]
signal[i]   = SMA(obv, signalLength)

bullish     : prev obv <= prev signal AND
              cur obv > cur signal
              (accumulation trend trigger up --
               cumulative buying pressure breaking above
               its baseline)
bearish     : prev obv >= prev signal AND
              cur obv < cur signal
              (distribution trend trigger down --
               cumulative selling pressure breaking
               below its baseline)
regime      : 'bullish' when obv >= signal
              'bearish' when obv <  signal
              'none'    when either is null
bias        : obv[i] vs obv[i-1] -> up / down / flat /
              none
```

Default `signalLength = 14`. The SMA signal smooths the
OBV series so the cross-sig event isolates persistent
accumulation / distribution trend triggers from
single-bar volume spikes.

Warmup is `signalLength - 1 = 13` for the default tuning:
OBV seeds at i = 0, then the signal SMA needs
`signalLength` OBV values to fill its window.

## Bit-exact anchors

All anchors use close + volume input with constant
volume `V = 1`.

- **CONST close = K, volume = 1**: every bar has flat
  close so `sign(close[i] - close[i-1]) = 0` -> OBV
  stays at 0 for every i. signal = SMA(0, 14) = 0. OBV
  === signal -> regime `bullish` (>=) for every valid
  bar. 0 crosses. Verified across K in {0, 1, 50, 200,
  1234}.
- **LINEAR UP close = i, volume = 1**: every bar has
  close[i] > close[i-1] so OBV[i] = OBV[i-1] + 1.
  OBV[0] = 0 -> OBV[i] = i. signal = SMA(OBV, 14) =
  `((i - 13) + ... + i) / 14 = (14i - 91) / 14 = i -
  6.5`. OBV - signal = `+6.5` -> regime `bullish`. 0
  crosses.
- **LINEAR DOWN close = -i, volume = 1**: every bar
  has close[i] < close[i-1] so OBV[i] = OBV[i-1] - 1.
  OBV[i] = -i. signal = -i + 6.5. OBV - signal = `-6.5`
  -> regime `bearish`. 0 crosses.
- **VARYING VOLUME synthetic**: a 4-bar sequence with
  volumes [0, 10, 5, 3] and closes [0, 1, 2, 1] yields
  OBV [0, 10, 15, 12], verifying that per-bar volume is
  accumulated with the correct sign.

The `+6.5 / -6.5` separation on LINEAR input is a
consequence of the SMA centroid lag: OBV is itself
LINEAR (cumulative sum of constant +/-1), so SMA(OBV,
period) reduces to the same `i - (period - 1) / 2`
identity as the SMA centerline -- but with the OBV
sitting `(period - 1) / 2` units above (LINEAR UP) or
below (LINEAR DOWN) its own SMA.

## Test coverage

48 vitest cases:

1. defaults: dimensions, signalLength
2. `getLineObvMidCrossSigFinitePoints`: filters NaN /
   Infinity / negative volume
3. `normalizeLineObvMidCrossSigLength`: floor + >= 1
   bound + fallback
4. `applyLineObvMidCrossSigSma`: CONST K, LINEAR `i -
   6.5`
5. `computeLineObvMidCrossSig`: CONST OBV = 0, LINEAR
   UP OBV = i + signal = i - 6.5, LINEAR DOWN OBV = -i
   + signal = -i + 6.5, varying volume synthetic, empty
6. classifiers: regime + bias
7. `detectLineObvMidCrossSigCrosses`: bullish, bearish,
   null, no double-fire
8. `runLineObvMidCrossSig`: CONST x5, LINEAR UP with
   `+6.5` invariant, LINEAR DOWN with `-6.5` invariant,
   sort, ok=false short, empty, custom signalLength,
   regime sum
9. `computeLineObvMidCrossSigLayout`: LINEAR UP paths,
   empty, custom dims, degenerate range padding
10. `describeLineObvMidCrossSigChart`: empty, count +
    signalLength + accumulation distribution mention
11. component render: region, empty fallback, OBV +
    signal paths, legend off, badge, aria, bullish count,
    hidden series

Result: 48/48 pass.

## Telemetry surface

`data-section="chart-line-obv-mid-cross-sig"` exposes:

- `data-signal-length`, `data-total-points`
- `data-bullish-count`, `data-bearish-count`
- `data-bullish-cross-count`, `data-bearish-cross-count`
- `data-up-bias-count`, `data-down-bias-count`,
  `data-flat-bias-count`
- `data-cross-count`

## Accessibility

- `role="region"`, descriptive `aria-label`, narrative
  `describeLineObvMidCrossSigChart` description that
  spells out the accumulation distribution semantics.
- Trigger markers are individually focusable
  (`tabIndex={0}`) with kind+bias `aria-label`.
- Hover targets respond to mouse and keyboard focus.
- Legend buttons are keyboard-toggleable via Enter /
  Space.

## Risk

- Pure-SVG, deterministic `.toFixed(2)`, no canvas, no
  chart lib dependency.
- Strict-inequality bullish vs `>=` regime classifier
  means the bullish boundary (OBV === signal) settles
  into the bullish band, matching every other
  chart-line-*-cross-sig sibling.
- OBV is unbounded (a running cumulative sum), so unlike
  the close-bounded MA cross-sig primitives the bottom-
  panel y-range can drift far from the price-panel
  range. Layout handles this by computing OBV's own
  min/max independently from the close range.
- Negative volume is filtered at the input boundary --
  the typical accounting convention treats volume as
  non-negative.
- OBV uses sign(close[i] - close[i-1]) rather than the
  raw price delta -- a single tick up adds the full
  volume regardless of how much price moved. This
  matches Granville's original 1963 formulation and
  modern stockcharts.com / TradingView conventions.
- Uses a distinct OBV ink colour (`#0d9488`, teal)
  matching the chart-line-vwap-divergence-cross sibling
  -- both are volume-aware indicators, so sharing the
  family ink colour makes them visually grouped when
  the dashboard renders multiple volume primitives.
