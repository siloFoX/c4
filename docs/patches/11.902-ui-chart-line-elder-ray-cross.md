# Patch 11.902 -- UI: chart-line-elder-ray-cross primitive

## Summary

Added `<ChartLineElderRayCross />` -- pure-SVG dual-panel React
/TS primitive that renders the close (top panel) with bullish /
bearish arrow overlays at every Bull Power vs Bear Power cross
trigger, and the Elder Ray Bull Power / Bear Power lines (bottom
panel) with cross dot markers. Crossover variant of the Elder
Ray family that flags buyer / seller dominance regime shift
events distinct from the base Elder Ray line readings.

## Algorithm

```
highProxy[i] = max(close[i-h+1..i])
lowProxy[i]  = min(close[i-h+1..i])
ema[i]       = EMA(close, length)
bull[i]      = highProxy - ema
bear[i]      = lowProxy - ema
bullish     : (bull - bear) crosses up   (prev <= 0, cur > 0)
bearish     : (bull - bear) crosses down (prev >= 0, cur < 0)
```

Defaults `length = 13` (canonical Elder Ray EMA window),
`highProxyLength = 2` (close-only proxy that synthesizes the
high / low pair from a 2-bar window of closes). Regime classifier
`bullish` (bull > bear), `bearish` (bull < bear), `neutral`
(bull === bear), `none` (either null).

## Bit-exact anchor

CONST close = K: highProxy = lowProxy = K every bar; EMA(K) =
K via the `min === max` precision short-circuit; bull = bear =
0. diff = 0 -> regime `neutral`, cross count = 0. Verified
across K = 0, 1, 5, 17, 100, 1234.

## API surface

- `<ChartLineElderRayCross />` `forwardRef<HTMLDivElement>`
- `getLineElderRayCrossFinitePoints(data)`
- `normalizeLineElderRayCrossLength(value, fallback)`
- `applyLineElderRayCrossEma(values, length)`
- `computeLineElderRayCross(series, options)`
- `classifyLineElderRayCrossRegime(bull, bear)`
- `detectLineElderRayCrossCrosses(series, bull, bear)`
- `runLineElderRayCross(data, options)`
- `computeLineElderRayCrossLayout(opts)`
- `describeLineElderRayCrossChart(data, options)`

## Visual

- Two stacked panels, ARPS design system colors via tokens.
- Top: close line + bullish/bearish polygon arrows at cross
  bars.
- Bottom: Bull Power (green) + Bear Power (red) lines + cross
  dot markers + dashed zero line.
- Tooltip lists x / close / bull / bear / regime / counts /
  crosses.
- Config badge + togglable legend (close / Bull Power / Bear
  Power).
- Cross marker is `role="graphics-symbol"` with `tabIndex={0}`.

## Test summary

`web/src/components/ui/chart-line-elder-ray-cross.test.tsx` --
54 vitest cases, all pass.

Coverage includes:

- finite-point sanitization
- length normalization
- EMA CONST short-circuit
- compute / classify / detect helpers
- bit-exact CONST K (bull=bear=0) anchor
- LINEAR UP sanity (bull > bear)
- runLineElderRayCross defaults, custom args, x-sort
- layout panel stacking + zero line placement
- describe text contents
- component empty / region / ref forwarding
- data-attributes (lengths / counts)
- legend uncontrolled + controlled
- show* toggles (axis / grid / legend / crosses / overlay)
- animate=false class strip
- zero line render
- CONST integration anchor (K in {0, 1, 5, 17, 100, 1234})

## Touched

- `web/src/components/ui/chart-line-elder-ray-cross.tsx` (new)
- `web/src/components/ui/chart-line-elder-ray-cross.test.tsx`
  (new)
- `web/src/components/ui/index.ts` (barrel)
- `package.json`, `package-lock.json`, `web/package.json`,
  `web/package-lock.json` (1.11.919 -> 1.11.920)
- `docs/patches/11.902-ui-chart-line-elder-ray-cross.md` (new)
- `CHANGELOG.md` (prepend)
- `/root/c4/docs/autonomous-queue-v10.md` row 11.902 -> done
