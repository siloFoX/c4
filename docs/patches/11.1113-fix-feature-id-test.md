# 11.1113 -- FIX: update 5 stale test expectations in use-selected-feature-id.test.ts (v1.11.1131)

## Scope

`web/src/lib/use-selected-feature-id.test.ts` had 5 FAILING tests
because they asserted the OLD hero feature id (`scribe`). The
registry intentionally moved `workers-hero` to FEATURES[0] in TODO
11.314 (with scribe shifted down). The test was stale, not the
registry. Fix scope is ONLY the test file; no registry edits; no
non-test source touched.

## Edits

Imported the canonical `FEATURES` from the registry and switched every
hardcoded `'scribe'` assertion to `FEATURES[0]!.id` so the test stays
correct if the hero feature ever changes again.

| Line | Before | After |
|---|---|---|
| 27/L33 (test name + body) | `it('falls back to FEATURES[0].id (scribe) ...')` + `expect(...).toBe('scribe')` | `it('falls back to FEATURES[0].id ...')` + `expect(...).toBe(FEATURES[0]!.id)` |
| L62/L68 | `expect(...).toBe('scribe')` | `expect(...).toBe(FEATURES[0]!.id)` |
| L87/L93 | `expect(...).toBe('scribe')` (hashchange-valid pre-state) | `expect(...).toBe(FEATURES[0]!.id)` |
| L101/L107 | `expect(...).toBe('scribe')` (hashchange-unknown stays-put) | `expect(...).toBe(FEATURES[0]!.id)` |
| L110/L116 | `expect(...).toBe('scribe')` (non-`#/feature/` prefix ignored) | `expect(...).toBe(FEATURES[0]!.id)` |

Also updated the file's docstring comment to drop the `(currently 'scribe')`
parenthetical and noted in a small `// (TODO 11.1113)` block that the
assertions now reference `FEATURES[0]!.id` dynamically.

Added one import line:

```ts
import { FEATURES } from '../pages/registry';
```

`FEATURES[0]!.id` uses a non-null assertion because the registry's
indexed access is `FeatureDef | undefined` under
`noUncheckedIndexedAccess`. `FEATURES` is a non-empty literal so the
assertion never throws at runtime; this is a typed assertion, not
`as any`.

## Verification (mandated)

- `vitest run src/lib/use-selected-feature-id.test.ts`:
  **1/1 file, 14/14 tests PASS** (previously 5 failing).
- `tsc --noEmit`: project-wide total **unchanged at 928** -- no new
  type errors.

## Risk

- Pure test-only change.
- Future registry reorderings keep the test green automatically since
  the assertion now reads through the canonical export.
