# Patch 11.919 -- UI: chart-line-volume-osc-cross-sig primitive

## Summary

Added `<ChartLineVolumeOscCrossSig />` -- pure-SVG dual-panel
React/TS primitive that renders the close (top panel) with
bullish / bearish arrow overlays at every Volume Oscillator vs
EMA-smoothed signal cross trigger, and the close-only Volume
Oscillator + signal line in the bottom panel with cross dot
markers and a dashed zero baseline. Signal-cross variant of
the Volume Oscillator family that flags volume momentum
trigger events distinct from the zero-line crossings published
by 11.918 `chart-line-volume-osc-cross`.

## Algorithm

```
shortMa[i]   = EMA(close, shortLength)             (close-only volume proxy)
longMa[i]    = EMA(close, longLength)
volOsc[i]    = longMa > 0
                ? ((shortMa - longMa) / longMa) * 100
                : 0
signal[i]    = EMA(volOsc, signalLength)
bullish     : (volOsc - signal) crosses up   (prev <= 0, cur > 0)
bearish     : (volOsc - signal) crosses down (prev >= 0, cur < 0)
```

Defaults `shortLength = 5`, `longLength = 10` (canonical
Volume Oscillator parameters), `signalLength = 4`. Regime
classifier `bullish` (volOsc > signal), `bearish` (volOsc <
signal), `neutral` (volOsc === signal), `none` (either null).

## Bit-exact anchor

CONST close = K: short - long = 0 every bar -> volOsc = 0 (or
0 via divide-by-zero guard for K = 0). signal EMA of 0s = 0
via the `min === max` precision short-circuit. volOsc ===
signal -> regime `neutral`, cross count = 0. Verified across
K = 0, 1, 5, 17, 100, 1234.

LINEAR UP yields positive volOsc (short EMA outruns long EMA
on rising data) and positive signal (EMA of positive volOsc).

## API surface

- `<ChartLineVolumeOscCrossSig />` `forwardRef<HTMLDivElement>`
- `getLineVolumeOscCrossSigFinitePoints(data)`
- `normalizeLineVolumeOscCrossSigLength(value, fallback)`
- `applyLineVolumeOscCrossSigEma(values, length)`
- `computeLineVolumeOscCrossSig(series, options)`
- `classifyLineVolumeOscCrossSigRegime(volOsc, signal)`
- `detectLineVolumeOscCrossSigCrosses(series, vo, sig)`
- `runLineVolumeOscCrossSig(data, options)`
- `computeLineVolumeOscCrossSigLayout(opts)`
- `describeLineVolumeOscCrossSigChart(data, options)`

## Visual

- Two stacked panels, ARPS design system colors via tokens.
- Top: close line + bullish/bearish polygon arrows at cross
  bars.
- Bottom: Volume Oscillator + EMA signal lines + cross dot
  markers + dashed zero baseline.
- Tooltip lists x / close / volOsc% / signal / regime / counts
  / crosses.
- Config badge + togglable legend (close / volOsc / signal).
- Cross marker is `role="graphics-symbol"` with `tabIndex={0}`.

## Test summary

`web/src/components/ui/chart-line-volume-osc-cross-sig.test.tsx`
-- 54 vitest cases, all pass.

Coverage includes:

- finite-point sanitization
- length normalization
- EMA CONST short-circuit
- compute / classify / detect helpers
- bit-exact CONST K (volOsc=signal=0) anchor
- LINEAR UP positive directional anchor
- runLineVolumeOscCrossSig defaults, custom lengths, x-sort
- layout panel stacking + zero line placement
- describe text contents
- component empty / region / ref forwarding
- data-attributes (lengths / counts)
- legend uncontrolled + controlled
- show* toggles (axis / grid / legend / crosses / overlay)
- animate=false class strip
- zero line render
- CONST integration anchor (K in {0, 1, 5, 17, 100, 1234})

## Touched

- `web/src/components/ui/chart-line-volume-osc-cross-sig.tsx`
  (new)
- `web/src/components/ui/chart-line-volume-osc-cross-sig.test.tsx`
  (new)
- `web/src/components/ui/index.ts` (barrel)
- `package.json`, `package-lock.json`, `web/package.json`,
  `web/package-lock.json` (1.11.936 -> 1.11.937)
- `docs/patches/11.919-ui-chart-line-volume-osc-cross-sig.md`
  (new)
- `CHANGELOG.md` (prepend)
- `/root/c4/docs/autonomous-queue-v10.md` row 11.919 -> done
