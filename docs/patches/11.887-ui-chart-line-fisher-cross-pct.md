# Patch 11.887 -- UI: chart-line-fisher-cross-pct primitive

**Version:** v1.11.905
**Queue TODO:** 11.887
**Branch:** c4/c4-mgr-auto
**Status:** done

## Summary

Added `<ChartLineFisherCrossPct />` -- a pure-SVG, dual-panel
React/TS primitive that renders the close in the top panel and
overlays the Fisher Transform with its EMA-smoothed signal, plus
a `fisherPct = (fisher - signal)` deviation channel in the
bottom panel. The cross-pct treatment surfaces the *magnitude
of gaussian-normalised momentum events* (how stretched the
Fisher Transform is above or below its signal) separate from
the sign-based crossings.

Fisher Transform (single-series, close-only) with the standard
0.66 / 0.67 / 0.5 mixing coefficients:

```
highest[i] = max(close[i - length + 1 .. i])
lowest[i]  = min(close[i - length + 1 .. i])
norm[i]    = highest === lowest
               ? 0.5
               : (close[i] - lowest) / (highest - lowest)
xRaw[i]    = 0.66 * 2 * (norm[i] - 0.5) + 0.67 * x[i-1]
x[i]       = clamp(xRaw[i], -0.999, 0.999)
fisher[i]  = 0.5 * ln((1 + x[i]) / (1 - x[i])) +
             0.5 * fisher[i-1]
signal[i]  = EMA(fisher, signalLength)
fisherPct[i] = fisher[i] - signal[i]
```

Defaults: `length = 10` (canonical Fisher window),
`signalLength = 9`. Regime classifier: `above` (fisherPct > 0),
`below` (< 0), `at` (= 0), `none` (null).

## Bit-exact anchor

- **CONST close = K**: every highest === lowest = K so `norm =
  0.5` (the midpoint fallback) -> `2 * (norm - 0.5) = 0` ->
  `x[i] = 0.67 * x[i-1] = 0` (seeded at 0) -> `fisher[i] = 0.5
  * ln(1) + 0.5 * fisher[i-1] = 0`. signal EMA of 0s = 0.
  fisherPct = 0 every bar after warmup. Verified across K =
  0..1234.

## Surface

- Helpers exported: `getLineFisherCrossPctFinitePoints`,
  `normalizeLineFisherCrossPctLength`,
  `applyLineFisherCrossPctEma`, `computeLineFisherCrossPct`,
  `classifyLineFisherCrossPctRegime`,
  `runLineFisherCrossPct`,
  `computeLineFisherCrossPctLayout`,
  `describeLineFisherCrossPctChart`.
- Component: `<ChartLineFisherCrossPct />` (forwardRef on a
  `<div>` wrapping the SVG + legend + badge).
- Data attrs: `data-section="chart-line-fisher-cross-pct"`,
  `data-length`, `data-signal-length`, `data-total-points`,
  `data-above-count`, `data-below-count`, `data-at-count`.
- Accessibility: `role="region"` + `aria-label` +
  `aria-describedby`, inner `<svg role="img">`, sr-only desc,
  motion-safe fade-in.
- Legend: four toggleable buttons (`close`, `fisher`, `signal`,
  `fisherPct`); controlled / uncontrolled modes;
  `onSeriesToggle` reports diffs.
- Tooltip: `x`, `close`, `fisher`, `signal`, `fisherPct`,
  regime, plus running regime counts.
- Renderer: top panel renders the close, bottom panel overlays
  Fisher, signal, and fisherPct over a dashed zero reference.

## Tests (48 cases)

- `getLineFisherCrossPctFinitePoints` (3) -- null, NaN, null
  entries.
- `normalizeLineFisherCrossPctLength` (3) -- default, below-2,
  accept.
- `applyLineFisherCrossPctEma` (1) -- CONST bit-exact across
  K.
- `computeLineFisherCrossPct` (4) -- null, CONST -> 0/0/0,
  defaults, no mutation.
- `classifyLineFisherCrossPctRegime` (4) -- above, below, at,
  none.
- `runLineFisherCrossPct` (6) -- ok flags, defaults, options,
  sort by x, CONST regime at.
- `computeLineFisherCrossPctLayout` (5) -- empty, ok=true,
  panel stack, zero in osc axis, price + fisher paths.
- `describeLineFisherCrossPctChart` (3) -- empty, mention,
  both lengths.
- `<ChartLineFisherCrossPct />` (18) -- empty placeholder,
  region role, ref forward, data attrs, aria desc, four legend
  items, legend toggle, controlled hide, badge, zero / axis /
  grid / legend on/off, className+style, animate off,
  fisher+signal+pct paths, price path, defaultHiddenSeries.
- Fisher Cross Pct integration (1) -- CONST K bit-exact fisher
  = 0, signal = 0, fisherPct = 0 across K = 0..1234.

## Relationship to chart-line-fisher-cross (existing)

`chart-line-fisher-cross` (11.864) marks the discrete bullish /
bearish crossover events at the moment Fisher crosses its
signal. `chart-line-fisher-cross-pct` (11.887) renders the
continuous *magnitude* of the same deviation
(`fisher - signal`) as a third oscillator channel, so the
gaussian-normalised momentum events can be inspected by their
strength rather than only their sign-change timing.

## Files

- web/src/components/ui/chart-line-fisher-cross-pct.tsx (new)
- web/src/components/ui/chart-line-fisher-cross-pct.test.tsx
  (new)
- web/src/components/ui/index.ts (barrel export added)
- package.json + package-lock.json + web/package.json +
  web/package-lock.json bumped to 1.11.905
- CHANGELOG.md prepended

## Verification

- `npx vitest run
  src/components/ui/chart-line-fisher-cross-pct.test.tsx` --
  48/48 pass.
- Typecheck: no new errors introduced.
