# 11.1069 -- UI: chart-line-psar-flip-cross primitive (v1.11.1087)

## Scope

Add a pure-SVG dual-panel `ChartLinePsarFlipCross`
component that renders the close in the top panel and the
Wilder Parabolic SAR (Stop And Reverse) in the bottom
panel, marking bullish (SAR flips from above price to
below; trailing stop reversal from downtrend to uptrend)
/ bearish (SAR flips from below price to above; trailing
stop reversal from uptrend to downtrend) direction-flip
crossover trigger events with bias coloring derived from
the SAR value jump at the flip bar.

This primitive is the **stop-and-reverse core** of the
Parabolic SAR system: every flip event is the primary
trading trigger in Wilder's 1978 design, where the SAR is
intended to be used as a literal stop-loss line that flips
sides when price pierces it.

## Files

- `web/src/components/ui/chart-line-psar-flip-cross.tsx`
  -- new primitive (single-value input, Wilder SAR
  iteration with af acceleration + max-step cap +
  reversal handling, trend-flip detector, accessible SVG
  component).
- `web/src/components/ui/chart-line-psar-flip-cross.test.tsx`
  -- 57 vitest cases.
- `web/src/components/ui/index.ts` -- barrel export
  appended.
- `package.json` 1.11.1086 -> 1.11.1087.
- `web/package.json` 1.11.1086 -> 1.11.1087.
- `package-lock.json` root version bumped.
- `web/package-lock.json` web version bumped.

## PSAR flip-cross pipeline

```
nextSar  = sar + af * (ep - sar)              (Wilder)
clamp    = min(nextSar, values[i-1], values[i-2])
           in uptrend (cannot cross into recent lows)
         = max(nextSar, values[i-1], values[i-2])
           in downtrend (cannot cross into recent highs)
flip     = price pierces nextSar:
           - uptrend + v < nextSar -> 'down' flip
             (bearish: SAR now above price)
           - downtrend + v > nextSar -> 'up' flip
             (bullish: SAR now below price)
           SAR jumps to prior ep, ep := v, af := step.

bullish (PSAR flip up) :
  prev trend === 'down' && cur trend === 'up'
bearish (PSAR flip down) :
  prev trend === 'up' && cur trend === 'down'

regime   : 'bullish' when trend === 'up'
           'bearish' when trend === 'down'
           'none'    when trend === null
bias     : sar[i] vs sar[i-1] -> up/down/flat/none.
           At a flip, SAR jumps from one side of price to
           the other, so bias is typically large in
           magnitude.
```

Default `step = 0.02`, `maxStep = 0.2` -- canonical Wilder
SAR tuning.

## Sibling family (SAR signal family)

- **chart-line-parabolic-sar** -- raw SAR with reversal
  markers (the source of the SAR computation reused
  here)
- **chart-line-sar-cross-sig v1.11.1086** -- SAR vs
  SMA(SAR) trailing stop confirmation
- **chart-line-supertrend-flip-cross v1.11.1078** --
  analogous trend-flip detector for the Supertrend
  trailing stop (conceptual sibling, different stop
  line)
- **chart-line-psar-flip-cross v1.11.1087** (this
  primitive) -- SAR direction flip (the primary SAR
  trigger event)

Distinct from sibling sar-cross-sig v1.11.1086:
- **cross-sig** compares SAR against its own SMA
  (CONFIRMATION: SAR trajectory sustained enough to pull
  above/below its smoothed mean)
- **flip-cross** (this primitive) detects the SAR's own
  STOP-AND-REVERSE events (PRIMARY TRIGGER: SAR literally
  flips sides; the original SAR signal)

Warmup is i = 1 (SAR seeds at i = 0 from values[0] with
initial trend from the first move). The first potential
flip lands at i = 1 if the second value pierces the seed
SAR; in practice flips happen later after af has grown.

## Bit-exact anchors

All anchors use single-value input.

- **CONST** `value = K`: First move = 0, initial trend
  'up' (>=). SAR = K. Every subsequent bar v = K ===
  nextSar (K + step*(K-K) = K). The piercing check is
  v < nextSar (strict), so v === nextSar does NOT fire.
  trend stays 'up' forever. 0 flips. 0 reversals. regime
  `bullish` (40/40 bars). Verified across K in {0, 1,
  50, 200, 1234}.
- **LINEAR UP** `value = i`: First move +1, trend 'up'.
  SAR forms slow-moving lower envelope clamped by
  min(values[i-1], values[i-2]). Price never falls below
  SAR (price is monotonically increasing). 0 flips. 0
  reversals. regime `bullish` (40/40 bars).
- **LINEAR DOWN** `value = -i`: First move -1, trend
  'down'. SAR forms slow-moving upper envelope clamped
  by max(values[i-1], values[i-2]). Price never rises
  above SAR. 0 flips. 0 reversals. regime `bearish`
  (40/40 bars).
- **PEAK** linear up then linear down: The SAR algorithm
  detects the peak and flips trend from 'up' to 'down'
  (bearish flip). At least 1 cross is produced -- this
  is the canonical SAR trigger event.

All three steady-state anchors produce 0 flips because
price never pierces SAR. The PEAK anchor exercises the
flip detector and verifies it fires on actual trend
reversals.

## Test coverage

57 vitest cases:

1. defaults: dimensions, step, maxStep
2. `getLinePsarFlipCrossFinitePoints`: filters NaN /
   Infinity
3. `normalizeLinePsarFlipCrossStep`: positive bound +
   fallback
4. `computeLinePsarFlipCross`: CONST -> SAR = K constant
   + trend up throughout, LINEAR UP / DOWN trend stays,
   single-point degenerate, empty
5. `computeLinePsarFlipCross` PEAK -> at least one
   reversal
6. classifiers: regime (3-state with up/down/null) +
   bias (4-state)
7. `detectLinePsarFlipCrossCrosses`: bullish on down ->
   up, bearish on up -> down, no-flip same-trend,
   null-prev skip, bias from SAR jump direction
8. `runLinePsarFlipCross`: CONST band x5 (regime
   bullish, 0 flips), LINEAR UP (0 flips, all bullish),
   LINEAR DOWN (0 flips, all bearish), PEAK (>= 1 flip
   with bearish kind), sort, ok=false short, empty,
   custom step, regime sum, cross-count match
9. `computeLinePsarFlipCrossLayout`: LINEAR UP paths,
   LINEAR UP 0 markers, PEAK > 0 markers, empty
   fallback, custom dims, degenerate priceRange + osc
   range padding
10. `describeLinePsarFlipCrossChart`: empty, count +
    step + trailing stop reversal flip mention
11. component render: region, empty fallback, SAR path,
    legend off, badge, aria, counters, hidden series,
    two legend buttons, bearish counts, non-zero cross
    count for PEAK

Result: 57/57 pass.

## Telemetry surface

`data-section="chart-line-psar-flip-cross"` exposes:

- `data-step`, `data-max-step`, `data-total-points`
- `data-bullish-count`, `data-bearish-count`
- `data-bullish-cross-count`, `data-bearish-cross-count`
- `data-up-bias-count`, `data-down-bias-count`,
  `data-flat-bias-count`
- `data-cross-count`, `data-reversal-count`

Plus a debug-audit surface on the `run` object:
`sarValues`, `trendValues`, `reversedValues`, `samples`
(with `trend` and `reversed` flags) for bit-exact
verification.

## Accessibility

- `role="region"`, descriptive `aria-label`, narrative
  `describeLinePsarFlipCrossChart` description that
  spells out trailing stop reversal flip semantics and
  mentions Wilder's formulation.
- Trigger markers are individually focusable
  (`tabIndex={0}`) with kind+bias `aria-label`.
- Hover targets respond to mouse and keyboard focus.
- Legend buttons (value / SAR) are keyboard-toggleable
  via Enter / Space.
- Oscillator panel auto-scales to the SAR range with
  degenerate padding so flat lines (CONST input) render
  with stable tick spacing.

## Risk

- Pure-SVG, deterministic `.toFixed(2)`, no canvas, no
  chart lib dependency.
- 3-state regime classifier follows the flip-cross
  family convention (up = bullish, down = bearish, null
  = none).
- SAR algorithm is verbatim Wilder 1978 -- same code
  path as chart-line-parabolic-sar and
  chart-line-sar-cross-sig v1.11.1086 so reversal counts
  match across the family.
- The PSAR flip is the literal stop-and-reverse trigger
  from Wilder's original 1978 design. Crosses fire
  exactly when the SAR's own `reversed` flag triggers
  -- the cross-count and reversal-count should always
  match (verified by the PEAK test which checks both >
  0).
- The piercing check is `v < nextSar` (strict) in
  uptrend / `v > nextSar` (strict) in downtrend, so
  exactly equal values do NOT trigger flips. This
  matches Wilder's original specification and makes
  CONST input numerically bit-exact (no flip on K === K).
- On steady LINEAR input price never pierces SAR
  (clamping by recent low/high keeps SAR safely on the
  trailing side). 0 flips fire. Real flips fire when
  the price direction actually reverses, which is the
  SAR's primary purpose.
- Completes the SAR signal family triplet: raw SAR
  (parabolic-sar), confirmation (sar-cross-sig), and
  flip-trigger (this primitive). All three reuse the
  same Wilder iteration so reversal events line up
  across the family.
