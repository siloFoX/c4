# 11.378 -- UI: kbd primitive

- TODO id: 11.378
- Version: 1.11.396
- Date: 2026-05-18
- Scope:
  - `web/src/components/ui/kbd.tsx`
    (size scale)
  - `web/src/components/ui/kbd.test.tsx`
    (+6 cases)

## Goal

Dispatch:

- Build `web/src/components/ui/kbd.tsx` --
  keyboard shortcut display with Cmd / Ctrl
  / Shift / Alt symbols, platform-aware
  (Mac vs Win/Linux), size variants.

`<Kbd>` (11.250 / v1.11.268) already covered
most of the contract:

| dispatch item | shipped at |
| --- | --- |
| Cmd / Ctrl / Shift / Alt symbols | `MAC_GLYPH` (⌘ / ⌃ / ⇧ / ⌥) |
| platform-aware | `detectPlatform()` + `Mod` alias |
| combo parsing | `combo="Cmd+K"` -> per-token glyphs |
| keys array | `keys={['Ctrl', 'C']}` |
| literal child | `<Kbd>Esc</Kbd>` |
| custom separator | `separator` prop |
| special keys (Enter/Esc/Tab/Space/arrows) | mac glyph map |

The single remaining gap was **size
variants**. This patch adds them.

## Size scale

| size | padding | text | use case |
| --- | --- | --- | --- |
| `sm` | `px-1` | `text-[10px]` | dense menu rows, command palette suggestions |
| `md` (default) | `px-1.5` | `text-xs` | legacy byte-identical |
| `lg` | `px-2 py-0.5` | `text-sm` | hero shortcut-overlay strip |

Default `md` keeps the legacy 11.250 layout
byte-for-byte; every existing call site stays
identical.

## API

```tsx
import { Kbd } from './components/ui/kbd';

// Legacy (byte-identical)
<Kbd>Esc</Kbd>
<Kbd combo="Cmd+K" />

// Dense (menu rows)
<Kbd combo="Mod+P" size="sm" />

// Hero (overlay)
<Kbd combo="Cmd+Shift+P" size="lg" />
```

The size class applies to:

- the lone `<kbd>` in the literal-children
  call shape;
- every per-token `<kbd>` inside the combo
  / keys-array call shape (the wrapping
  `<span>` also carries `data-size` for
  outer-selector branching).

## Data attributes

`data-size` mirrors the prop on both the
outer wrapper (combo / keys-array calls)
and every `<kbd>` token. Combined with the
existing `data-platform` mac/other flag,
downstream tests can branch on size and
platform without parsing class lists.

## Tests

`kbd.test.tsx` -- **+6 new cases (33 total)**:

- default `size="md"` applies legacy
  `px-1.5 text-xs` classes + data-size attr.
- `size="sm"` applies `px-1 text-[10px]` +
  data-size attr.
- `size="lg"` applies `px-2 py-0.5 text-sm`
  + data-size attr.
- size applies uniformly across every kbd
  token inside a combo.
- `data-size` mirrors on the outer wrapper
  for combos.
- size composes with caller `className`
  without losing the size class.

**33/33 pass** under vitest 4.1.5. `npx tsc
--noEmit` clean for touched files.

## Pairs with existing primitives

- `<CommandPalette>` -- command palette
  rows use `<Kbd>` to surface shortcuts;
  the new `size="sm"` is the right pick
  for that surface.
- `<Tooltip>` -- inline tooltip tooltips
  can host a `<Kbd>` for "press X to do Y"
  copy.
- `<DropdownMenu>` -- menu items with
  `shortcut?` field (11.362) auto-render
  a `<Kbd>` chip; that surface uses the
  default `md`.

## Out of scope

- Per-page adoption (`sm` / `lg` rollout).
  All three sizes are additive; every
  existing call site stays byte-identical
  at `md`.
- A 4th size (`xs`). The 10px text floor
  in `sm` already hits the readability
  threshold; below that goes to icon-only
  modifiers.
- Themed palette overrides. The legacy
  `bg-muted text-muted-foreground` palette
  stays.
- Per-token size override. Size is applied
  uniformly across a combo; mixed sizes
  inside one chip would muddle the
  shortcut rhythm.
- A "vertical stack" combo layout. The
  inline horizontal layout is the
  canonical shortcut rhythm; vertical
  stacks belong in a separate "shortcut
  legend" primitive.
