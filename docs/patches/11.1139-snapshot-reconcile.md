# 11.1139 - reconcile 3 stale render-snapshot test files to verified-shipped behavior

## Scope

3 snapshot test files that drifted from current rendered output due to
documented prior feature work. Per the dispatch's STRICT CONTRACT, every
diff was individually examined and traced back to its source commit; ALL
17 failing assertions across the 3 files are INTENDED behavioral changes
that shipped via earlier TODOs. No SUSPECT diffs were found, so no diff
was left red. No component source files touched.

## Per-diff classification

| File | Diff signature | Origin commit | Verdict |
|---|---|---|---|
| Sidebar.snapshot.test.tsx (expanded + collapsed, 2 tests) | `+ data-axis="y"` on `.c4-scroll` | `d4f54b09` feat ScrollArea maxHeightPreset + axis (TODO 11.382, v1.11.400) | INTENDED |
| Sidebar.snapshot.test.tsx (both) | `+ data-portal="false"` on tooltip root | `0bfa791a` feat tooltip Radix-style extensions (TODO 11.360, v1.11.378) | INTENDED |
| Sidebar.snapshot.test.tsx (both) | Tooltip body: `+ data-placement="..." + data-tooltip-body="true"`, class string `absolute` moved within `cn(...)` concat | `0bfa791a` (TODO 11.360) | INTENDED |
| Sidebar.snapshot.test.tsx (both) | Kbd wrapper + each Kbd: `+ data-size="md"`, kbd class string `font-mono` moved before `px-1.5 text-xs` | `4de442fa` feat Kbd size scale (TODO 11.378, v1.11.396) | INTENDED |
| button.snapshot.test.tsx (10 tests: 6 variants + 4 sizes) | `+ data-section="button" + data-loading="false" + data-variant + data-size` on `<button>`, children wrapped in `<span data-section="button-children" class="inline-flex items-center gap-2">` | `bb97c0b1` feat Button primitive enhancements (TODO 11.308) | INTENDED |
| tooltip.snapshot.test.tsx (5 tests: hidden + 4 placements) | `+ data-portal="false"` on root, body span: `+ aria-label="..."`, `+ data-placement="..."`, `+ data-tooltip-body="true"`, class string `absolute` moved within `cn(...)` concat | `0bfa791a` (TODO 11.360) | INTENDED |

All 17 diffs INTENDED. 0 SUSPECT.

## Verification

- `tsc --noEmit -p web/tsconfig.json`: 0 errors (PROJECT-WIDE GREEN
  preserved from 11.1138).
- `npm run build`: green (built in ~11s).
- `npx vitest run` on the 3 snapshot test files: **3 files pass,
  19 tests pass.** All snapshots now match current render.
- `git status --porcelain` after this patch: only 3 test artifacts:
  - `web/src/components/ui/button.snapshot.test.tsx` (inline updated)
  - `web/src/components/ui/tooltip.snapshot.test.tsx` (inline updated)
  - `web/src/components/layout/__snapshots__/Sidebar.snapshot.test.tsx.snap`
    (external .snap, generated by vitest; Sidebar.snapshot.test.tsx itself
    unchanged)
  ZERO source files touched.
