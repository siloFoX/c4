# Patch 11.869 -- UI: chart-line-vroc-pct primitive

**Version:** v1.11.887
**Queue TODO:** 11.869
**Branch:** c4/c4-mgr-auto
**Status:** done

## Summary

Added `<ChartLineVrocPct />` -- a pure-SVG, dual-panel React/TS
primitive that renders the close on top and the Volume Rate of
Change percent oscillator on the bottom. Single-oscillator
counterpart of `chart-line-vroc-cross`; no signal line, just a
regime classifier:

- `above` (VROC > 0)
- `below` (VROC < 0)
- `at`    (= 0)
- `none`  (null)

Defaults: `length = 12`, vrocColor `#0ea5e9` (sky), zero line
`#475569`.

## Formula

```
VROC[i] = volume[i - length] === 0
            ? null
            : (volume[i] - volume[i - length]) / volume[i - length] * 100
```

## Bit-exact anchor

- **CONST volume = V (V > 0)**: every difference is 0 -> VROC =
  0 / V * 100 = 0 bit-exactly. Regime `at` for every bar after
  warmup. Verified across multiple V and length tuples. V = 0
  triggers the divide-by-zero guard -> VROC = null (regime
  `none`).

## Surface

- Helpers exported: `getLineVrocPctFinitePoints`,
  `normalizeLineVrocPctLength`, `computeLineVrocPct`,
  `classifyLineVrocPctRegime`, `runLineVrocPct`,
  `computeLineVrocPctLayout`, `describeLineVrocPctChart`.
- Component: `<ChartLineVrocPct />` (forwardRef on a `<div>`
  wrapping the SVG + legend + badge).
- Data attrs: `data-section="chart-line-vroc-pct"`,
  `data-length`, `data-total-points`, `data-above-count`,
  `data-below-count`, `data-at-count`.
- Accessibility: `role="region"` + `aria-label` +
  `aria-describedby`, inner `<svg role="img">`, sr-only desc,
  motion-safe fade-in.
- Legend: two toggleable buttons (`close`, `vroc`); controlled /
  uncontrolled modes; `onSeriesToggle` reports diffs.
- Tooltip: `x`, `close`, `volume`, `vroc`, regime, plus running
  above / below counts.
- Renderer: top panel for close, bottom panel for the VROC
  oscillator with a dashed zero reference.

## Tests (56 cases)

- `getLineVrocPctFinitePoints` (3) -- null, NaN, null entries.
- `normalizeLineVrocPctLength` (3) -- default, below-2, accept.
- `computeLineVrocPct` (7) -- null, CONST V > 0 zero bit-exact,
  CONST V = 0 null, LINEAR UP positive, LINEAR DOWN negative,
  length parity, no mutation.
- `classifyLineVrocPctRegime` (4) -- above, below, at, none.
- `runLineVrocPct` (9) -- ok flags, defaults, options, sort by x,
  CONST V > 0 regime at, CONST V = 0 regime none, LINEAR UP /
  DOWN regimes.
- `computeLineVrocPctLayout` (6) -- empty, ok=true, panel stack,
  vroc path, zero in osc axis, CONST V = 0 empty vroc path.
- `describeLineVrocPctChart` (3) -- empty, mention, length.
- `<ChartLineVrocPct />` (20) -- empty placeholder, region role,
  ref forward, data attrs, regime counts, aria description,
  two legend items, legend toggle, controlled hide, badge,
  zero / axis / grid / legend on/off, className+style, animate
  off, vroc path, price path, defaultHiddenSeries.
- VROC Pct integration (1) -- CONST V > 0 VROC = 0 bit-exact
  across multiple V and length tuples.

## Files

- web/src/components/ui/chart-line-vroc-pct.tsx (new)
- web/src/components/ui/chart-line-vroc-pct.test.tsx (new)
- web/src/components/ui/index.ts (barrel export added)
- package.json + package-lock.json + web/package.json +
  web/package-lock.json bumped to 1.11.887
- CHANGELOG.md prepended

## Verification

- `npx vitest run src/components/ui/chart-line-vroc-pct.test.tsx`
  -- 56/56 pass.
- Typecheck: no new errors introduced (pre-existing errors in
  unrelated components remain).
