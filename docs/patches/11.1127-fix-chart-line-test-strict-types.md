# 11.1127 - fix 44 source-confirmed tsc strict-type errors in 22 chart-line .test.tsx files (chart bucket batch 7)

## Scope

Seventh batch of the chart showcase tsc-debt clear. 22 chart-line
indicator TEST files, exactly **2 TS2532 errors each** (44 total).
Fix scope is ONLY the .test.tsx files - **no chart component .tsx
was touched**.

## Baseline (HEAD before this patch)

`tsc --noEmit -p web/tsconfig.json`: 506 errors total, 44 in the 22
target files. Every error is the same shape:

```
expect(buttons[0].getAttribute('data-series-id')).toBe('price');
expect(buttons[1].getAttribute('data-series-id')).toBe('<key>');
```

after a `expect(buttons).toHaveLength(2);` guarantee.

Target files (22): chart-line-stoch-mid-cross-sig,
chart-line-stc-oversold-cross, chart-line-stc-overbought-cross,
chart-line-stc-mid-cross, chart-line-schaff-zero-cross,
chart-line-schaff-oversold-cross, chart-line-schaff-overbought-cross,
chart-line-rsi-mid-cross, chart-line-rsi-divergence-cross,
chart-line-roc-zero-cross, chart-line-rmi-zero-cross,
chart-line-rmi-oversold-cross, chart-line-rmi-overbought-cross,
chart-line-rmi-mid-cross, chart-line-qstick-zero-cross,
chart-line-qstick-mid-cross, chart-line-ppo-zero-cross,
chart-line-momentum-zero-cross, chart-line-mfi-oversold-cross,
chart-line-mfi-overbought-cross, chart-line-mfi-mid-cross,
chart-line-macd-zero-cross.

## After this patch

`tsc --noEmit -p web/tsconfig.json`: **462** errors total, **0**
across the same 22 files. Delta = 506 - 462 = **-44**. Zero new
errors anywhere.

## What changed

Single sed pattern across all 22 files:
- `expect(buttons[0].getAttribute` -> `expect(buttons[0]!.getAttribute`
- `expect(buttons[1].getAttribute` -> `expect(buttons[1]!.getAttribute`

Each test had a `toHaveLength(2)` guarantee 1-2 lines above the
indexed access, so the non-null `!` is safe.

## Verification

- `tsc --noEmit -p web/tsconfig.json`:
  - Baseline (HEAD): 506 total, 44 in target files.
  - After: 462 total, 0 in target files.
  - Net: -44, no new errors anywhere.
- `npx vitest run` on the 22 files: **22 files pass, 1517 tests pass,
  0 failed.** Zero NEW failures vs before.
- No `as any`, no `ts-ignore`, no fresh `ts-expect-error`.
- No edits outside the 22 dispatched test files.
- No test assertion behaviour changed.
