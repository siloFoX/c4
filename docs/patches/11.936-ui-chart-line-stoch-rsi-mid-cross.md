# 11.936 -- UI: chart-line-stoch-rsi-mid-cross primitive (v1.11.954)

## Summary

Adds `ChartLineStochRsiMidCross`, a pure-SVG dual-panel chart
that renders the close in the top panel and the close-only
Stochastic RSI line in the bottom panel. Marks bullish (cross
up through midline 50) / bearish (cross down through midline
50) second-derivative momentum centerline regime trigger
events. Stochastic RSI is the Stochastic oscillator applied
to the RSI series, exposing momentum-of-momentum turning
points earlier than the underlying RSI line. Continues the
midline-cross family (RSI 50, Stochastic 50, CCI 0, MFI 50,
now Stochastic RSI 50).

## Files

- `web/src/components/ui/chart-line-stoch-rsi-mid-cross.tsx`
  -- new, ~1300 LoC; pure-functional close-only Stochastic
  RSI pipeline (Wilder-smoothed RSI then Stochastic of RSI
  then SMA-k smoothing) plus a `forwardRef` React component.
- `web/src/components/ui/chart-line-stoch-rsi-mid-cross.test.tsx`
  -- new, 73 vitest cases covering pure helpers, Wilder
  smoothing, SMA, Stochastic RSI compute pipeline, regime
  classifier, single-threshold cross detector, layout, and
  JSX render path.
- `web/src/components/ui/index.ts` -- barrel re-export.
- `package.json` / `package-lock.json` / `web/package.json` /
  `web/package-lock.json` -- bumped 1.11.953 -> 1.11.954.

## Pipeline

For close-only data with defaults `rsiLength = 14`,
`stochLength = 14`, `kSmoothing = 3`:

```
rsi[i]   = close-only RSI(close, rsiLength)
rRange   = max(rsi[i-n+1..i]) - min(rsi[i-n+1..i])
rawK[i]  = rRange > 0
             ? ((rsi - lowestRsi) / rRange) * 100
             : 50
stochK[i]= SMA(rawK, kSmoothing)
bullish  : prev stochK <= 50 && cur stochK > 50
bearish  : prev stochK >= 50 && cur stochK < 50
```

Total warmup is `rsiLength + stochLength + kSmoothing - 2` =
29 bars by default.

## Bit-exact anchor

- CONST `close = K` for any finite K -> delta = 0 -> RSI = 50
  via zero-flow fallback. Range of RSI over the Stochastic
  window = 0 -> rawK = 50 via the zero-range neutral fallback.
  SMA of 50s = 50. stochK = 50 sits on the threshold but the
  strict-inequality detector never fires. regime `bullish`
  (stochK >= 50). cross count = 0. Verified across K in
  {0, 1, 42, 100, 1234}.
- LINEAR UP `close = i` -> delta = +1 every bar -> RSI = 100
  constant. Range of RSI = 0 -> stochK = 50 (same neutral
  fallback). regime `bullish` at boundary. 0 crosses. Worth
  noting: even an extreme RSI of 100 maps to stochK = 50
  when RSI is constant -- the Stochastic normalizes against
  its own window.
- LINEAR DOWN `close = -i` -> RSI = 0 constant -> stochK =
  50 (zero-range fallback). regime `bullish` at boundary. 0
  crosses. Same Stochastic-of-constant quirk.
- Decline (30 bars) followed by sharp rise (delta = +5) ->
  RSI rises from 0 to high, Stochastic of RSI sweeps through
  50, firing 1 bullish midline cross.
- Rise (30 bars) followed by sharp decline (delta = -5) ->
  1 bearish midline cross.

## Regime classifier

| stochK range            | regime      |
|-------------------------|-------------|
| stochK >= 50            | bullish     |
| stochK < 50             | bearish     |
| null                    | none        |

## Layout / a11y

- ARIA region + role=img SVG + sr-only desc.
- `data-section` attributes on every drawn group for
  end-to-end assertions.
- One horizontal reference band inside the oscillator panel
  (threshold at 50, the centerline).
- Cross markers in oscillator panel + chevron overlays in
  price panel; each carries `role="graphics-symbol"`,
  `tabIndex=0`, and a discrete colour per kind.
- Motion-safe fade-in animation, motion-reduce respected.
- Controlled / uncontrolled hidden-series legend with `Enter`
  / `Space` keyboard toggle parity.

## Defaults

- rsiLength = 14
- stochLength = 14
- kSmoothing = 3
- threshold = 50
- oscillator range = 0 .. 100 (fixed)
- width 720, height 460, padding 44, panelGap 12

## Testing

- `npx vitest run src/components/ui/chart-line-stoch-rsi-mid-cross.test.tsx`
  -> 73 / 73 green (transform 89ms, tests 284ms).
