# 11.1123 - fix 47 source-confirmed tsc strict-type errors in 11 chart-line .test.tsx files (chart bucket batch 3)

## Scope

Third batch of the chart showcase tsc-debt clear. 11 chart-line
indicator TEST files holding 47 errors total. Fix scope is ONLY
the .test.tsx files - **no chart component .tsx was touched**.

## Baseline (HEAD before this patch)

`tsc --noEmit -p web/tsconfig.json`: 693 errors total, 47 in the 11
target files. All 47 errors are TS4111 index-signature bracket access
on `HTMLElement.dataset.<key>` lookups inside the per-component
`data-section="..."` assertions.

Per-file error count: adx-cross-extreme (5), atr-channel-cross (5),
choppiness-cross (5), rsi-overbought-cross (4), rsi-oversold-cross
(4), stoch-cross-pct (4), trix-cross-sig (4), volume-osc-cross (4),
vroc-cross-sig (4), vwap-cross-sig (4), williams-r-cross (4). Total
= 47.

## After this patch

`tsc --noEmit -p web/tsconfig.json`: **646** errors total, **0**
across the same 11 files. Delta = 693 - 646 = **-47**. Zero new
errors anywhere.

## What changed (per file, minimal)

Same shape as 11.1121 / 11.1122: pure `dataset.X` -> `dataset['X']`
bracket access. No behaviour change, no assertion change. Per-file
keys:

- **chart-line-adx-cross-extreme.test.tsx** -- length, upperThreshold, lowerThreshold, totalPoints, crossCount.
- **chart-line-atr-channel-cross.test.tsx** -- length, atrLength, mult, totalPoints, crossCount.
- **chart-line-choppiness-cross.test.tsx** -- length, trendThreshold, chopThreshold, totalPoints, crossCount.
- **chart-line-rsi-overbought-cross.test.tsx** -- length, threshold, totalPoints, crossCount.
- **chart-line-rsi-oversold-cross.test.tsx** -- length, threshold, totalPoints, crossCount.
- **chart-line-stoch-cross-pct.test.tsx** -- kLength, slowKLength, dLength, totalPoints.
- **chart-line-trix-cross-sig.test.tsx** -- length, signalLength, totalPoints, crossCount.
- **chart-line-volume-osc-cross.test.tsx** -- shortLength, longLength, totalPoints, crossCount.
- **chart-line-vroc-cross-sig.test.tsx** -- length, signalLength, totalPoints, crossCount.
- **chart-line-vwap-cross-sig.test.tsx** -- length, signalLength, totalPoints, crossCount.
- **chart-line-williams-r-cross.test.tsx** -- length, signalLength, totalPoints, crossCount.

## Verification

- `tsc --noEmit -p web/tsconfig.json`:
  - Baseline (HEAD): 693 total, 47 in target files.
  - After: 646 total, 0 in target files.
  - Net: -47, no new errors anywhere.
- `npx vitest run` on the 11 files: **11 files pass, 620 tests pass,
  0 failed.** Zero NEW failures vs before.
- No `as any`, no `ts-ignore`, no fresh `ts-expect-error`.
- No edits outside the 11 dispatched test files.
- No test assertion behaviour changed.
