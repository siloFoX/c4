# 11.295 -- EmptyState helpLink + data selectors (v1.11.313)

Component-scope-only enhancement of the existing `EmptyState`
primitive. No daemon-side change and no `c4` CLI surface change.
The default surface (no helpLink) is preserved byte-for-byte
modulo the new data-section attribute set.

## Why

The existing EmptyState ships three affordance slots:

- `action` -- primary button-style CTA.
- `secondaryAction` -- button OR anchor styled as a
  primary-text link.
- (and an `icon`/`illustration` slot above the text).

The dispatch asks for "illustration slot, primary/secondary
action buttons, optional secondary help link". The first three
already exist; the missing piece is the "optional secondary
help link" -- a dedicated, visually-quieter help-doc anchor
that should NOT compete with the primary or secondary CTAs.

Patch 11.295 adds the new `helpLink` slot with a quieter
muted-foreground treatment so the visual hierarchy reads:

  action (button) > secondaryAction (primary-text link) >
  helpLink (muted-foreground link).

The patch also widens the data-attribute selector set so e2e
tests can target the title / description / illustration /
action / secondary / helpLink slots without parsing class
names.

## What changed

### `web/src/components/ui/empty-state.tsx`

- New exported type
  `EmptyStateHelpLink = { label: string; href: string }`.
- New `helpLink?: EmptyStateHelpLink` prop. Renders below the
  action + secondaryAction stack as a `<a>` with the
  muted-foreground tone. External hrefs (starting with
  `http`) open in a new tab with
  `rel="noreferrer noopener"`; relative paths render without
  the new-tab attributes.
- Data-attribute selectors added:
  - `data-section="empty-state"` on the root div.
  - `data-section="empty-state-illustration"` on the
    icon/illustration slot wrapper (only when an icon /
    illustration is set).
  - `data-section="empty-state-text"` on the
    title+description wrapper.
  - `data-section="empty-state-title"` on the title span.
  - `data-section="empty-state-description"` on the
    description span.
  - `data-section="empty-state-action"` on the primary
    action wrapper (button-shape or custom ReactNode).
  - `data-section="empty-state-secondary"` on the secondary
    affordance (button OR anchor branch).
  - `data-section="empty-state-help-link"` on the new
    helpLink anchor.
- `EmptyState.displayName = 'EmptyState'` for devtools.

### Adopted

- Primitive enhancement only this commit. The existing
  EmptyState call sites (Workers empty, Sessions empty,
  History empty, Templates empty, etc.) inherit the new
  data-section attributes without any caller change.

## Deferred (dispatch follow-ups)

- Workers empty -> add `helpLink` pointing to the workers
  docs (`/docs/workers` or the canonical hosted URL).
- Sessions empty -> same for sessions.
- History empty -> same for history.

## Tests

- `web/src/components/ui/empty-state.test.tsx` -- 13 new
  vitest cases:
  - helpLink renders with label + relative href.
  - External href gets target=_blank + rel=noreferrer
    noopener.
  - Relative href does not get the external attrs.
  - helpLink is hidden when omitted.
  - helpLink coexists with action + secondaryAction.
  - data-section="empty-state" on the root.
  - data-section="empty-state-text" on the title/description
    wrapper.
  - data-section="empty-state-title" +
    "empty-state-description" content.
  - data-section="empty-state-illustration" only when set.
  - data-section="empty-state-action" on the primary action
    wrapper.
  - data-section="empty-state-secondary" on the secondary
    affordance.
  - data-section="empty-state-help-link" on the help link.
  - Stable displayName.
  All 34 (21 original + 13 new) green.

## Versions

- 1.11.312 -> 1.11.313 across root + web package.json + both
  lockfiles. CHANGELOG.md entry under `## [1.11.313]`.

## Notes

- The helpLink is intentionally a quieter visual treatment
  than `secondaryAction` (text-muted-foreground vs
  text-primary). The hierarchy is: action (button) >
  secondaryAction (primary-text link) > helpLink
  (muted-foreground link).
- External-link detection uses `href.startsWith('http')` so
  protocol-relative URLs (`//example.com/x`) and mailto:
  hrefs fall through as relative-style anchors. That matches
  the secondaryAction behaviour for consistency.
