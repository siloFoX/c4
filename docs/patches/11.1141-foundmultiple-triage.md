# 11.1141 - triage 3 test files failing with TestingLibrary found-multiple-elements

## Scope

3 test files where TestingLibrary `getByText` / `getByRole` queries
matched multiple intentionally-rendered elements. Per the dispatch's
STRICT CONTRACT, every failing assertion was traced to the component
source + git log; all 15 failing tests classify as intended dual
render (or downstream class drift from intended ListItem migration),
NOT real duplication bugs. Test queries scoped to the intended
instance; ZERO source files touched.

## Per-file triage

| File | Failures | Root cause (component source + commit) | Verdict | Fix |
|---|---|---|---|---|
| ErrorBoundary.test.tsx | 2 | The error UI renders BOTH a Collapsible trigger button with accessible name "Stack trace" AND a sibling "Copy stack trace" outline button. The test queried by `/stack trace/i` (case-insensitive regex) which matched both buttons. | Intended dual render | Switch to exact name `'Stack trace'` (the Copy button's accessible name is "Copy stack trace", so exact-string match disambiguates without breaking semantics). |
| SessionsAttachedSection.test.tsx | 1 | `TODO 11.210 / v1.11.228` introduced the `<RelativeTime>` component, which renders a `<time>` element. The DOM is now `<span>- <time>1 month ago</time></span>` where the dash sits in the span's direct text node. testing-library's `getNodeText` only considers direct text nodes, which (after normalize) collapses to "-" - the regex `/^-\s\S/` cannot match because there is no `\S` following the whitespace. | Intended dual render (the dash+time block is two DOM nodes by design) | Replace regex query with a function matcher that probes the span's full `textContent` so the prefix-plus-relative-time pattern is preserved. |
| SessionsListSection.test.tsx | 12 | `TODO 11.149 / v1.11.167` added a read-only Recent activity Timeline `<ol data-timeline>` above the interactive grouped `<ul>`. The timeline reproduces shortIds + descriptions for the 5 newest sessions, so any unscoped `screen.getByText(/sid-.../)` or `getByText('proj-b')` matches two elements: the timeline title/description AND the row content in the main list. Additionally, `TODO 11.165 / v1.11.183` migrated session rows from a raw `<button class="hover:bg-accent...">` to the `ListItem` primitive whose interactive root uses `hover:bg-muted/30`. | Intended dual render (timeline + interactive list) + intended hover-token migration | (a) Extended `renderSection` to expose `mainList = container.querySelector('ul.divide-y')`; (b) Scoped all 12 failing tests through `within(mainList!).{getByText\|queryByText}(...)` to skip the timeline; (c) Updated the hover-class assertion from `/hover:bg-accent/` to `/hover:bg-muted/` to match the ListItem-standard hover token. |

All 15 failures: intended-dual-render (fixed). 0 real-bug (reported).

## Verification

- `tsc --noEmit -p web/tsconfig.json`: 0 errors (PROJECT-WIDE GREEN
  preserved from 11.1138-11.1140).
- `npm run build`: green (~11s).
- `vitest run` on the 3 test files: 3/3 files pass + 83/83 tests
  pass.
- `git diff --name-only` after this patch: only the 3 dispatched
  test files. ZERO source files touched.
