# 11.1124 - fix 48 source-confirmed tsc strict-type errors in 13 chart-line .test.tsx files (chart bucket batch 4)

## Scope

Fourth batch of the chart showcase tsc-debt clear. 13 chart-line
indicator TEST files holding 48 errors total (44x TS4111 + 4x
TS2532). Fix scope is ONLY the .test.tsx files - **no chart
component .tsx was touched**.

## Baseline (HEAD before this patch)

`tsc --noEmit -p web/tsconfig.json`: 646 errors total, 48 in the 13
target files.

Per-file error count:
- balance-power-cross (4 TS4111)
- bb-cross-sig (4 TS4111)
- cci-cross-sig (4 TS4111)
- dema-cross-pct (4 TS4111)
- ehlers-fisher-cross (4 TS4111)
- elder-bear-cross (4 TS4111)
- elder-bull-cross (4 TS4111)
- elder-ray-cross (4 TS4111)
- hma-cross-pct (4 TS4111)
- mfi-cross-sig (4 TS4111)
- roc-cross-sig (4 TS4111)
- stoch-rsi-overbought-cross (2 TS2532)
- stoch-rsi-oversold-cross (2 TS2532)

Total: **48**.

## After this patch

`tsc --noEmit -p web/tsconfig.json`: **598** errors total, **0**
across the same 13 files. Delta = 646 - 598 = **-48**. Zero new
errors anywhere.

## What changed (per file, minimal)

11 files use the same `dataset.X` -> `dataset['X']` bracket access
pattern as 11.1121 / 11.1122 / 11.1123 on `HTMLElement.dataset`
lookups inside the `data-section` assertions. 2 files use non-null
`!` on `container.querySelectorAll(...)[N]` after a `toHaveLength(2)`
guarantee.

- **chart-line-balance-power-cross.test.tsx** -- barLength, smoothLength, totalPoints, crossCount.
- **chart-line-bb-cross-sig.test.tsx** -- length, mult, totalPoints, crossCount.
- **chart-line-cci-cross-sig.test.tsx** -- length, signalLength, totalPoints, crossCount.
- **chart-line-dema-cross-pct.test.tsx** -- length, totalPoints, aboveCount, belowCount.
- **chart-line-ehlers-fisher-cross.test.tsx** -- length, signalLength, totalPoints, crossCount.
- **chart-line-elder-bear-cross.test.tsx** -- length, lowProxyLength, totalPoints, crossCount.
- **chart-line-elder-bull-cross.test.tsx** -- length, highProxyLength, totalPoints, crossCount.
- **chart-line-elder-ray-cross.test.tsx** -- length, highProxyLength, totalPoints, crossCount.
- **chart-line-hma-cross-pct.test.tsx** -- length, totalPoints, aboveCount, belowCount.
- **chart-line-mfi-cross-sig.test.tsx** -- length, signalLength, totalPoints, crossCount.
- **chart-line-roc-cross-sig.test.tsx** -- length, signalLength, totalPoints, crossCount.
- **chart-line-stoch-rsi-overbought-cross.test.tsx** -- 2x `buttons[N].getAttribute(...)` -> `buttons[N]!.getAttribute(...)` after `toHaveLength(2)` guarantees presence.
- **chart-line-stoch-rsi-oversold-cross.test.tsx** -- same 2x non-null after `toHaveLength(2)`.

## Verification

- `tsc --noEmit -p web/tsconfig.json`:
  - Baseline (HEAD): 646 total, 48 in target files.
  - After: 598 total, 0 in target files.
  - Net: -48, no new errors anywhere.
- `npx vitest run` on the 13 files: **13 files pass, 740 tests pass,
  0 failed.** Zero NEW failures vs before.
- No `as any`, no `ts-ignore`, no fresh `ts-expect-error`.
- No edits outside the 13 dispatched test files.
- No test assertion behaviour changed.
