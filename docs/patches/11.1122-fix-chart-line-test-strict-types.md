# 11.1122 - fix 50 source-confirmed tsc strict-type errors in 10 chart-line .test.tsx files (chart bucket batch 2)

## Scope

Second batch of the chart showcase tsc-debt clear. 10 chart-line
indicator TEST files, 5 errors each (50 total). Fix scope is ONLY
the .test.tsx files - **no chart component .tsx was touched**.

## Baseline (HEAD before this patch)

`tsc --noEmit -p web/tsconfig.json`: 743 errors total, 50 in the 10
target files (5 each):

- chart-line-stoch-overbought-cross
- chart-line-stoch-cross-sig
- chart-line-squeeze-cross
- chart-line-psar-cross-pct
- chart-line-macd-hist-cross
- chart-line-macd-cross-sig
- chart-line-kvo-cross
- chart-line-kvo-cross-sig
- chart-line-keltner-cross-sig
- chart-line-keltner-cross-pct

All 50 errors are TS4111 index-signature bracket access on the
`HTMLElement.dataset.<key>` lookups inside the per-component
`data-section="..."` assertions.

## After this patch

`tsc --noEmit -p web/tsconfig.json`: **693** errors total, **0**
across the same 10 files. Delta = 743 - 693 = **-50**. Zero new
errors anywhere.

## What changed (per file, minimal)

Same shape as 11.1121: pure `dataset.X` -> `dataset['X']` bracket
access. No behaviour change, no assertion change. Per-file keys:

- **chart-line-keltner-cross-pct.test.tsx** -- length, atrLength, mult, totalPoints, crossCount.
- **chart-line-keltner-cross-sig.test.tsx** -- length, atrLength, mult, totalPoints, crossCount.
- **chart-line-kvo-cross-sig.test.tsx** -- fastLength, slowLength, signalLength, totalPoints, crossCount.
- **chart-line-kvo-cross.test.tsx** -- fastLength, slowLength, signalLength, totalPoints, crossCount.
- **chart-line-macd-cross-sig.test.tsx** -- fastLength, slowLength, signalLength, totalPoints, crossCount.
- **chart-line-macd-hist-cross.test.tsx** -- fastLength, slowLength, signalLength, totalPoints, crossCount.
- **chart-line-psar-cross-pct.test.tsx** -- afStep, afMax, totalPoints, aboveCount, belowCount.
- **chart-line-squeeze-cross.test.tsx** -- length, bbMult, kcMult, totalPoints, crossCount.
- **chart-line-stoch-cross-sig.test.tsx** -- length, kSmoothing, signalLength, totalPoints, crossCount.
- **chart-line-stoch-overbought-cross.test.tsx** -- length, kSmoothing, threshold, totalPoints, crossCount.

## Verification

- `tsc --noEmit -p web/tsconfig.json`:
  - Baseline (HEAD): 743 total, 50 in target files.
  - After: 693 total, 0 in target files.
  - Net: -50, no new errors anywhere.
- `npx vitest run` on the 10 files: **10 files pass, 568 tests pass,
  0 failed.** Zero NEW failures vs before.
- No `as any`, no `ts-ignore`, no fresh `ts-expect-error`.
- No edits outside the 10 dispatched test files.
- No test assertion behaviour changed.
