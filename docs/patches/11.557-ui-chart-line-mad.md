# 11.557 -- web/src/components/ui/chart-line-mad.tsx

Pure-SVG line chart with a **rolling Median Absolute
Deviation (MAD) band overlay**. The raw observations render
as a dimmed reference line; the rolling median renders as a
solid coloured centre line; a translucent
`median +/- k * scaledMAD` band fills the robust spread
envelope; points outside the band are flagged as robust
outliers.

## Math

For index `t` the trailing window is the slice
`values[max(0, t - W + 1) .. t]` -- it expands from a single
point until it reaches the full window length `W`. Over that
window:

```
median   = median of the window
MAD      = median(|v - median| for v in the window)
scaledMAD = MAD * madScale
upper    = median + k * scaledMAD
lower    = median - k * scaledMAD
```

- `MAD` is the **Median Absolute Deviation** -- the median
  of the absolute deviations from the window median.
- `madScale` (default `1.4826`) is the Gaussian-consistency
  constant that makes `scale * MAD` an unbiased estimator of
  the standard deviation for normal data.
- `k` (default `3`) is the band-width multiplier (the
  canonical Hampel-style robust threshold).

A point is a **robust outlier** when it falls strictly
outside the band: `'above'` when `raw > upper`, `'below'`
when `raw < lower`.

### Why MAD is robust

MAD has a **50% breakdown point**: up to half the window can
be arbitrarily corrupted without changing it. A lone outlier
inside the window does **not** inflate the MAD, so the band
does not widen to swallow the outlier -- the outlier stays
visibly outside the band. This is the decisive difference
from mean/standard-deviation bands.

### Verified properties (by test)

- `MAD([1,2,3,4,5]) = 1`; `MAD([1,2,3,4,5,6]) = 1.5`.
- **`MAD([1,2,3,4,1000]) = 1`** -- unchanged from
  `[1,2,3,4,5]` despite the 1000; the lone outlier does not
  inflate it.
- A constant window has `MAD = 0` (the band collapses onto
  the median).
- For the `OUTLIER_FIXTURE` (`[1,2,3,4,5,50]`, window 5) the
  final window `[2,3,4,5,50]` has `median = 4`, `MAD = 1`,
  `scaledMAD = 1.4826`, band `[~-0.45, ~8.45]` -- and the
  `50` lands far above it, flagged.
- A lone spike inside an otherwise-constant window
  (`[10,10,10,10,100]`) has `MAD = 0`, so the band collapses
  and the `100` is caught -- a mean/stddev band would have
  been inflated by the `100` itself and missed it.

## Distinct from related primitives

- `<ChartLineHampel>` (11.548): the Hampel **filter** also
  uses the rolling median + MAD, but it *replaces* flagged
  outliers with the window median and draws an
  original-vs-filtered overlay -- it cleans the data.
  ChartLineMad instead draws the MAD **band as an overlay**:
  it does not modify any value, it shows the robust spread
  envelope and which points fall outside it.
- `<ChartLineBollinger>` (11.537): Bollinger Bands use a
  rolling **mean** +/- `k` * rolling **sample standard
  deviation**. Mean and standard deviation are
  *non-robust* -- a single outlier shifts the mean and
  inflates the stddev, widening the band so the outlier
  hides inside it. ChartLineMad uses the rolling **median**
  and **MAD**, both robust, so the band stays tight and the
  outlier stands out.
- `<ChartLineEwmsd>` (11.555): plots an EWMA mean and an
  exponentially-weighted standard-deviation band -- still a
  non-robust mean/variance pair, with exponential weighting.
  ChartLineMad uses robust order statistics over a flat
  trailing window.
- `<ChartLineControl>` (11.534): a Shewhart SPC chart with
  *global, constant, non-robust* `mean +/- k*sigma` limits.
  ChartLineMad's band is rolling and robust.
- `<ChartLineZscore>` (11.556): a rolling z-score panel
  (mean / standard deviation). ChartLineMad uses the robust
  median / MAD pair and shows the band on the main panel
  rather than a separate z-score panel.

The robust counterpart of Bollinger Bands -- the
order-statistic spread envelope that does not let outliers
hide themselves.

## Public surface

- `<ChartLineMad series={...} />`.
- `series: ChartLineMadSeries[]` --
  `{id, label, data, color?, window?, multiplier?}`.
  Per-series `window` / `multiplier` overrides beat
  chart-level.
- `window` (default 20) -- rolling window length; clamped to
  an integer `>= 2`.
- `multiplier` (default 3) -- band-width `k`; clamped to
  `>= 0`.
- `madScale` (default 1.4826) -- MAD-to-sigma scale; clamped
  to `> 0`.
- `rawColor`, `outlierColor`.
- `bandOpacity` (default 0.15).
- Toggle flags: `showAxis`, `showGrid`, `showDots`,
  `showLegend`, `showTooltip`, `showConfigBadge`, `showRaw`,
  `showMedian`, `showBand`, `showOutliers`, `animate`.
- Controlled + uncontrolled visibility.
- Standard sizing + format + a11y props.
- `formatValue` / `formatX` / `formatCoefficient`.
- `onPointClick({series, point})`.

## Pure helpers

- `getLineMadDefaultColor(index)`.
- `getLineMadFinitePoints(points)`.
- `normaliseLineMadWindow(value)` -- integer `>= 2`, default
  20.
- `normaliseLineMadMultiplier(value)` -- `>= 0`, default 3.
- `normaliseLineMadScale(value)` -- `> 0`, default 1.4826.
- `computeLineMadMedian(values)` -- median; drops non-finite;
  NaN for empty.
- `computeLineMadDeviation(values, centerMedian?)` -- the
  MAD (median of absolute deviations from the median);
  accepts a pre-computed centre median; NaN for empty.
- `classifyLineMadOutlier(value, lower, upper)` -- `'above' |
  'below' | 'within'` (strict comparison).
- `computeRollingMad(values, window)` -- rolling median +
  MAD per index; returns `{median, mad, windowSize}`. Drops
  non-finite values.
- `runLineMad(points, options?)` -- canonical pipeline.
  Sorts ascending, drops non-finite, builds the band and the
  outlier flags, returns per-sample `{index, x, raw, median,
  mad, scaledMad, upper, lower, outlier, windowSize}` plus
  outlier counts and `finalMedian` / `finalScaledMad` /
  `maxScaledMad`.
- `computeLineMadLayout({...})` -- projects raw + median
  paths, the band fill + edge paths, and the outlier list.
- `describeLineMadChart(series, options?)`.

## ARIA + test selectors

- Root `role="region"` + `aria-label` + `aria-describedby`.
- SVG `role="img"`.
- Raw path + median path + outlier markers + every dot
  `role="graphics-symbol"` + `tabIndex={0}`. The median path
  aria-label includes the window length.
- Every node carries `data-section="chart-line-mad-..."`.
- Root mirrors `data-series-count`,
  `data-visible-series-count`, `data-total-points`,
  `data-total-outliers`, `data-window`, `data-multiplier`,
  `data-mad-scale`, `data-animate`.
- Series groups expose `data-series-window`,
  `data-series-multiplier`, `data-series-mad-scale`,
  `data-series-final-median`, `data-series-final-scaled-mad`,
  `data-series-max-scaled-mad`, `data-series-outlier-count`,
  `data-series-above-count`, `data-series-below-count`,
  `data-series-finite-count`.
- Raw path `data-kind="raw"`; median path
  `data-kind="median"`; band edge paths `data-kind="upper"` /
  `"lower"`. Dots expose `data-median`, `data-mad`,
  `data-scaled-mad`, `data-upper`, `data-lower`,
  `data-outlier`. Outlier markers carry `data-outlier` and
  `data-point-index`.

## Tooltip

Hovering a dot shows the series label, x, raw value, bold
`rolling median`, `scaled MAD`, the `band: [lower, upper]`
range, and -- when the point is outside the band -- a
coloured `robust outlier (<above|below>)` row.

## Config badge

Top-left badge shows
`MAD W=<window> k=<multiplier> c=<madScale> out=<count>`.

## Tests

86 vitest cases in `chart-line-mad.test.tsx`, all passing:

- Defaults: positive sizes, window >= 2 + multiplier +
  scale 1.4826, 10-colour palette.
- `getLineMadDefaultColor`: cycle, NaN / negative.
- `getLineMadFinitePoints`: drops non-finite.
- `normaliseLineMadWindow`: fallback, clamp >= 2, floor.
- `normaliseLineMadMultiplier`: fallback, clamp >= 0.
- `normaliseLineMadScale`: fallback for non-finite /
  non-positive, pass valid.
- `computeLineMadMedian`: empty -> NaN, odd / even length,
  sorts internally, drops non-finite.
- `computeLineMadDeviation`: empty -> NaN, **MAD([1..5])
  = 1**, **MAD([1..6]) = 1.5**, **robust: MAD([1,2,3,4,1000])
  = 1**, constant -> 0, pre-computed centre median.
- `computeRollingMad`: empty, window expands 1,2,3,3,3,
  rolling median + MAD verified on a ramp, single-point
  window -> MAD 0, constant -> MAD 0, drops non-finite.
- `classifyLineMadOutlier`: above / below / within / strict
  boundary / non-finite.
- `runLineMad`: empty, **band = median +/- k * scaledMAD**,
  **flags a robust outlier outside the band**, **robustness:
  a lone spike inside the window is still flagged (MAD 0)**,
  constant series no outliers, **index 0 always within**,
  sorts ascending + drops non-finite, tracks finalMedian +
  finalScaledMad + maxScaledMad, exposes window + multiplier
  + madScale.
- `computeLineMadLayout`: empty / degenerate / hidden / raw
  + median + band fill + edge paths / config + outliers
  exposed / per-series window + multiplier overrides / y
  range covers the band / hidden excluded / bounds overrides
  / totalPoints.
- `describeLineMadChart`: No data, summary mentions the MAD
  band + window + robust outliers, hidden filter.
- `<ChartLineMad>` render: empty, raw path kind=raw, median
  path kind=median, band fill + upper/lower edge paths,
  hide band, hide raw, hide median, **outlier marker for
  the detected robust outlier**, hide outliers, dots via
  showDots, config badge with window + multiplier + scale +
  outlier count + hide, ARIA region + img + sr-only desc,
  root data-*, series group data-*, tooltip with raw +
  median + MAD + band rows + an outlier row on the flagged
  dot + hide on leave + omit via prop, onPointClick payload,
  legend with window + outlier + toggle + omit, animate
  flag + class, ref forwarding, displayName, custom
  ariaLabel, xLabel + yLabel, constant series renders zero
  outliers.

## Files

- `web/src/components/ui/chart-line-mad.tsx`
- `web/src/components/ui/chart-line-mad.test.tsx`
- `web/src/components/ui/index.ts` -- appended
  `export * from './chart-line-mad';`
- `CHANGELOG.md` -- prepended `[1.11.575]` entry.
- `package.json`, `package-lock.json`, `web/package.json`,
  `web/package-lock.json` -- bumped to `1.11.575`.
