# Patch 11.892 -- UI: chart-line-roc-cross-pct primitive

**Version:** v1.11.910
**Queue TODO:** 11.892
**Branch:** c4/c4-mgr-auto
**Status:** done

## Summary

Added `<ChartLineRocCrossPct />` -- a pure-SVG, dual-panel
React/TS primitive that renders the close in the top panel and
overlays the Rate of Change (ROC), its EMA-smoothed signal, and
the `rocPct = (ROC - signal)` deviation channel in the bottom
panel. ROC is itself a percent indicator, so the cross-pct
treatment surfaces the *magnitude of momentum deviation* (how
stretched ROC is above or below its signal) on the same percent
scale for cross-instrument comparability, distinct from the
sign-based crossings published by 11.891
`chart-line-roc-cross-sig`:

```
ROC[i]    = close[i - length] === 0
              ? null
              : (close[i] - close[i - length]) /
                close[i - length] * 100
signal[i] = EMA(ROC, signalLength)
rocPct[i] = ROC[i] - signal[i]
```

Defaults: `length = 14` (canonical ROC window), `signalLength =
9`. Regime classifier: `above` (rocPct > 0), `below` (< 0),
`at` (= 0), `none` (null).

## Bit-exact anchor

- **CONST close = K (K > 0)**: `close[i] - close[i-length] = K
  - K = 0` so `ROC = 0` every bar after warmup. signal EMA of
  0s = 0. `rocPct = 0 - 0 = 0` every bar after the chained
  warmup. Verified across K = 1..1234. CONST K = 0 triggers
  the divide-by-zero guard -> ROC = null -> rocPct = null,
  regime none.

## Surface

- Helpers exported: `getLineRocCrossPctFinitePoints`,
  `normalizeLineRocCrossPctLength`,
  `applyLineRocCrossPctEma`, `computeLineRocCrossPct`,
  `classifyLineRocCrossPctRegime`, `runLineRocCrossPct`,
  `computeLineRocCrossPctLayout`,
  `describeLineRocCrossPctChart`.
- Component: `<ChartLineRocCrossPct />` (forwardRef on a
  `<div>` wrapping the SVG + legend + badge).
- Data attrs: `data-section="chart-line-roc-cross-pct"`,
  `data-length`, `data-signal-length`, `data-total-points`,
  `data-above-count`, `data-below-count`, `data-at-count`.
- Accessibility: `role="region"` + `aria-label` +
  `aria-describedby`, inner `<svg role="img">`, sr-only desc,
  motion-safe fade-in.
- Legend: four toggleable buttons (`close`, `roc`, `signal`,
  `rocPct`); controlled / uncontrolled modes;
  `onSeriesToggle` reports diffs.
- Tooltip: `x`, `close`, `roc`, `signal`, `rocPct`, regime,
  plus running regime counts.
- Renderer: top panel renders the close, bottom panel overlays
  ROC, signal, and rocPct over a dashed zero reference.

## Tests (50 cases)

- `getLineRocCrossPctFinitePoints` (3) -- null, NaN, null
  entries.
- `normalizeLineRocCrossPctLength` (3) -- default, below-1,
  accept.
- `applyLineRocCrossPctEma` (1) -- CONST bit-exact across K.
- `computeLineRocCrossPct` (5) -- null, CONST K > 0 -> 0/0/0,
  CONST K = 0 -> null, defaults, no mutation.
- `classifyLineRocCrossPctRegime` (4) -- above, below, at,
  none.
- `runLineRocCrossPct` (7) -- ok flags, defaults, options,
  sort by x, CONST K > 0 regime at, CONST K = 0 regime none.
- `computeLineRocCrossPctLayout` (5) -- empty, ok=true, panel
  stack, zero in osc axis, price + roc paths.
- `describeLineRocCrossPctChart` (3) -- empty, mention, both
  lengths.
- `<ChartLineRocCrossPct />` (18) -- empty placeholder, region
  role, ref forward, data attrs, aria desc, four legend items,
  legend toggle, controlled hide, badge, zero / axis / grid /
  legend on/off, className+style, animate off, roc+signal+pct
  paths, price path, defaultHiddenSeries.
- ROC Cross Pct integration (1) -- CONST K > 0 bit-exact ROC =
  0, signal = 0, rocPct = 0 across K = 1..1234.

## Relationship to chart-line-roc-cross-sig (11.891)

| primitive | bottom panel | use-case |
| --- | --- | --- |
| `chart-line-roc-cross-sig` (11.891) | ROC + signal + cross dots + price arrows | discrete trigger events |
| `chart-line-roc-cross-pct` (11.892) | ROC + signal + (ROC - signal) magnitude channel | continuous momentum deviation magnitude |

11.891 publishes the actionable bullish / bearish cross events;
11.892 surfaces the continuous percent magnitude of the
deviation between ROC and its signal so the momentum strength
can be compared across instruments.

## Files

- web/src/components/ui/chart-line-roc-cross-pct.tsx (new)
- web/src/components/ui/chart-line-roc-cross-pct.test.tsx (new)
- web/src/components/ui/index.ts (barrel export added)
- package.json + package-lock.json + web/package.json +
  web/package-lock.json bumped to 1.11.910
- CHANGELOG.md prepended

## Verification

- `npx vitest run
  src/components/ui/chart-line-roc-cross-pct.test.tsx` -- 50/50
  pass.
- Typecheck: no new errors introduced.
