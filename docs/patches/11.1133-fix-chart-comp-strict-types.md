# 11.1133 - fix 43 source-confirmed tsc strict-type errors in 32 chart component files (chart bucket batch 13, fourth component batch)

## Scope

Fourth chart **component** source batch. 32 chart `.tsx` component
files holding 43 errors total. Every fix is PURE TYPE - erased at
compile, zero JS emission change, zero rendered output change.

## Baseline (HEAD before this patch)

`tsc --noEmit -p web/tsconfig.json`: 234 errors total, 43 in the 32
target files.

Error mix:
- TS2379 exactOptional (28): inline options types got `| undefined`
  added to each optional property per the dispatch's exact
  instruction.
- TS7022 self-referential implicit any (5): `const next: number`
  annotation to break the inference cycle.
- TS6133 unused (7): genuinely-unused locals removed in
  chart-line-anomaly (highAnomalyColor, lowAnomalyColor),
  chart-pictogram (headCy), chart-radial-bar (totalThickness),
  chart-stacked-bar (labelMargin), chart-waterfall (next).
- TS6196 unused type (1): InternalNode interface in chart-sunburst.
- TS2322 (3): `!` non-null on `axisLabels[i]` in chart-radial-area,
  chart-radial-line, and on `adx[i]` in chart-line-adx-divergence-cross.
- TS2532 possibly-undefined (2): `!` non-null on `pattern[k]`, etc.
  in chart-line-decompose, and `series[hovered]!` in chart-stream.
- TS2345 (1): `cumulativePercent[hovered]!` in chart-pareto.
- TS18048 (1): same shape as TS2532, covered by `series[hovered]!`
  in chart-stream.

Total: **43**.

## After this patch

`tsc --noEmit -p web/tsconfig.json`: **191** errors total, **0** in
the 32 files. Delta = 234 - 191 = **-43**. Zero new errors anywhere.
`.test.*` files still at zero project-wide. Non-chart-component
files still at zero project-wide.

## What changed (per file, minimal pure-type)

All 32 files received minimal pure-type fixes following the proven
method from 11.1130 / 11.1131 / 11.1132. See per-file detail in
the commit message.

## Verification

- `tsc --noEmit -p web/tsconfig.json`:
  - Baseline (HEAD): 234 total, 43 in target files.
  - After: 191 total, 0 in target files.
  - Net: -43, no new errors anywhere.
  - **Zero `.test.*` errors anywhere project-wide.**
  - **Zero non-chart-component errors anywhere project-wide.**
- `npx vitest run` on 12 matching test files: **12 files pass,
  809 tests pass, 0 failed.** Zero NEW failures vs before.
- Playwright `e2e/gallery-no-crash.spec.ts`: **1 test passed**.
- No `as any`, no `ts-ignore`, no fresh `ts-expect-error`.
- No edits outside the 32 dispatched component files.
- No rendered output change.
