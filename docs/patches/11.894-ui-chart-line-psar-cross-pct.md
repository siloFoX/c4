# Patch 11.894 -- UI: chart-line-psar-cross-pct primitive

**Version:** v1.11.912
**Queue TODO:** 11.894
**Branch:** c4/c4-mgr-auto
**Status:** done

## Summary

Added `<ChartLinePsarCrossPct />` -- a pure-SVG, dual-panel
React/TS primitive that overlays the close with the Parabolic
SAR (PSAR) in the top panel and renders the `close - PSAR`
deviation scaled to the close as a percent in the bottom panel.
The percent normalisation makes the parabolic trend strength
comparable across instruments at different price magnitudes:

```
AF[i]       = acceleration factor (afStep..afMax, increments
              on each new extreme point)
EP[i]       = extreme point in current trend (max close while
              up, min close while down)
newSAR      = SAR[i-1] + AF[i-1] * (EP[i-1] - SAR[i-1])
            with cap = min(close[i-1], close[i-2]) when up,
                 cap = max(close[i-1], close[i-2]) when down
            with reversal when newSAR crosses close[i]
psarPct[i]  = close[i] === 0 ? null
                             : (close[i] - SAR[i]) / close[i] *
                               100
```

Defaults: `afStep = 0.02`, `afMax = 0.2` (canonical Wilder).
Regime classifier: `above` (psarPct > 0; close above SAR =
uptrend), `below` (close below SAR = downtrend),
`at` (close === SAR), `none` (null).

## Bit-exact anchor

- **CONST close = K (K > 0)**: SAR initialises at K with EP =
  K. Each newSAR = K + AF * (K - K) = K, so SAR stays at K
  forever; no reversal triggers. `close - SAR = 0` ->
  `psarPct = 0 / K * 100 = 0` every bar. Verified across K =
  1..1234. K = 0 triggers the divide-by-zero guard -> psarPct
  = null.

## Surface

- Helpers exported: `getLinePsarCrossPctFinitePoints`,
  `normalizeLinePsarCrossPctAf`, `computeLinePsarCrossPct`,
  `classifyLinePsarCrossPctRegime`, `runLinePsarCrossPct`,
  `computeLinePsarCrossPctLayout`,
  `describeLinePsarCrossPctChart`.
- Component: `<ChartLinePsarCrossPct />` (forwardRef on a
  `<div>` wrapping the SVG + legend + badge).
- Data attrs: `data-section="chart-line-psar-cross-pct"`,
  `data-af-step`, `data-af-max`, `data-total-points`,
  `data-above-count`, `data-below-count`, `data-at-count`.
- Accessibility: `role="region"` + `aria-label` +
  `aria-describedby`, inner `<svg role="img">`, sr-only desc,
  motion-safe fade-in.
- Legend: three toggleable buttons (`close`, `psar`,
  `psarPct`); controlled / uncontrolled modes;
  `onSeriesToggle` reports diffs.
- Tooltip: `x`, `close`, `psar`, `psarPct`, regime, plus
  running regime counts.
- Renderer: top panel overlays close + PSAR, bottom panel
  renders the percent deviation with a dashed zero reference.

## Tests (52 cases)

- `getLinePsarCrossPctFinitePoints` (3) -- null, NaN, null
  entries.
- `normalizeLinePsarCrossPctAf` (4) -- zero, negative, above 1,
  accept.
- `computeLinePsarCrossPct` (6) -- null, CONST K > 0 bit-exact,
  CONST K = 0 null, defaults, no mutation, LINEAR UP behavior.
- `classifyLinePsarCrossPctRegime` (4) -- above, below, at,
  none.
- `runLinePsarCrossPct` (7) -- ok flags, defaults, options,
  sort by x, CONST K > 0 regime at, CONST K = 0 regime none.
- `computeLinePsarCrossPctLayout` (5) -- empty, ok=true, panel
  stack, zero in pct axis, price + psar paths.
- `describeLinePsarCrossPctChart` (3) -- empty, mention,
  afStep + afMax.
- `<ChartLinePsarCrossPct />` (19) -- empty placeholder, region
  role, ref forward, data attrs, regime counts, aria desc,
  three legend items, legend toggle, controlled hide, badge,
  zero / axis / grid / legend on/off, className+style, animate
  off, psar+pct paths, price path, defaultHiddenSeries.
- PSAR Pct integration (1) -- CONST K > 0 SAR = K bit-exact
  and psarPct = 0 across K = 1..1234.

## Relationship to chart-line-psar-cross (11.858)

| primitive | bottom panel | use-case |
| --- | --- | --- |
| `chart-line-psar-cross` (11.858) | price-vs-PSAR cross dots | discrete parabolic trend flip events |
| `chart-line-psar-cross-pct` (11.894) | (close - PSAR) / close * 100 magnitude channel | continuous parabolic trend strength scaled to price |

11.858 marks the discrete bullish / bearish flip events when
price crosses PSAR. 11.894 surfaces the continuous percent
distance from PSAR so the trend strength can be compared across
instruments at different price magnitudes.

## Files

- web/src/components/ui/chart-line-psar-cross-pct.tsx (new)
- web/src/components/ui/chart-line-psar-cross-pct.test.tsx (new)
- web/src/components/ui/index.ts (barrel export added)
- package.json + package-lock.json + web/package.json +
  web/package-lock.json bumped to 1.11.912
- CHANGELOG.md prepended

## Verification

- `npx vitest run
  src/components/ui/chart-line-psar-cross-pct.test.tsx` --
  52/52 pass.
- Typecheck: no new errors introduced.
