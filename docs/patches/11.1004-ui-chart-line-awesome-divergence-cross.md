# Patch 11.1004 -- UI: chart-line-awesome-divergence-cross primitive

- Version: v1.11.1022
- Queue id: 11.1004
- Branch: c4/c4-mgr-auto (worker dispatch via auto-w986)
- Worker: auto-w986
- Date: 2026-05-27
- Owner: c4 manager autonomous dispatch (8.28)

## Summary

Add `web/src/components/ui/chart-line-awesome-divergence-cross.tsx`
-- close-only Awesome Oscillator (AO) divergence detector
marking discrete price-versus-AO direction disagreement events
for momentum reversal warning. AO companion to the MACD / RSI
/ CMO / Stochastic / CCI divergence primitives.

## Math anchor (bit-exact)

```
ao[i]   = SMA(close, fast) - SMA(close, slow)
priceUp = close[i] > close[i-window]
aoUp    = ao[i]    > ao[i-window]
five-state regime model with crosses suppressed when prev/cur is 'none'
```

- **CONST close = K**: ao=0 -> aligned-bearish (0 crosses).
- **LINEAR UP close = i**: ao = 14.5 constant (5-vs-34 SMA lag
  difference: fast lag 2, slow lag 16.5, diff 14.5). aoUp =
  false (14.5 === 14.5) -> divergent-bearish (canonical
  bearish divergence, 0 crosses entered from `none`).
- **LINEAR DOWN close = -i**: ao = -14.5 -> aligned-bearish (0
  crosses).

For 60 bars at defaults (fastLength=5, slowLength=34,
divergenceWindow=5): noneCount=38, post-warmup regime count=22.

## API

```ts
interface ChartLineAwesomeDivergenceCrossProps {
  data: { x: number; close: number }[];
  fastLength?: number;        // default 5
  slowLength?: number;        // default 34
  divergenceWindow?: number;  // default 5
}
```

## Tests

57 cases. CONST K=60 -> alignedBearishCount=22. LINEAR UP ->
divergentBearishCount=22 (AO plateaus at 14.5). LINEAR DOWN ->
alignedBearishCount=22. Layout uses symmetric +/-1.1*span
around zero with [-1, 1] fallback.

## Files

- web/src/components/ui/chart-line-awesome-divergence-cross.tsx (new)
- web/src/components/ui/chart-line-awesome-divergence-cross.test.tsx (new)
- web/src/components/ui/index.ts (barrel append)
- package.json + package-lock.json (root + web) 1.11.1021 -> 1.11.1022
- CHANGELOG.md (new entry)
- docs/autonomous-queue-v10.md (11.1004 flipped todo -> done)
