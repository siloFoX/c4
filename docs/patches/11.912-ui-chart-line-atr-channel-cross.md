# Patch 11.912 -- UI: chart-line-atr-channel-cross primitive

## Summary

Added `<ChartLineAtrChannelCross />` -- pure-SVG dual-panel
React/TS primitive that renders the close + upper / middle /
lower ATR Channel (top panel) with bullish / bearish arrow
overlays at every close vs middle line cross trigger, and the
`close - mid` deviation oscillator (bottom panel) with cross
dot markers and a dashed zero baseline. ATR Channel variant of
the volatility-band family that uses an SMA mid (instead of the
Keltner EMA mid) to anchor ATR-band-dependent trend regime
events.

## Algorithm

```
mid[i]    = SMA(close, length)
TR[i]     = i == 0 ? 0 : |close[i] - close[i-1]|   (close-only)
atr[i]    = Wilder smooth of TR over atrLength
upper[i]  = mid + mult * atr
lower[i]  = mid - mult * atr
diff[i]   = close[i] - mid[i]
bullish  : (close - mid) crosses up   (prev <= 0, cur > 0)
bearish  : (close - mid) crosses down (prev >= 0, cur < 0)
```

Defaults `length = 20` (SMA mid window), `atrLength = 10`,
`mult = 2`. The SMA mid is the only structural difference from
the Keltner Channel (which uses EMA). Regime classifier
`bullish` (close > mid), `bearish` (close < mid), `neutral`
(close === mid), `none` (mid null).

## Bit-exact anchor

CONST close = K: SMA(K) = K via the `min === max` short-
circuit; TR = 0 every bar -> ATR = 0; upper = lower = mid =
K; close - mid = 0 every bar. Regime is `neutral`, cross
count = 0. Verified across K = 0, 1, 5, 17, 100, 1234.

LINEAR UP has a clean anchor: rolling SMA lags the
monotonically increasing close, so diff > 0 and regime is
`bullish`.

## API surface

- `<ChartLineAtrChannelCross />` `forwardRef<HTMLDivElement>`
- `getLineAtrChannelCrossFinitePoints(data)`
- `normalizeLineAtrChannelCrossLength(value, fallback)`
- `normalizeLineAtrChannelCrossMult(value, fallback)`
- `applyLineAtrChannelCrossSma(values, length)`
- `applyLineAtrChannelCrossWilder(values, length)`
- `computeLineAtrChannelCross(series, options)`
- `classifyLineAtrChannelCrossRegime(diff)`
- `detectLineAtrChannelCrossCrosses(series, diff)`
- `runLineAtrChannelCross(data, options)`
- `computeLineAtrChannelCrossLayout(opts)`
- `describeLineAtrChannelCrossChart(data, options)`

## Visual

- Two stacked panels, ARPS design system colors via tokens.
- Top: close line + upper / middle / lower ATR Channel envelope
  + bullish/bearish polygon arrows at cross bars.
- Bottom: (close - middle) deviation line + cross dot markers +
  dashed zero baseline.
- Tooltip lists x / close / upper / mid / lower / diff / regime
  / counts / crosses.
- Config badge + togglable legend (close / mid / upper / lower).
- Cross marker is `role="graphics-symbol"` with `tabIndex={0}`.

## Test summary

`web/src/components/ui/chart-line-atr-channel-cross.test.tsx`
-- 57 vitest cases, all pass.

Coverage includes:

- finite-point sanitization
- length + mult normalization
- SMA + Wilder CONST short-circuits
- compute / classify / detect helpers
- bit-exact CONST K (mid=upper=lower=K, diff=0) anchor
- LINEAR UP anchor (diff>0, regime bullish)
- runLineAtrChannelCross defaults, custom args, x-sort
- layout panel stacking + zero line placement
- describe text contents
- component empty / region / ref forwarding
- data-attributes (length / atrLength / mult / counts)
- legend uncontrolled + controlled
- show* toggles (axis / grid / legend / crosses / overlay)
- animate=false class strip
- zero line render
- CONST integration anchor (K in {0, 1, 5, 17, 100, 1234})

## Touched

- `web/src/components/ui/chart-line-atr-channel-cross.tsx`
  (new)
- `web/src/components/ui/chart-line-atr-channel-cross.test.tsx`
  (new)
- `web/src/components/ui/index.ts` (barrel)
- `package.json`, `package-lock.json`, `web/package.json`,
  `web/package-lock.json` (1.11.929 -> 1.11.930)
- `docs/patches/11.912-ui-chart-line-atr-channel-cross.md`
  (new)
- `CHANGELOG.md` (prepend)
- `/root/c4/docs/autonomous-queue-v10.md` row 11.912 -> done
