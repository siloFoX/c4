# 11.1115 -- CHORE: fix 29 source-confirmed tsc strict-type errors in 8 test files (v1.11.1133)

## Scope

Continuation of the test type-clean campaign. This batch covers 8 test
files / 29 errors. Scope is ONLY those 8 files; no non-test source
touched; no `as any` / `ts-ignore`; no test assertion was changed.

## Edits

| File | Count | Edit |
|---|---:|---|
| `ChatView.test.tsx` | 4 | Removed 4 write-only `last*` mock-prop trackers (`lastAppendArgs / lastHeaderProps / lastLogProps / lastComposerProps`) via the same Node-pass removal pattern as 11.1110/11.1114. One follow-up: dropping `lastAppendArgs = args;` left the `args` mock-param unused (TS6133), renamed to `_args` (TS convention for intentional). |
| `WorkerDetail.test.tsx` | 3 | Removed 3 write-only `last*` mock-prop trackers (`lastHeaderProps / lastXtermProps / lastKeysProps`). |
| `MeetingsList.test.tsx` | 4 | `forkSpans[0]!.textContent` x2 (gated by `expect(forkSpans).toHaveLength(1)`); `first!.querySelector(...)` and `second!.querySelector(...)` (each preceded by a `rows = container.querySelectorAll('li')` with a known multi-row fixture). |
| `MeetingsStateActions.test.tsx` | 4 | 4x `btns[N]!.textContent` (gated by `expect(btns).toHaveLength(4)` one line above). |
| `RiskCheckResult.test.tsx` | 3 | 1x `pres[0]!.textContent` (gated by `toBeGreaterThanOrEqual(1)`) + widened `makeResult(over: Partial<CheckResponse> = {})` helper to mapped type `{ [K in keyof CheckResponse]?: CheckResponse[K] \| undefined }` + return `as CheckResponse` so `makeResult({ inspectedSource: undefined })` and `makeResult({ intent: undefined })` accept the explicit-undefined override (same playbook as 11.1111). |
| `SpecialistsList.test.tsx` | 4 | Same `makeSpecialist` helper widening + return cast (the L41 `const B: Specialist = makeSpecialist({ ..., tags: undefined })` literal had the TS2379). 3x `vetoPills[0]!.closest` / `badges[0]!.closest` / `pills[0]!.closest` (each gated by a `length >= 1` / `length` check one line above). |
| `ui/file-input.test.tsx` | 4 | 4x `mock.calls[0]!` (and `[0]!` chained at the deeper position for `onFiles.mock.calls[0]![0]![0]!.name`); each site preceded by `expect(onError).toHaveBeenCalled()` or `expect(onFiles).toHaveBeenCalledTimes(1)`. |
| `hooks/use-table-sort.test.tsx` | 3 | Added explicit type argument to all 7 `useTableSort(KEY, { key: 'total', dir })` declarations: `useTableSort<'total' \| 'worker'>(...)`. The hook is `useTableSort<K extends string = string>(...)` and TS infers K from the literal `{ key: 'total' }`, narrowing K to `'total'`; subsequent `onSortChange('worker', ...)` then fails. Widening K explicitly at the call site keeps the test's runtime semantics identical. |

13 unused trackers removed across ChatView (4) + WorkerDetail (3) via
a single Node pass (decl + every write site).

## Verification (mandated)

- `tsc --noEmit` errors in the 8 files: **29 -> 0**.
- Project-wide tsc total: **899 -> 870** (delta exactly **-29**,
  matches "at least -29"). Diff confirms **zero new errors anywhere**.
- `vitest run` on the 8 test files: **8/8 files, 215/215 tests pass**.
  No NEW failures introduced (no pre-existing failures in this batch
  either -- all 8 files were already green at the test level despite
  the tsc errors).

## Risk

- Pure type-level edits + dead-code removal of write-only mock
  trackers. The `useTableSort<'total' | 'worker'>` widening is a
  type-only change; runtime is identical.
- Each `!` is gated by a length / `toHaveBeenCalled` expectation a
  line or two above. The TS6133 removals drop truly-dead writes (the
  variables were never read).
- The two helper widenings (`makeResult` / `makeSpecialist`) preserve
  runtime semantics -- the spread still wins over defaults; the cast
  just lets tsc accept an explicit-undefined override.
