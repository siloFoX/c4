# Patch 11.858 -- UI: chart-line-psar-cross primitive

**Version:** v1.11.876
**Queue TODO:** 11.858
**Branch:** c4/c4-mgr-auto
**Status:** done

## Summary

Added `<ChartLinePsarCross />` -- a pure-SVG, dual-panel React/TS
primitive that overlays Wilder's Parabolic SAR dots on the close
in the top panel and renders the `close - SAR` deviation in the
bottom panel. Markers fire at every SAR flip -- the canonical
"stop and reverse" event.

Cross events:

- `flip-up`   -- trend newly turned up (SAR moved below price)
- `flip-down` -- trend newly turned down (SAR moved above price)

Defaults: `afInitial = 0.02`, `afStep = 0.02`, `afMax = 0.2`
(Wilder's classic), upSarColor `#16a34a`, downSarColor `#dc2626`,
deviationColor `#0ea5e9`, flipUpColor green, flipDownColor red,
zero line `#475569`.

## Algorithm (Wilder's classic Parabolic SAR)

1. Initial trend = `up`, `SAR[0] = low[0]`, `EP = high[0]`,
   `AF = afInitial`.
2. For each subsequent bar i:
   - `next_sar = SAR[i-1] + AF * (EP - SAR[i-1])`.
   - In uptrend, clamp `next_sar <= min(low[i-1], low[i-2])`.
   - In downtrend, clamp `next_sar >= max(high[i-1], high[i-2])`.
   - If uptrend and `low[i] < next_sar` -> flip to down:
     `SAR[i] = EP`, `EP = low[i]`, `AF = afInitial`.
   - Else if downtrend and `high[i] > next_sar` -> flip to up:
     `SAR[i] = EP`, `EP = high[i]`, `AF = afInitial`.
   - Else (no flip): `SAR[i] = next_sar`.
     In uptrend if `high[i] > EP` -> `EP = high[i]`,
     `AF = min(AF + afStep, afMax)`.
     In downtrend if `low[i] < EP` -> `EP = low[i]`,
     `AF = min(AF + afStep, afMax)`.

## Bit-exact anchors

- **CONST h = l = close = K**: `SAR[0] = K`, `EP = K`. Each bar's
  `next_sar = K + AF*(K - K) = K`, the clamp keeps it at K, and
  the flip checks (`low < SAR` / `high > SAR`) are false because
  both sides equal K. SAR stays K bit-exactly, trend stays `up`,
  deviation = 0, zero crosses.
- **LINEAR UP h = l = close = i + 1**: starts in uptrend with
  `SAR = 1`, `EP = 1`. The first three bars keep SAR pinned at 1
  by the previous-low cap; from bar 3 onward the cap (= 2) lets
  SAR climb above 1, but the rising close stays well above SAR so
  no flip fires. Zero `flip-down`s, trend stays `up` throughout.
- **LINEAR DOWN h = l = close = N - i**: starts in uptrend with
  SAR = N. At bar 1 the new low (= N-1) is strictly less than
  SAR -> flip to downtrend, exactly one `flip-down` event fires.
  After that bar the downtrend cap (= max prior highs) keeps SAR
  above the dropping high; SAR stays above price; no further
  flips. Verified across multiple `afInitial` values.

## Surface

- Helpers exported: `getLinePsarCrossFinitePoints`,
  `normalizeLinePsarCrossAcceleration`, `computeLinePsarCross`,
  `classifyLinePsarCrossRelation`, `detectLinePsarCrossFlips`,
  `runLinePsarCross`, `computeLinePsarCrossLayout`,
  `describeLinePsarCrossChart`.
- Component: `<ChartLinePsarCross />` (forwardRef on a `<div>`
  wrapping the SVG + legend + badge).
- Data attrs: `data-section="chart-line-psar-cross"`,
  `data-af-initial`, `data-af-step`, `data-af-max`,
  `data-total-points`, `data-flip-up-count`,
  `data-flip-down-count`.
- Accessibility: `role="region"` + `aria-label` + `aria-describedby`,
  inner `<svg role="img">`, sr-only description span,
  `role="graphics-symbol"` + `tabIndex={0}` on each cross marker,
  motion-safe fade-in.
- Legend: three toggleable buttons (`close`, `sar`, `deviation`);
  controlled / uncontrolled modes; `onSeriesToggle` diffs.
- Tooltip: `x`, `close`, `sar`, `deviation`, `trend`, `relation`,
  `cross`, `af`, `ep`, plus running flip-up / flip-down counts.
- Renderer: top panel overlays close + SAR dots (green dots when
  trend is `up`, red dots when `down`). Bottom panel renders the
  `close - SAR` deviation with a dashed zero reference.

## Tests (68 cases)

- `getLinePsarCrossFinitePoints` (3) -- null, NaN, null entries.
- `normalizeLinePsarCrossAcceleration` (3) -- default, out-of-
  range, accept.
- `computeLinePsarCross` (8) -- null input, CONST SAR=K and
  trend=up, LINEAR UP seed SAR=1/EP=1, LINEAR UP trend stays up
  (SAR stays below rising close), LINEAR DOWN flips at bar 1,
  LINEAR DOWN trend stays down, length parity, no mutation.
- `classifyLinePsarCrossRelation` (4) -- bullish, bearish, equal,
  none.
- `detectLinePsarCrossFlips` (5) -- flip-up, flip-down, same
  trend null, first bar null, none state.
- `runLinePsarCross` (12) -- ok flags, defaults, options, sort by
  x, three anchors (CONST 0 flips, LINEAR UP 0 flips, LINEAR DOWN
  1 flip-down at bar 1), regime checks for each anchor.
- `computeLinePsarCrossLayout` (7) -- empty, ok=true, panel stack,
  price path + dots, SAR dots, deviation path, marker counts on
  LINEAR DOWN (1) and LINEAR UP (0).
- `describeLinePsarCrossChart` (3) -- empty, mention, params.
- `<ChartLinePsarCross />` (22) -- empty placeholder, region role,
  ref forward, data attrs (af params + flip counts), aria
  description, three legend items, legend toggle, controlled
  hide, badge, zero/axis/grid/legend on/off, className+style,
  animate off, sar dots + deviation + price paths, flip-down
  marker rendering, defaultHiddenSeries.
- PSAR Cross integration (2) -- CONST and LINEAR UP zero flips
  across multiple `afInitial` values; LINEAR DOWN always fires
  exactly 1 flip-down.

## Files

- web/src/components/ui/chart-line-psar-cross.tsx (new)
- web/src/components/ui/chart-line-psar-cross.test.tsx (new)
- web/src/components/ui/index.ts (barrel export added)
- package.json + package-lock.json + web/package.json +
  web/package-lock.json bumped to 1.11.876
- CHANGELOG.md prepended

## Verification

- `npx vitest run src/components/ui/chart-line-psar-cross.test.tsx`
  -- 68/68 pass.
- Typecheck: no new errors introduced (pre-existing errors in
  unrelated components remain).
