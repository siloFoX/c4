# Patch 11.910 -- UI: chart-line-williams-r-cross primitive

## Summary

Added `<ChartLineWilliamsRCross />` -- pure-SVG dual-panel
React/TS primitive that renders the close (top panel) with
bullish / bearish arrow overlays at every Williams %R vs EMA-
smoothed signal cross, and the close-only Williams %R line
alongside its signal (bottom panel) on a fixed -100..0
oscillator with cross dot markers and -80 / -50 / -20
reference bands. Signal-cross variant of the Williams %R family
that flags overbought / oversold regime transition trigger
events distinct from the canonical -80 / -20 line readings.

## Algorithm

```
hi[i]      = max(close[i-n+1..i])
lo[i]      = min(close[i-n+1..i])
range[i]   = hi - lo
wr[i]      = range > 0
              ? ((hi - close) / range) * -100
              : -50  (neutral fallback when range collapses)
signal[i]  = EMA(wr, signalLength)
bullish   : (wr - signal) crosses up   (prev <= 0, cur > 0)
bearish   : (wr - signal) crosses down (prev >= 0, cur < 0)
```

Defaults `length = 14` (canonical Williams %R window),
`signalLength = 3`. Regime classifier `bullish` (wr > signal),
`bearish` (wr < signal), `neutral` (wr === signal), `none`
(either null). Range -100..0 (overbought at -20, oversold at
-80, mid at -50).

## Bit-exact anchor

CONST close = K: range = hi - lo = 0 every bar -> wr = -50
neutral fallback. signal EMA of -50s = -50 via the
`min === max` precision short-circuit. wr === signal -> regime
`neutral`, cross count = 0. Verified across K = 0, 1, 5, 17,
100, 1234.

LINEAR UP has bit-exact anchor: every settled bar has hi =
close, so wr = ((close - close) / range) * -100 = 0. LINEAR
DOWN mirror: lowest = close, so wr = ((hi - close) / range)
* -100 = -100.

## API surface

- `<ChartLineWilliamsRCross />` `forwardRef<HTMLDivElement>`
- `getLineWilliamsRCrossFinitePoints(data)`
- `normalizeLineWilliamsRCrossLength(value, fallback)`
- `applyLineWilliamsRCrossEma(values, length)`
- `computeLineWilliamsRCross(series, options)`
- `classifyLineWilliamsRCrossRegime(wr, signal)`
- `detectLineWilliamsRCrossCrosses(series, wr, sig)`
- `runLineWilliamsRCross(data, options)`
- `computeLineWilliamsRCrossLayout(opts)`
- `describeLineWilliamsRCrossChart(data, options)`

## Visual

- Two stacked panels with the osc panel pinned to -100..0.
- Top: close line + bullish/bearish polygon arrows at cross
  bars.
- Bottom: Williams %R + signal lines + cross dot markers +
  -80 / -50 / -20 reference bands.
- Tooltip lists x / close / %R / signal / regime / counts /
  crosses.
- Config badge + togglable legend (close / %R / signal).
- Cross marker is `role="graphics-symbol"` with `tabIndex={0}`.

## Test summary

`web/src/components/ui/chart-line-williams-r-cross.test.tsx`
-- 57 vitest cases, all pass.

Coverage includes:

- finite-point sanitization
- length normalization
- EMA CONST short-circuit
- compute / classify / detect helpers
- bit-exact CONST K (wr=signal=-50) anchor
- LINEAR UP (wr=0), LINEAR DOWN (wr=-100) anchors
- runLineWilliamsRCross defaults, custom lengths, x-sort
- layout panel stacking + osc range -100..0
- describe text contents
- component empty / region / ref forwarding
- data-attributes (lengths / counts)
- legend uncontrolled + controlled
- show* toggles (axis / grid / bands / legend / crosses /
  overlay)
- animate=false class strip
- CONST + LINEAR integration anchors (K in {0, 1, 5, 17, 100,
  1234})

## Touched

- `web/src/components/ui/chart-line-williams-r-cross.tsx`
  (new)
- `web/src/components/ui/chart-line-williams-r-cross.test.tsx`
  (new)
- `web/src/components/ui/index.ts` (barrel)
- `package.json`, `package-lock.json`, `web/package.json`,
  `web/package-lock.json` (1.11.927 -> 1.11.928)
- `docs/patches/11.910-ui-chart-line-williams-r-cross.md`
  (new)
- `CHANGELOG.md` (prepend)
- `/root/c4/docs/autonomous-queue-v10.md` row 11.910 -> done
