# Patch 11.879 -- UI: chart-line-mfi-cross-pct primitive

**Version:** v1.11.897
**Queue TODO:** 11.879
**Branch:** c4/c4-mgr-auto
**Status:** done

## Summary

Added `<ChartLineMfiCrossPct />` -- a pure-SVG, dual-panel
React/TS primitive that renders the close in the top panel and
overlays the Money Flow Index (MFI), its EMA-smoothed signal,
and the `mfiPct = (MFI - signal) / 100 * 100` deviation channel
in the bottom panel. MFI is a volume-weighted RSI, so the
percent treatment surfaces the *volume-weighted momentum
magnitude* (how stretched MFI is above or below its signal,
scaled by the 0..100 MFI range) separate from the sign-based
MFI-over-signal crossings:

```
tp[i]     = close[i]                       (single-series TP)
rawMF[i]  = tp[i] * volume[i]
pos[i]    = tp[i] > tp[i-1] ? rawMF[i] : 0
neg[i]    = tp[i] < tp[i-1] ? rawMF[i] : 0
PSum[i]   = sum(pos[i-length+1 .. i])
NSum[i]   = sum(neg[i-length+1 .. i])
MFI[i]    = (PSum===0 && NSum===0) ? 50
          : NSum===0 ? 100
          : PSum===0 ? 0
          : 100 - 100 / (1 + PSum / NSum)
signal[i] = EMA(MFI, signalLength)
mfiPct[i] = (MFI[i] - signal[i]) / 100 * 100
```

Defaults: `length = 14` (canonical MFI), `signalLength = 9`.
Regime classifier: `above` (mfiPct > 0), `below` (< 0),
`at` (= 0), `none` (null).

## Bit-exact anchors (three)

- **CONST {close = K, volume = V}**: tp[i] === tp[i-1] every
  bar so pos = neg = 0 throughout the window. PSum = NSum = 0
  -> MFI = 50 every bar. Signal EMA of 50s = 50. mfiPct = 0.
  Verified across multiple K and V tuples.
- **LINEAR UP step > 0 with V > 0**: tp[i] > tp[i-1] every bar
  so only pos counts. NSum = 0 -> MFI = 100 every bar. Signal
  EMA of 100s = 100. mfiPct = 0.
- **LINEAR DOWN step < 0 with V > 0**: tp[i] < tp[i-1] every
  bar so only neg counts. PSum = 0 -> MFI = 0 every bar.
  Signal EMA of 0s = 0. mfiPct = 0.

All three anchors hit `mfiPct = 0` bit-exact.

## Surface

- Helpers exported: `getLineMfiCrossPctFinitePoints`,
  `normalizeLineMfiCrossPctLength`,
  `applyLineMfiCrossPctEma`, `computeLineMfiCrossPct`,
  `classifyLineMfiCrossPctRegime`, `runLineMfiCrossPct`,
  `computeLineMfiCrossPctLayout`,
  `describeLineMfiCrossPctChart`.
- Component: `<ChartLineMfiCrossPct />` (forwardRef on a
  `<div>` wrapping the SVG + legend + badge).
- Data attrs: `data-section="chart-line-mfi-cross-pct"`,
  `data-length`, `data-signal-length`, `data-total-points`,
  `data-above-count`, `data-below-count`, `data-at-count`.
- Accessibility: `role="region"` + `aria-label` +
  `aria-describedby`, inner `<svg role="img">`, sr-only desc,
  motion-safe fade-in.
- Legend: four toggleable buttons (`close`, `mfi`, `signal`,
  `mfiPct`); controlled / uncontrolled modes; `onSeriesToggle`
  reports diffs.
- Tooltip: `x`, `close`, `mfi`, `signal`, `mfiPct`, regime,
  plus running regime counts.
- Renderer: top panel renders the close, bottom panel overlays
  MFI, signal, and mfiPct over a dashed zero reference.

## Tests (50 cases)

- `getLineMfiCrossPctFinitePoints` (3) -- null, NaN, null
  entries.
- `normalizeLineMfiCrossPctLength` (3) -- default, below-1,
  accept.
- `applyLineMfiCrossPctEma` (1) -- CONST bit-exact across K.
- `computeLineMfiCrossPct` (6) -- null, CONST -> 50/50/0,
  LINEAR UP -> 100/100/0, LINEAR DOWN -> 0/0/0, defaults, no
  mutation.
- `classifyLineMfiCrossPctRegime` (4) -- above, below, at,
  none.
- `runLineMfiCrossPct` (6) -- ok flags, defaults, options,
  sort by x, CONST regime at.
- `computeLineMfiCrossPctLayout` (5) -- empty, ok=true, panel
  stack, zero in osc axis, price + mfi paths.
- `describeLineMfiCrossPctChart` (3) -- empty, mention, both
  lengths.
- `<ChartLineMfiCrossPct />` (18) -- empty placeholder, region
  role, ref forward, data attrs, aria desc, four legend items,
  legend toggle, controlled hide, badge, zero / axis / grid /
  legend on/off, className+style, animate off, mfi+signal+pct
  paths, price path, defaultHiddenSeries.
- MFI Cross Pct integration (1) -- three bit-exact anchors
  verified across multiple K, V, and start values.

## Relationship to chart-line-rsi-cross-pct (11.878)

| primitive | momentum input | signal weighting |
| --- | --- | --- |
| `chart-line-rsi-cross-pct` (11.878) | RSI (price-only) | none -- pure price momentum |
| `chart-line-mfi-cross-pct` (11.879) | MFI (volume-weighted RSI) | volume amplifies the deviation |

MFI is the volume-weighted analogue of RSI, so this primitive
follows the same percent-of-range framing as 11.878 but adds the
volume multiplier so bars with large volume contribute more to
the momentum reading.

## Files

- web/src/components/ui/chart-line-mfi-cross-pct.tsx (new)
- web/src/components/ui/chart-line-mfi-cross-pct.test.tsx (new)
- web/src/components/ui/index.ts (barrel export added)
- package.json + package-lock.json + web/package.json +
  web/package-lock.json bumped to 1.11.897
- CHANGELOG.md prepended

## Verification

- `npx vitest run
  src/components/ui/chart-line-mfi-cross-pct.test.tsx` -- 50/50
  pass.
- Typecheck: no new errors introduced.
