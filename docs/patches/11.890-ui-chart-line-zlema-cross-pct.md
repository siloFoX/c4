# Patch 11.890 -- UI: chart-line-zlema-cross-pct primitive

**Version:** v1.11.908
**Queue TODO:** 11.890
**Branch:** c4/c4-mgr-auto
**Status:** done

## Summary

Added `<ChartLineZlemaCrossPct />` -- a pure-SVG, dual-panel
React/TS primitive that overlays the close with the Zero Lag
EMA (ZLEMA) in the top panel and renders the `close - ZLEMA`
deviation scaled to the close as a percent in the bottom panel.
The percent normalisation makes the lag-corrected ZLEMA
momentum comparable across instruments at different price
magnitudes:

```
lag         = floor((length - 1) / 2)
zlemaIn[i]  = i >= lag
                ? close[i] + (close[i] - close[i - lag])
                : null
ZLEMA[i]    = EMA(zlemaIn, length)
zlemaPct[i] = close[i] === 0
                ? null
                : (close[i] - ZLEMA[i]) / close[i] * 100
```

Defaults: `length = 14`. Regime classifier: `above` (zlemaPct >
0), `below` (< 0), `at` (= 0), `none` (null).

## Bit-exact anchor

- **CONST close = K (K > 0)**: every `close - close[i-lag] = K
  - K = 0`, so `zlemaIn[i] = K`. EMA(K, length) collapses to K
  via the SMA-seeded `min === max` precision fix.
  `close - ZLEMA = 0`, `zlemaPct = 0 / K * 100 = 0` every bar
  after warmup. K = 0 triggers the divide-by-zero guard ->
  zlemaPct = null. Verified across K = 1..1234 and length =
  9..21.

## Surface

- Helpers exported: `getLineZlemaCrossPctFinitePoints`,
  `normalizeLineZlemaCrossPctLength`,
  `applyLineZlemaCrossPctEma`, `computeLineZlemaCrossPct`,
  `classifyLineZlemaCrossPctRegime`, `runLineZlemaCrossPct`,
  `computeLineZlemaCrossPctLayout`,
  `describeLineZlemaCrossPctChart`.
- Component: `<ChartLineZlemaCrossPct />` (forwardRef on a
  `<div>` wrapping the SVG + legend + badge).
- Data attrs: `data-section="chart-line-zlema-cross-pct"`,
  `data-length`, `data-lag`, `data-total-points`,
  `data-above-count`, `data-below-count`, `data-at-count`.
- Accessibility: `role="region"` + `aria-label` +
  `aria-describedby`, inner `<svg role="img">`, sr-only desc,
  motion-safe fade-in.
- Legend: three toggleable buttons (`close`, `zlema`,
  `zlemaPct`); controlled / uncontrolled modes;
  `onSeriesToggle` reports diffs.
- Tooltip: `x`, `close`, `zlema`, `zlemaPct`, regime, plus
  running regime counts.
- Renderer: top panel overlays close + ZLEMA, bottom panel
  renders the percent deviation with a dashed zero reference.

## Tests (52 cases)

- `getLineZlemaCrossPctFinitePoints` (3) -- null, NaN, null
  entries.
- `normalizeLineZlemaCrossPctLength` (3) -- default, below-2,
  accept.
- `applyLineZlemaCrossPctEma` (1) -- CONST bit-exact across K.
- `computeLineZlemaCrossPct` (6) -- null, CONST K > 0 bit-
  exact, CONST K = 0 null, defaults, no mutation, lag formula.
- `classifyLineZlemaCrossPctRegime` (4) -- above, below, at,
  none.
- `runLineZlemaCrossPct` (7) -- ok flags, defaults, options,
  sort by x, CONST K > 0 regime at, CONST K = 0 regime none.
- `computeLineZlemaCrossPctLayout` (5) -- empty, ok=true, panel
  stack, zero in pct axis, price + zlema paths.
- `describeLineZlemaCrossPctChart` (3) -- empty, mention,
  length.
- `<ChartLineZlemaCrossPct />` (19) -- empty placeholder,
  region role, ref forward, data attrs, regime counts, aria
  desc, three legend items, legend toggle, controlled hide,
  badge, zero / axis / grid / legend on/off, className+style,
  animate off, zlema+pct paths, price path, defaultHiddenSeries.
- ZLEMA Pct integration (1) -- CONST K > 0 ZLEMA = K bit-exact
  and zlemaPct = 0 across multiple K and length tuples.

## Relationship to chart-line-zlema-cross (11.865)

| primitive | bottom panel | use-case |
| --- | --- | --- |
| `chart-line-zlema-cross` (11.865) | price-over-ZLEMA cross dots | discrete trend flip triggers |
| `chart-line-zlema-cross-pct` (11.890) | (close - ZLEMA) / close * 100 magnitude channel | continuous trend-momentum magnitude scaled to price |

11.865 marks discrete bullish / bearish crossovers; 11.890
surfaces the continuous percent deviation of close from the
lag-corrected ZLEMA trend so the momentum strength can be
compared across instruments at different price magnitudes.

## Files

- web/src/components/ui/chart-line-zlema-cross-pct.tsx (new)
- web/src/components/ui/chart-line-zlema-cross-pct.test.tsx
  (new)
- web/src/components/ui/index.ts (barrel export added)
- package.json + package-lock.json + web/package.json +
  web/package-lock.json bumped to 1.11.908
- CHANGELOG.md prepended

## Verification

- `npx vitest run
  src/components/ui/chart-line-zlema-cross-pct.test.tsx` --
  52/52 pass.
- Typecheck: no new errors introduced.
