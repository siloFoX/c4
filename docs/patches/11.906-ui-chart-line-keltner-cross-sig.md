# Patch 11.906 -- UI: chart-line-keltner-cross-sig primitive

## Summary

Added `<ChartLineKeltnerCrossSig />` -- pure-SVG dual-panel
React/TS primitive that renders the close + upper / middle /
lower Keltner Channel (top panel) with bullish / bearish arrow
overlays at every close vs middle line cross trigger, and a
`close - middle` deviation oscillator (bottom panel) with cross
dot markers and a dashed zero baseline. Signal-cross variant of
the Keltner Channel family that flags adaptive volatility
regime trigger events distinct from envelope touches.

## Algorithm

```
mid[i]    = EMA(close, length)
TR[i]     = i == 0 ? 0 : |close[i] - close[i-1]|   (close-only)
atr[i]    = Wilder smooth of TR over atrLength
upper[i]  = mid + mult * atr
lower[i]  = mid - mult * atr
diff[i]   = close[i] - mid[i]
bullish  : (close - mid) crosses up   (prev <= 0, cur > 0)
bearish  : (close - mid) crosses down (prev >= 0, cur < 0)
```

Defaults `length = 20` (EMA window), `atrLength = 10`,
`mult = 2`. Regime classifier `bullish` (close > mid),
`bearish` (close < mid), `neutral` (close === mid),
`none` (mid null).

## Bit-exact anchor

CONST close = K: EMA(K) collapses to K via the `min === max`
precision short-circuit; TR = 0 every bar -> ATR = 0; upper =
lower = mid = K; close - mid = 0 every bar. Regime is
`neutral`, cross count = 0. Verified across K = 0, 1, 5, 17,
100, 1234.

LINEAR UP has a clean anchor: rolling EMA lags monotonic
close, so diff > 0 and regime is `bullish`.

## API surface

- `<ChartLineKeltnerCrossSig />` `forwardRef<HTMLDivElement>`
- `getLineKeltnerCrossSigFinitePoints(data)`
- `normalizeLineKeltnerCrossSigLength(value, fallback)`
- `normalizeLineKeltnerCrossSigMult(value, fallback)`
- `applyLineKeltnerCrossSigEma(values, length)`
- `applyLineKeltnerCrossSigWilder(values, length)`
- `computeLineKeltnerCrossSig(series, options)`
- `classifyLineKeltnerCrossSigRegime(diff)`
- `detectLineKeltnerCrossSigCrosses(series, diff)`
- `runLineKeltnerCrossSig(data, options)`
- `computeLineKeltnerCrossSigLayout(opts)`
- `describeLineKeltnerCrossSigChart(data, options)`

## Visual

- Two stacked panels, ARPS design system colors via tokens.
- Top: close line + upper / middle / lower Keltner envelope +
  bullish/bearish polygon arrows at cross bars.
- Bottom: (close - middle) deviation line + cross dot markers +
  dashed zero baseline.
- Tooltip lists x / close / upper / mid / lower / diff / regime
  / counts / crosses.
- Config badge + togglable legend (close / mid / upper / lower).
- Cross marker is `role="graphics-symbol"` with `tabIndex={0}`.

## Test summary

`web/src/components/ui/chart-line-keltner-cross-sig.test.tsx`
-- 57 vitest cases, all pass.

Coverage includes:

- finite-point sanitization
- length + mult normalization
- Wilder + EMA CONST short-circuit
- compute / classify / detect helpers
- bit-exact CONST K (diff=0, mid=upper=lower=K) anchor
- LINEAR UP anchor (diff>0, regime bullish)
- runLineKeltnerCrossSig defaults, custom args, x-sort
- layout panel stacking + zero line placement
- describe text contents
- component empty / region / ref forwarding
- data-attributes (length / atrLength / mult / counts)
- legend uncontrolled + controlled
- show* toggles (axis / grid / legend / crosses / overlay)
- animate=false class strip
- zero line render
- CONST integration anchor (K in {0, 1, 5, 17, 100, 1234})

## Touched

- `web/src/components/ui/chart-line-keltner-cross-sig.tsx`
  (new)
- `web/src/components/ui/chart-line-keltner-cross-sig.test.tsx`
  (new)
- `web/src/components/ui/index.ts` (barrel)
- `package.json`, `package-lock.json`, `web/package.json`,
  `web/package-lock.json` (1.11.923 -> 1.11.924)
- `docs/patches/11.906-ui-chart-line-keltner-cross-sig.md`
  (new)
- `CHANGELOG.md` (prepend)
- `/root/c4/docs/autonomous-queue-v10.md` row 11.906 -> done
