# 11.1114 -- CHORE: fix 29 source-confirmed tsc strict-type errors in 6 src/components test files (v1.11.1132)

## Scope

Continuation of the test type-clean campaign. This batch covers 6
src/components test files / 29 errors. Scope is ONLY those 6 files;
no non-test source touched; no `as any` / `ts-ignore`; no test
assertion was changed.

## Edits

| File | Count | Edit |
|---|---:|---|
| `RiskSandboxPreview.test.tsx` | 5 | 4x `pres[0].textContent` -> `pres[0]!.textContent` (each gated by `expect(pres.length).toBeGreaterThanOrEqual(1)` one line above). Plus L184 destructure `const { container } = render(...)` was unused -- dropped the destructure (the test uses `screen.*` not `container`). |
| `SessionsView.test.tsx` | 5 | Removed 5 write-only `last*` mock-prop trackers (`lastListCardProps / lastRightPaneProps / lastAttachModalProps / lastNewChatProps / lastTourProps`). Same playbook as 11.1110: TS6133 because the variable is declared + written in mock bodies + reset in beforeEach but never read; tests assert via `data-testid` on the mock JSX, not in-memory captures. Removed declarations + every write site. |
| `SpecialistsEnrichmentPanels.test.tsx` | 6 | 2 TS2379 (`actor: undefined` / `reason: undefined`) fixed by widening the helper `makeAudit(over: Partial<AuditEntry> = {})` -> `over: { [K in keyof AuditEntry]?: AuditEntry[K] \| undefined }` and asserting the spread-merged return `as AuditEntry` (same playbook as 11.1111). 4 TS2345 (HTMLElement \| undefined) -> non-null on `within(items[N])` (gated by the array's construction with two/three known elements). |
| `WikiBulkPublishRow.test.tsx` | 5 | 5x `user.click(checkboxes[N])` -> `user.click(checkboxes[N]!)`. `getAllByRole('checkbox')` returns a `HTMLElement[]`; the index access becomes `HTMLElement \| undefined` under `noUncheckedIndexedAccess`. The component always renders 2 checkboxes (commit + push), so indices 0 and 1 are guaranteed. |
| `WikiView.test.tsx` | 4 | Removed 4 write-only `last*` mock-prop trackers (`lastHeaderProps / lastResultsProps / lastDetailHeaderProps / lastDetailProps`). Same TS6133 + same removal pattern. |
| `WorkflowEditor.test.tsx` | 4 | Removed 4 write-only `last*` mock-prop trackers (`lastListCardProps / lastHeaderProps / lastGraphProps / lastRunsPanelProps`). Same pattern. |

13 unused trackers removed across the three sister files (5 + 4 + 4)
via a single mechanical Node pass that drops the top-level
`let lastX...` declaration AND every in-mock-body / beforeEach
assignment in one shot.

## Verification (mandated)

- `tsc --noEmit` errors in the 6 files: **29 -> 0**.
- Project-wide tsc total: **928 -> 899** (delta exactly **-29**,
  matches "at least -29"). Diff confirms **zero new errors anywhere**.
- `vitest run` on the 6 test files: **5/6 files, 155/156 tests pass**.
  The 1 failing test (`WikiBulkPublishRow.test.tsx:248 -> wraps the
  row in a flex container with the border-t and pt-2 classes`) is a
  class-name assertion at L248, not one of the lines I touched
  (mine are at L119/129/138/148/209). Verified on pristine main
  (`git checkout` + targeted `-t` run): `1 failed | 26 skipped` --
  same passing-test count as before, no regression introduced.

## Risk

- Pure type-level edits plus dead-code removal of write-only mock
  trackers.
- Each `!` assertion is gated by a length / membership expectation a
  line or two above (or by the component contract -- 2 checkboxes
  always rendered). The TS6133 removals drop truly-dead writes (the
  variable was never read).
- The `makeAudit` widening preserves runtime semantics: the spread
  still wins, the cast just lets tsc accept the spread when an
  override sets a required field to undefined (the intentional
  test-the-undefined-path case).
