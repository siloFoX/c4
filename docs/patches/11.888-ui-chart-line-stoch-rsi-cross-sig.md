# Patch 11.888 -- UI: chart-line-stoch-rsi-cross-sig primitive

**Version:** v1.11.906
**Queue TODO:** 11.888
**Branch:** c4/c4-mgr-auto
**Status:** done

## Summary

Added `<ChartLineStochRsiCrossSig />` -- a pure-SVG, dual-panel
React/TS primitive that renders the close in the top panel and
overlays the Stochastic RSI %K with its EMA-smoothed signal in
the bottom panel, marking bullish / bearish cross trigger
events. The trigger-focused StochRSI variant distinct from
11.857 chart-line-stoch-rsi-cross (which marks K-over-D
crossings): this primitive replaces the %D line with an EMA-
smoothed signal so the actionable momentum oscillator trigger
events line up with the broader cross-sig family.

```
RSI[i]       = Wilder RSI(close, rsiLength)
                -- midpoint = 50 when both avgGain and avgLoss
                   are zero
highest[i]   = max(RSI[i - stochLength + 1 .. i])
lowest[i]    = min(RSI[i - stochLength + 1 .. i])
stochRSI[i]  = highest === lowest
                 ? 50
                 : (RSI[i] - lowest) / (highest - lowest) * 100
K[i]         = SMA(stochRSI, slowKLength)
signal[i]    = EMA(K, signalLength)
bullish     : (K - signal) crosses up    (prev <= 0, cur > 0)
bearish     : (K - signal) crosses down  (prev >= 0, cur < 0)
```

Defaults: `rsiLength = 14`, `stochLength = 14`, `slowKLength =
3`, `signalLength = 9`. Regime classifier: `bullish` (K >
signal), `bearish` (K < signal), `neutral` (K === signal),
`none` (either side null).

## Bit-exact anchors (three)

- **CONST close = K (any K)**: RSI = 50 every bar -> highest
  === lowest = 50 -> stochRSI = 50 via the degenerate-range
  midpoint fallback -> K = SMA(50) = 50 -> signal EMA of 50s =
  50. K === signal -> regime `neutral`, cross count = 0.
- **LINEAR UP step > 0**: RSI = 100 every bar -> degenerate
  range -> stochRSI = 50 -> K = 50 -> signal = 50 -> regime
  `neutral`, cross count = 0.
- **LINEAR DOWN step < 0**: RSI = 0 every bar -> degenerate
  range -> stochRSI = 50 -> K = 50 -> signal = 50 -> regime
  `neutral`, cross count = 0.

All three anchors verified across multiple K and start values.

## Surface

- Helpers exported: `getLineStochRsiCrossSigFinitePoints`,
  `normalizeLineStochRsiCrossSigLength`,
  `applyLineStochRsiCrossSigWilder`,
  `applyLineStochRsiCrossSigSma`,
  `applyLineStochRsiCrossSigEma`,
  `computeLineStochRsiCrossSig`,
  `classifyLineStochRsiCrossSigRegime`,
  `detectLineStochRsiCrossSigCrosses`,
  `runLineStochRsiCrossSig`,
  `computeLineStochRsiCrossSigLayout`,
  `describeLineStochRsiCrossSigChart`.
- Component: `<ChartLineStochRsiCrossSig />` (forwardRef on a
  `<div>` wrapping the SVG + legend + badge).
- Data attrs: `data-section="chart-line-stoch-rsi-cross-sig"`,
  `data-rsi-length`, `data-stoch-length`, `data-slow-k-length`,
  `data-signal-length`, `data-total-points`,
  `data-bullish-count`, `data-bearish-count`,
  `data-neutral-count`, `data-cross-count`.
- Accessibility: `role="region"` + `aria-label` +
  `aria-describedby`, inner `<svg role="img">`, sr-only desc,
  motion-safe fade-in; oscillator cross markers carry
  `role="graphics-symbol"` + `tabIndex={0}` + descriptive
  `aria-label`. Overlay arrow markers on the price panel carry
  the same attributes for direct charting-overlay use.
- Legend: three toggleable buttons (`close`, `k`, `signal`);
  controlled / uncontrolled modes; `onSeriesToggle` reports
  diffs.
- Tooltip: `x`, `close`, `k`, `signal`, regime, running regime
  counts, and total cross count.
- Renderer: top panel renders the close with bullish / bearish
  arrow overlays at every cross trigger; bottom panel overlays
  K with the signal line, dashed zero reference, and dot
  markers at the same cross indices.

## Tests (58 cases)

- `getLineStochRsiCrossSigFinitePoints` (3) -- null, NaN, null
  entries.
- `normalizeLineStochRsiCrossSigLength` (3) -- default,
  below-1, accept.
- `applyLineStochRsiCrossSigWilder` (1) -- CONST bit-exact
  across K.
- `applyLineStochRsiCrossSigSma` (1) -- CONST bit-exact across
  K.
- `applyLineStochRsiCrossSigEma` (1) -- CONST bit-exact across
  K.
- `computeLineStochRsiCrossSig` (6) -- null, CONST -> 50/50,
  LINEAR UP -> 50/50, LINEAR DOWN -> 50/50, defaults, no
  mutation.
- `classifyLineStochRsiCrossSigRegime` (4) -- bullish,
  bearish, neutral, none.
- `detectLineStochRsiCrossSigCrosses` (3) -- bullish, bearish,
  skip null.
- `runLineStochRsiCrossSig` (6) -- ok flags, defaults, options,
  sort by x, CONST regime neutral / 0 crosses.
- `computeLineStochRsiCrossSigLayout` (5) -- empty, ok=true,
  panel stack, zero in osc axis, price + k paths.
- `describeLineStochRsiCrossSigChart` (3) -- empty, mention,
  all four lengths.
- `<ChartLineStochRsiCrossSig />` (21) -- empty placeholder,
  region role, ref forward, data attrs, cross count, aria desc,
  three legend items, legend toggle, controlled hide, badge,
  zero / axis / grid / legend / crosses / overlay crosses
  on/off, className+style, animate off, k+signal paths, price
  path, defaultHiddenSeries.
- StochRSI Cross Signal integration (1) -- three regime anchors
  all yield K = 50, signal = 50, neutral, 0 crosses, verified
  across multiple K / start values.

## Difference vs chart-line-stoch-rsi-cross (11.857)

| primitive | second line | use-case |
| --- | --- | --- |
| `chart-line-stoch-rsi-cross` (11.857) | %D = SMA(K, 3) | canonical K/D crossings |
| `chart-line-stoch-rsi-cross-sig` (11.888) | EMA(K, signalLength) | EMA-smoothed signal crossings, family-consistent |

11.857 marks the discrete bullish / bearish events when the
StochRSI %K crosses %D (also a SMA). 11.888 replaces %D with an
EMA-smoothed signal so the StochRSI participates in the
cross-sig family alongside MACD, ATR, RSI, MFI, etc.

## Files

- web/src/components/ui/chart-line-stoch-rsi-cross-sig.tsx (new)
- web/src/components/ui/chart-line-stoch-rsi-cross-sig.test.tsx
  (new)
- web/src/components/ui/index.ts (barrel export added)
- package.json + package-lock.json + web/package.json +
  web/package-lock.json bumped to 1.11.906
- CHANGELOG.md prepended

## Verification

- `npx vitest run
  src/components/ui/chart-line-stoch-rsi-cross-sig.test.tsx` --
  58/58 pass.
- Typecheck: no new errors introduced.
