# 11.1143 - triage 5 element-missing/count-state tests (REVERSED bias: default REAL BUG)

## Scope

5 test files with 9 failing assertions. Per the dispatch's REVERSED-
bias contract (default verdict REAL BUG, mark STALE only when git
history proves a deliberate intended change AND the test predates
it), every failure was traced to a specific named source commit
that explicitly migrated UI primitives. All 9 satisfy the STALE
criterion; all 9 fixed test-only. ZERO source files touched.

## Per-file verdict table (git-history evidence)

| File | Failing assertions | Root-cause commit | Verdict | Action |
|---|---|---|---|---|
| `src/components/MeetingsMaintenancePanel.test.tsx` | 1 (`does NOT render the integrity message text when integrityMsg is null`) | `bb97c0b1` "feat(ui): Button primitive enhancements (TODO 11.308, v1.11.181)" - wrapped Button children in `<span data-section="button-children">`, so the legacy `row.querySelector('span')` now matches the button-internal wrapper rather than the message span. | STALE | Scope query to `span.truncate` (the integrity message span class) so the wrapper inside the button no longer matches. |
| `src/components/SpecialistsAuditPanel.test.tsx` | 5 (one-li-per-entry, reverse-order, reason-copy "— manual cleanup", add chip text-success, remove chip text-rose-700) | `5fa26dcf` "feat(ui): timeline primitive (11.149) v1.11.167" - audit-log section migrated from raw `<ul>/<li>` with per-action tone classes to the Timeline primitive (`<ol data-timeline>` with `<li data-timeline-item data-tone="...">` and a uniformly-styled action chip). The "— {reason}" em-dash prefix was dropped because the Timeline owns the visual separation via connector + dot. | STALE | Query `ol[data-timeline]` instead of `<ul>`; assert tone via `closest('[data-timeline-item="true"]').getAttribute('data-tone')` instead of chip className; drop the em-dash from the reason-copy assertion. |
| `src/components/WorkflowSelectedHeader.test.tsx` | 1 (`omits the inputs error span when inputsError is null`) | `bb97c0b1` "feat(ui): Button primitive enhancements (TODO 11.308, v1.11.181)" - same `<span data-section="button-children">` wrapper now has empty direct-text content (matching the legacy `c === ''` predicate) when children are non-text (icons/nested spans). | STALE | Switch to a class-scoped query `span.text-destructive` that matches the assertion's stated intent ("no destructive-tone span when inputsError is null"). |
| `src/pages/Health.test.tsx` | 1 (`does NOT render the loading skeleton when data is already present`) | `688daabe` "feat(ui): adopt StatusDot in WorkerList, SessionsListSection, Health (11.171)" - StatusDot adoption added a `<span role="status" aria-label="Status: online">` to the Health page that the test's broad `queryByRole('status')` falsely catches. The skeleton at `src/pages/Health.tsx:325` uses `role="status"` with `aria-label="Refresh health"`. | STALE | Scope to `queryByRole('status', { name: 'Refresh health' })` so the StatusDot does not match. |
| `src/lib/markdown.test.tsx` | 1 (`renders a horizontal rule for --- and longer dash runs`) | `f25102c9` "feat(ui): separator primitive (11.147) v1.11.165" - markdown HR branch migrated from `<hr>` to the shared `<Separator />` primitive, which renders `<div data-section="separator" role="none">` by default. | STALE | Query `[data-section="separator"]` instead of `<hr>`. |

All 9 failing assertions: STALE (fixed test-only). 0 REAL bugs. 0
left failing.

## Verification

- `tsc --noEmit -p web/tsconfig.json`: 0 errors (PROJECT-WIDE GREEN
  preserved).
- `npm run build`: green (~10s).
- `vitest run` on the 5 dispatched files: **5/5 files pass, 178/178
  tests pass.**
- `git diff --name-only`: only the 5 dispatched test files. ZERO
  source files touched.
