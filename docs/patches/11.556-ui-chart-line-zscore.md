# 11.556 -- web/src/components/ui/chart-line-zscore.tsx

Pure-SVG two-panel line chart with a **rolling z-score
panel**. The main panel (top) renders the raw series; the
sub-panel (bottom) renders the rolling z-score as a
**continuous line** -- the standardized deviation of every
point, not a set of threshold flags.

## Math

For index `t` the **trailing window** is the slice
`values[max(0, t - W + 1) .. t]` -- it expands from a single
point until it reaches the full window length `W`. Over
that window:

```
mean = average of the window
std  = population standard deviation of the window
       (sum of squared deviations / n)
z_t  = (values[t] - mean) / std
```

When `std = 0` (a constant window, including the
single-point window at index 0) the z-score is defined as
`0` -- the value sits exactly on the window mean, zero
standard deviations away. This keeps the z-score a
**continuous signal with no gaps** from the very first
point.

The z-score answers "how many standard deviations is the
current value from its recent rolling mean?" -- a
scale-free, locally-standardized view of the series.

### Verified properties (by test)

- A constant series has `std = 0` and `z = 0` throughout.
- A linear ramp produces, once the window is full, a
  **constant** z-score of `sqrt(3/2)` for `W = 3` (window
  `[a, a+d, a+2d]` has `mean = a+d`, `std = d*sqrt(2/3)`, so
  the last point's z is `d / (d*sqrt(2/3)) = sqrt(3/2)`).
- A two-point window gives `z = +/- 1`.
- A single spike at the end of a `W`-length window of zeros
  has `z = sqrt(W - 1)`: the `SPIKE_BEYOND` fixture
  (`[0]*9 + [50]`, `W = 10`) yields exactly `mean = 5`,
  `std = 15`, `z = 3`.
- The trailing window size grows `1, 2, ..., W, W, W, ...`.

## "Continuous z, not threshold flags"

This primitive is deliberately distinct from
`<ChartLineAnomaly>` (11.524): anomaly detection **flags**
the points whose rolling z-score exceeds a threshold,
drawing discrete markers only on the extremes. ChartLineZscore
instead shows the **entire z-score curve** as a continuous
line in its own panel -- every point's standardized
deviation, plotted over time. Optional dashed reference
guide lines at `+/- referenceLevel` (default 2) give visual
context, but no point is marked or flagged; the continuous
curve is the whole point. A `band` field
(`'within' | 'beyond'`) is computed per sample purely for
the tooltip text, not for any on-chart marker.

## Distinct from related primitives

- `<ChartLineAnomaly>` (11.524): flags threshold-crossing
  points with discrete markers. ChartLineZscore shows the
  continuous z curve -- the signal itself, not its extremes.
- `<ChartLineControl>` (11.534): a Shewhart SPC chart with
  **global, constant** `mean +/- k*sigma` limits computed
  once over the whole series. The z-score uses a **rolling
  window**, so the standardization is local and adapts to a
  changing baseline.
- `<ChartLineCusum>` (11.554): accumulates deviations into a
  cumulative sum to detect drift. The z-score is the
  **per-point** standardized deviation, not a cumulative
  quantity.
- `<ChartLineEwmsd>` (11.555): plots an EWMA mean and an
  exponentially-weighted standard-deviation band around the
  raw series. ChartLineZscore plots the standardized
  deviation itself (`(y - mean) / std`) as a separate
  signal, using a flat trailing window rather than
  exponential weighting.

The canonical rolling-standardization view -- the building
block under z-score anomaly detection, shown here in full
rather than reduced to flags.

## Public surface

- `<ChartLineZscore series={...} />`.
- `series: ChartLineZscoreSeries[]` --
  `{id, label, data, color?, window?}`. The per-series
  `window` override beats chart-level.
- `window` (default 20) -- rolling window length; clamped
  to an integer `>= 2`.
- `referenceLevel` (default 2) -- magnitude of the `+/-`
  guide lines; clamped to `>= 0`.
- `subHeightRatio` (default 0.4; clamped to `[0.1, 0.9]`).
- `referenceColor`.
- Toggle flags: `showAxis`, `showGrid`, `showDots`,
  `showLegend`, `showTooltip`, `showConfigBadge`, `showRaw`,
  `showZscore`, `showZeroLine`, `showReferenceLines`,
  `animate`.
- Controlled + uncontrolled visibility.
- Standard sizing + format + a11y props.
- `formatValue` / `formatX` / `formatZscore`.
- `onPointClick({series, point})`.

## Pure helpers

- `getLineZscoreDefaultColor(index)`.
- `getLineZscoreFinitePoints(points)`.
- `normaliseLineZscoreWindow(value)` -- integer `>= 2`,
  default 20.
- `normaliseLineZscoreReferenceLevel(value)` -- `>= 0`,
  default 2.
- `normaliseLineZscoreSubHeightRatio(value)` -- `[0.1,0.9]`.
- `classifyLineZscoreBand(z, referenceLevel)` -- `'within' |
  'beyond'` (strict comparison `|z| > referenceLevel`).
- `computeRollingZScores(values, window)` -- the rolling
  mean / std / z recursion; returns per-index `{mean, std,
  z, windowSize}`. Drops non-finite values.
- `runLineZscore(points, options?)` -- canonical pipeline.
  Sorts ascending, drops non-finite, computes the rolling
  z-score, returns per-sample `{index, x, raw, mean, std,
  z, windowSize, band}` plus `maxAbsZ`, `finalZ`,
  `beyondCount`.
- `computeLineZscoreLayout({...})` -- projects the main +
  sub panels and the raw + z-score paths.
- `describeLineZscoreChart(series, options?)`.

## Layout

Two stacked SVG panels sharing the x axis:
- **Main panel** (top, ~60%): the raw series line.
- **z-score sub-panel** (bottom, ~40%): the continuous
  z-score line, a centred zero reference line, and dashed
  `+/- referenceLevel` guide lines. The sub-panel y range is
  symmetric around zero.

## ARIA + test selectors

- Root `role="region"` + `aria-label` + `aria-describedby`.
- SVG `role="img"`.
- Raw path + z-score path + every dot `role="graphics-symbol"`
  + `tabIndex={0}`. The z-score path aria-label includes the
  window length.
- Every node carries `data-section="chart-line-zscore-..."`.
- Root mirrors `data-series-count`,
  `data-visible-series-count`, `data-total-points`,
  `data-window`, `data-reference-level`, `data-max-abs-z`,
  `data-animate`.
- Series groups expose `data-series-window`,
  `data-series-reference-level`, `data-series-max-abs-z`,
  `data-series-final-z`, `data-series-beyond-count`,
  `data-series-finite-count`.
- Raw path `data-kind="raw"`; z-score path
  `data-kind="zscore"`. Reference lines carry
  `data-side="upper"` / `"lower"`. Dots expose `data-mean`,
  `data-std`, `data-zscore`, `data-window-size`,
  `data-band`.

## Tooltip

Hovering a dot shows the series label, x, raw value, the
window mean, the window std, the bold `z-score`, and a
coloured `within`/`beyond <referenceLevel> sigma` row.

## Config badge

Top-left badge shows
`ZS W=<window> ref=<referenceLevel> maxZ=<maxAbsZ>`.

## Tests

77 vitest cases in `chart-line-zscore.test.tsx`, all
passing:

- Defaults: positive sizes, window >= 2 + reference level,
  sub ratio in (0, 1), 10-colour palette.
- `getLineZscoreDefaultColor`: cycle, NaN / negative.
- `getLineZscoreFinitePoints`: drops non-finite.
- `normaliseLineZscoreWindow`: fallback, clamp >= 2, floor.
- `normaliseLineZscoreReferenceLevel`: fallback, clamp >= 0.
- `normaliseLineZscoreSubHeightRatio`: clamp `[0.1, 0.9]`.
- `classifyLineZscoreBand`: beyond / within / strict
  boundary / non-finite.
- `computeRollingZScores`: empty, single point, **constant
  -> std + z 0**, **window expands 1,2,3,3,3**, **linear
  ramp full-window z = sqrt(3/2)**, two-point window
  z = +/- 1, **SPIKE_BEYOND mean 5 / std 15 / z 3
  verified**, drops non-finite.
- `runLineZscore`: empty, per-sample z matches the rolling
  computation, **classifies points beyond the reference
  level**, **z exactly at the reference stays within**,
  constant series stays within, sorts ascending + drops
  non-finite, tracks maxAbsZ + finalZ, exposes window +
  referenceLevel, records the trailing window size.
- `computeLineZscoreLayout`: empty / degenerate / hidden /
  stacked main + sub panels / raw + z-score paths / window
  + reference + maxAbsZ + beyond count exposed / symmetric
  sub y range / per-series window override / hidden
  excluded / bounds overrides / totalPoints.
- `describeLineZscoreChart`: No data, summary mentions
  rolling z-score + window + beyond, hidden filter.
- `<ChartLineZscore>` render: empty, raw path kind=raw,
  **continuous z-score path kind=zscore**, zero line +
  hide, reference guide lines upper + lower + hide, hide
  raw, hide zscore, dots via showDots, config badge with
  window + reference + max z + hide, ARIA region + img +
  sr-only desc, root data-*, series group data-*, tooltip
  with raw + mean + std + z-score + band rows + hide on
  leave + omit via prop, onPointClick payload, legend with
  window + max z + toggle + omit, animate flag + class,
  ref forwarding, displayName, custom ariaLabel, xLabel +
  yLabel + subLabel, dot exposes z-score + band attrs.

## Files

- `web/src/components/ui/chart-line-zscore.tsx`
- `web/src/components/ui/chart-line-zscore.test.tsx`
- `web/src/components/ui/index.ts` -- appended
  `export * from './chart-line-zscore';`
- `CHANGELOG.md` -- prepended `[1.11.574]` entry.
- `package.json`, `package-lock.json`, `web/package.json`,
  `web/package-lock.json` -- bumped to `1.11.574`.
