# 11.1136 - fix 42 source-confirmed tsc strict-type errors in 42 chart component files (chart bucket batch 16, seventh component batch)

## Scope

Seventh chart **component** source batch. 42 chart `.tsx` component
files, exactly 1 TS2379 each (42 total). Every fix is PURE TYPE -
erased at compile, zero JS emission change, zero rendered output
change.

## Baseline (HEAD before this patch)

`tsc --noEmit -p web/tsconfig.json`: 123 errors total, 42 in the 42
target files (1 each, all TS2379 exactOptional).

## After this patch

`tsc --noEmit -p web/tsconfig.json`: **81** errors total, **0** in
the 42 files. Delta = 123 - 81 = **-42**. Zero new errors anywhere.
`.test.*` files still at zero project-wide. Non-chart-component
files still at zero project-wide.

## What changed (per file, minimal pure-type)

All 42 files have inline options types of the form
`options: { ... X?: number; ... } = {}` with TS2379 at the call
site complaining about `{ X: number | undefined }` not being
assignable to `{ X?: number }` under `exactOptionalPropertyTypes`.

Per the dispatch's exact instruction: add `| undefined` to each
optional property of the TARGET inline option type in the same
file. The fix is purely the type annotation; the value at the call
site is unchanged.

## Verification

- `tsc --noEmit -p web/tsconfig.json`:
  - Baseline (HEAD): 123 total, 42 in target files.
  - After: 81 total, 0 in target files.
  - Net: -42, no new errors anywhere.
  - **Zero `.test.*` errors anywhere project-wide.**
  - **Zero non-chart-component errors anywhere project-wide.**
- `npx vitest run` on 42 matching test files: **42 files pass,
  2426 tests pass, 0 failed.** Zero NEW failures vs before.
- Playwright `e2e/gallery-no-crash.spec.ts`: **1 test passed**.
- No `as any`, no `ts-ignore`, no fresh `ts-expect-error`.
- No edits outside the 42 dispatched component files.
- No rendered output change.
