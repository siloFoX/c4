# Patch 11.885 -- UI: chart-line-obv-cross-sig primitive

**Version:** v1.11.903
**Queue TODO:** 11.885
**Branch:** c4/c4-mgr-auto
**Status:** done

## Summary

Added `<ChartLineObvCrossSig />` -- a pure-SVG, dual-panel
React/TS primitive that renders the close in the top panel and
overlays the On Balance Volume (OBV) with its EMA-smoothed
signal in the bottom panel, marking bullish / bearish cross
trigger events. The trigger-focused OBV variant: this primitive
isolates the actionable *accumulation / distribution trigger
events* when OBV crosses its signal and paints them as arrow
overlays on the price panel for direct charting-overlay use:

```
OBV[0]    = 0
OBV[i]    = OBV[i-1] + (close[i] > close[i-1] ? volume[i]
                     : close[i] < close[i-1] ? -volume[i]
                     : 0)
signal[i] = EMA(OBV, signalLength)
bullish  : (OBV - signal) crosses up    (prev <= 0, cur > 0)
bearish  : (OBV - signal) crosses down  (prev >= 0, cur < 0)
```

Defaults: `signalLength = 20`. Regime classifier: `bullish` (OBV
> signal), `bearish` (OBV < signal), `neutral` (OBV === signal),
`none` (either side null).

## Bit-exact anchors (three)

- **CONST {close = K, volume = V}**: every close[i] ===
  close[i-1] so OBV stays at 0 forever. signal EMA of 0s = 0.
  OBV === signal everywhere -> regime `neutral`, cross count =
  0. Verified across multiple K and V tuples.
- **LINEAR UP step > 0 with V > 0**: every close[i] > close[i-1]
  so OBV grows monotonically (OBV[i] = i * V). Signal EMA seeds
  when OBV is already above its average so OBV > signal after
  warmup -> regime `bullish`, cross count = 0 (no transition).
- **LINEAR DOWN step < 0 with V > 0**: symmetric, OBV[i] = -i*V
  -> regime `bearish` after warmup, cross count = 0.

## Surface

- Helpers exported: `getLineObvCrossSigFinitePoints`,
  `normalizeLineObvCrossSigLength`,
  `applyLineObvCrossSigEma`, `computeLineObvCrossSig`,
  `classifyLineObvCrossSigRegime`,
  `detectLineObvCrossSigCrosses`, `runLineObvCrossSig`,
  `computeLineObvCrossSigLayout`,
  `describeLineObvCrossSigChart`.
- Component: `<ChartLineObvCrossSig />` (forwardRef on a
  `<div>` wrapping the SVG + legend + badge).
- Data attrs: `data-section="chart-line-obv-cross-sig"`,
  `data-signal-length`, `data-total-points`,
  `data-bullish-count`, `data-bearish-count`,
  `data-neutral-count`, `data-cross-count`.
- Accessibility: `role="region"` + `aria-label` +
  `aria-describedby`, inner `<svg role="img">`, sr-only desc,
  motion-safe fade-in; oscillator cross markers carry
  `role="graphics-symbol"` + `tabIndex={0}` + descriptive
  `aria-label`. Overlay arrow markers on the price panel carry
  the same attributes for direct charting-overlay use.
- Legend: three toggleable buttons (`close`, `obv`, `signal`);
  controlled / uncontrolled modes; `onSeriesToggle` reports
  diffs.
- Tooltip: `x`, `close`, `obv`, `signal`, regime, running
  regime counts, and total cross count.
- Renderer: top panel renders the close with bullish / bearish
  arrow overlays at every cross trigger; bottom panel overlays
  OBV with the signal line, dashed zero reference, and dot
  markers at the same cross indices.

## Tests (58 cases)

- `getLineObvCrossSigFinitePoints` (3) -- null, NaN, null
  entries.
- `normalizeLineObvCrossSigLength` (3) -- default, below-1,
  accept.
- `applyLineObvCrossSigEma` (1) -- CONST bit-exact across K.
- `computeLineObvCrossSig` (6) -- null, CONST -> 0/0, LINEAR
  UP -> monotonic OBV, LINEAR DOWN -> monotonic negative OBV,
  defaults, no mutation.
- `classifyLineObvCrossSigRegime` (4) -- bullish, bearish,
  neutral, none.
- `detectLineObvCrossSigCrosses` (3) -- bullish, bearish, skip
  null.
- `runLineObvCrossSig` (8) -- ok flags, defaults, options,
  sort by x, CONST neutral / 0 crosses, LINEAR UP bullish / 0
  crosses, LINEAR DOWN bearish / 0 crosses.
- `computeLineObvCrossSigLayout` (5) -- empty, ok=true, panel
  stack, zero in osc axis, price + obv paths.
- `describeLineObvCrossSigChart` (3) -- empty, mention, signal
  length.
- `<ChartLineObvCrossSig />` (21) -- empty placeholder, region
  role, ref forward, data attrs, cross count, aria desc, three
  legend items, legend toggle, controlled hide, badge, zero /
  axis / grid / legend / crosses / overlay crosses on/off,
  className+style, animate off, obv+signal paths, price path,
  defaultHiddenSeries.
- OBV Cross Signal integration (1) -- three regime anchors
  with 0 crosses each verified across multiple K, V, and start
  values.

## Files

- web/src/components/ui/chart-line-obv-cross-sig.tsx (new)
- web/src/components/ui/chart-line-obv-cross-sig.test.tsx (new)
- web/src/components/ui/index.ts (barrel export added)
- package.json + package-lock.json + web/package.json +
  web/package-lock.json bumped to 1.11.903
- CHANGELOG.md prepended

## Verification

- `npx vitest run
  src/components/ui/chart-line-obv-cross-sig.test.tsx` -- 58/58
  pass.
- Typecheck: no new errors introduced.
