# Patch 11.897 -- UI: chart-line-squeeze-cross primitive

**Version:** v1.11.915
**Queue TODO:** 11.897
**Branch:** c4/c4-mgr-auto
**Status:** done

## Summary

Added `<ChartLineSqueezeCross />` -- a pure-SVG, dual-panel
React/TS primitive that renders the close in the top panel and
the TTM Squeeze momentum oscillator in the bottom panel, marking
bullish / bearish cross trigger events when a Bollinger-inside-
Keltner squeeze releases. Cross markers are also painted as
arrow overlays on the price panel for direct charting-overlay
use.

Simplified close-only TTM Squeeze:

```
TR[i]       = i === 0 ? 0 : |close[i] - close[i-1]|
sma[i]      = SMA(close, length)
stddev[i]   = sample stddev of close window of length
atr[i]      = Wilder smooth of TR over length
bbWidth     = bbMult * stddev[i]
kcWidth     = kcMult * atr[i]
squeezeOn   = bbWidth <= kcWidth
momentum    = close[i] - sma[i]
release     = previous squeezeOn -> current squeezeOn
              transitions from true to false
bullish     : release && momentum > 0
bearish     : release && momentum < 0
```

Defaults: `length = 20`, `bbMult = 2`, `kcMult = 1.5`
(canonical TTM Squeeze). Regime classifier: `on` (squeezeOn
currently true), `bullish` (most recent release fired with
positive momentum), `bearish` (most recent release fired with
negative momentum), `none` (warmup / null).

## Bit-exact anchors

- **CONST close = K**: every TR = 0, stddev = 0, ATR = 0 ->
  bbWidth = 0 and kcWidth = 0 -> `squeezeOn = (0 <= 0) = true`
  forever. No release transitions trigger -> cross count = 0.
  momentum = K - K = 0 every bar -> regime `on` after warmup.
  Verified across multiple K including K = 0.
- **LINEAR UP step > 0**: stddev > 0 and ATR is a positive
  constant; the canonical multipliers leave bbWidth far above
  kcWidth so `squeezeOn = false` from the very first
  computable bar. Since prev squeezeOn was never true after
  warmup, no release event fires -> cross count = 0.
- **LINEAR DOWN step < 0**: symmetric, squeezeOn stays false,
  no release events -> cross count = 0.

## Surface

- Helpers exported: `getLineSqueezeCrossFinitePoints`,
  `normalizeLineSqueezeCrossLength`,
  `normalizeLineSqueezeCrossMult`,
  `applyLineSqueezeCrossWilder`, `computeLineSqueezeCross`,
  `classifyLineSqueezeCrossRegime`,
  `detectLineSqueezeCrossCrosses`, `runLineSqueezeCross`,
  `computeLineSqueezeCrossLayout`,
  `describeLineSqueezeCrossChart`.
- Component: `<ChartLineSqueezeCross />` (forwardRef on a
  `<div>` wrapping the SVG + legend + badge).
- Data attrs: `data-section="chart-line-squeeze-cross"`,
  `data-length`, `data-bb-mult`, `data-kc-mult`,
  `data-total-points`, `data-on-count`, `data-bullish-count`,
  `data-bearish-count`, `data-cross-count`.
- Accessibility: `role="region"` + `aria-label` +
  `aria-describedby`, inner `<svg role="img">`, sr-only desc,
  motion-safe fade-in; oscillator cross markers carry
  `role="graphics-symbol"` + `tabIndex={0}` + descriptive
  `aria-label`. Overlay arrow markers on the price panel carry
  the same attributes for direct charting-overlay use.
- Legend: three toggleable buttons (`close`, `momentum`,
  `squeeze`); controlled / uncontrolled modes;
  `onSeriesToggle` reports diffs.
- Tooltip: `x`, `close`, `momentum`, `squeeze` on/off,
  regime, running regime counts, and total cross count.
- Renderer: top panel renders the close with bullish / bearish
  arrow overlays at every release trigger; bottom panel
  renders the momentum oscillator with a dashed zero line and
  squeeze-on dot markers at the panel bottom, plus dot
  markers at every release index.

## Tests (62 cases)

- `getLineSqueezeCrossFinitePoints` (3) -- null, NaN, null
  entries.
- `normalizeLineSqueezeCrossLength` (3) -- default, below-2,
  accept.
- `normalizeLineSqueezeCrossMult` (3) -- non-positive, non-
  number, accept.
- `applyLineSqueezeCrossWilder` (1) -- CONST bit-exact across
  K.
- `computeLineSqueezeCross` (6) -- null, CONST -> 0/true,
  LINEAR UP -> >0/false, LINEAR DOWN -> <0/false, defaults, no
  mutation.
- `classifyLineSqueezeCrossRegime` (4) -- on, bullish,
  bearish, none.
- `detectLineSqueezeCrossCrosses` (3) -- bullish release,
  bearish release, skip zero momentum.
- `runLineSqueezeCross` (8) -- ok flags, defaults, options,
  sort by x, CONST regime on / 0 crosses, LINEAR UP 0 crosses,
  LINEAR DOWN 0 crosses.
- `computeLineSqueezeCrossLayout` (5) -- empty, ok=true, panel
  stack, zero in osc axis, price + momentum paths.
- `describeLineSqueezeCrossChart` (3) -- empty, mention,
  length + multipliers.
- `<ChartLineSqueezeCross />` (22) -- empty placeholder,
  region role, ref forward, data attrs, cross count, aria
  desc, three legend items, legend toggle, controlled hide,
  badge, zero / axis / grid / legend / crosses / overlay
  crosses on/off, className+style, animate off, momentum
  path, price path, squeeze markers, defaultHiddenSeries.
- Squeeze Cross integration (1) -- three anchors all with 0
  crosses verified across multiple K, start values.

## Files

- web/src/components/ui/chart-line-squeeze-cross.tsx (new)
- web/src/components/ui/chart-line-squeeze-cross.test.tsx
  (new)
- web/src/components/ui/index.ts (barrel export added)
- package.json + package-lock.json + web/package.json +
  web/package-lock.json bumped to 1.11.915
- CHANGELOG.md prepended

## Verification

- `npx vitest run
  src/components/ui/chart-line-squeeze-cross.test.tsx` --
  62/62 pass.
- Typecheck: no new errors introduced.
