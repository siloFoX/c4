# 11.1110 -- CHORE: fix 36 source-confirmed tsc strict-type errors in 3 src/components test files (v1.11.1128)

## Scope

The project tsconfig type-checks tests, so `tsc --noEmit` reports
strict-type errors in component tests too. This batch covers 3 files /
36 errors. Scope is ONLY those 3 files; no non-test source edits; no
`as any` / `ts-ignore`; no test assertion was changed. Non-null
assertion `!` is allowed by the dispatch where setup guarantees
presence.

## Edits

| File | Count | Codes | Pattern |
|---|---:|---|---|
| `WorkerActions.test.tsx` | 16 | 1 TS6133 + 15 TS2532 | Deleted the dead `let lastToastProps...` declaration AND the in-mock assignment AND the `lastToastProps = null;` reset in `beforeEach` (the variable was write-only -- TS6133). 15 sites switched `runActionMock.mock.calls[0][0]` -> `runActionMock.mock.calls[0]![0]`. Every site is preceded by a click action that guarantees the mock fired (and several are gated explicitly via `expect(runActionMock).toHaveBeenCalledTimes(1)` right above). |
| `SpecialistsView.test.tsx` | 12 | 11 TS6133 + 1 TS2322 | The 11 `let last*` mock-prop trackers (`lastSummaryRendered / lastBulkOpsProps / lastAuditRendered / lastListCardHeader / lastListProps / lastDetailHeader / lastMetadataPanel / lastTagEditor / lastScoreHistory / lastPromptPanel / lastEnrichmentPanels`) were write-only -- declared, written in mock bodies, and reset in beforeEach, but never read. Removed all 11 declarations AND every write site (mock-body assignment + beforeEach reset) in a single mechanical pass; tests still assert via `data-testid` markers in the mock JSX. The 1 TS2322 at L593 was `filtered: [SPECS[0]]` -- `SPECS[0]` is `Specialist \| undefined` -- non-null asserted to `[SPECS[0]!]` (SPECS is defined statically above with several entries). |
| `ui/stepper.test.tsx` | 8 | 7 TS2532 + 1 TS18048 | `items[N].getAttribute(...)` -> `items[N]!.getAttribute(...)` (7 sites at L43-46 + L149-151) and `last.querySelector(...)` -> `last!.querySelector(...)` at L160. Every site is in a test that just rendered a `<Stepper steps={baseSteps} currentIndex=...>` with `baseSteps.length=4`, so the indices and the `items[length-1]` are guaranteed by construction. |

No test assertion was changed; runtime behaviour is identical.

## Verification (mandated)

- `tsc --noEmit` errors in the 3 files: **36 -> 0**.
- Project-wide tsc total: **1021 -> 985** (delta exactly **-36**,
  matches "at least -36"). Diff confirms **zero new errors anywhere**.
- `vitest run` on the 3 test files: **3/3 files, 119/119 tests pass**.
  Same number of passing tests as before -- the edits are purely
  type-level (assertions and dead-code removal).

## Risk

- Pure type-level edits plus dead-code removal.
- Each `!` is gated by a render / click / array-length-by-construction
  expectation a line or two above, so the assertion never throws at
  runtime in the test bodies.
- The 12 unused-tracker removals (1 in WorkerActions + 11 in
  SpecialistsView) drop write-only `let` variables. They were
  TS6133-flagged precisely because nothing ever read them: the
  associated tests assert via `data-testid` on the JSX the mock
  returns, not via these in-memory captures. Removing the writes does
  not change the rendered output of the mocks.
