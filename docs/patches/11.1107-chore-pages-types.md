# 11.1107 -- CHORE: fix 14 source-confirmed tsc strict-type errors in 9 src/pages files (v1.11.1125)

## Scope

`tsc --noEmit` reports 14 strict-type errors in 9 real (non-test) page
components that keep the build gate red. Scope is ONLY those 9 files;
no edits anywhere else; no `as any` / `ts-ignore` / `ts-expect-error`.

## Edits

| File | Line | Code | Edit |
|---|---:|---|---|
| `DesignSystem.tsx` | 96 | TS6133 | Removed the dead `MissingPrimitive` placeholder function (declared but never called) |
| `Notifications.tsx` | 139 | TS2538 | `const type = types[i % types.length] ?? 'system';` -- guards the index against `noUncheckedIndexedAccess` undefined; `'system'` is a valid `NotificationType` and the loop is bounded so the fallback never fires |
| `Notifications.tsx` | 144 | TS2322 | Same fix as L139 -- once `type` is narrowed to `NotificationType`, the object-literal `type` shorthand-key satisfies the field |
| `Notifications.tsx` | 145 | (collateral) | After the L139/144 fix unblocked tsc, the next line `pool[N]` (string | undefined) surfaced as a new error against `NotificationItem.title: string`. Added `?? ''` fallback. Net total error delta is still `-14` (the new error appeared and was immediately fixed inside scope). |
| `Profiles.tsx` | 408 | TS2322 | Removed the unsupported `name={...}` prop from `RadioGroup` -- `RadioGroupProps<V>` does not declare a `name` prop. RadioGroup is a controlled component (value/onChange), so the missing `name` attribute has no effect on the runtime behaviour the page uses. |
| `Risk.tsx` | 4 | TS6133 | Dropped the unused `Input` named import. |
| `Scribe.tsx` | 96 | TS2322 | Item-array literal failure was rooted at L109 -- once L109 was fixed, the array satisfied `DataListItem[]`. No edit at L96 itself. |
| `Scribe.tsx` | 109 | TS2375 | `copyValue: status.contextPath \|\| undefined` violated `exactOptionalPropertyTypes` on `DataListItem.copyValue?: string`. Replaced with conditional spread `...(status.contextPath ? { copyValue: status.contextPath } : {})` so `copyValue` is OMITTED when no path is set instead of passed as undefined. |
| `Settings.tsx` | 259 | TS2322 | Removed the unsupported `name="settings-locale"` prop from `RadioGroup`. The `(next: string) => void` `onChange` is fine via parameter contravariance once `name` is gone. |
| `Templates.tsx` | 3 | TS6133 | Dropped the unused `LoadingSkeleton` from the `PageFrame` named-import list. |
| `Templates.tsx` | 342 | TS2322 | Removed `autoResize` and `maxRows` from `Textarea` -- `TextareaProps` declares only `label / hint / error / variant / showCharCount` on top of `TextareaHTMLAttributes`. Neither was an actual feature flag the component honoured; they were noise props from an old API. The remaining `rows={4}` already drives the textarea's initial height. |
| `Workers.tsx` | 263 | TS2322 | `Sparkline variant="line"` -> `variant="default"`. `SparklineVariant` is `default \| success \| warning \| destructive \| info \| muted`; the chart was always rendered as a line (the component's render mode), so `"default"` is the right tone preset for the workers hero. |
| `Workers.tsx` | 302 | TS2322 | `BadgeCounter variant="solid"` -> `variant="numeric"`. `BadgeCounterVariant` is `numeric \| dot`; this badge is showing the worker count, so `numeric` is the correct variant. |
| `Workspaces.tsx` | 179 | TS2322 | Removed `variant="ghost"` from `IconButton`. `IconButtonProps` exposes a `tone` preset, not `variant`; the prop was being spread onto the underlying `<button>` as a non-standard attribute and was inert at runtime. |
| `Workspaces.tsx` | 203 | TS2322 | Same as L179 -- removed `variant="ghost"`. |

## Verification (mandated)

- `tsc --noEmit` errors in the 9 files: **14 -> 0**.
- Project-wide tsc total: **1090 -> 1076** (delta exactly **-14**,
  matches the dispatch's "lower by at least 14"). Diff confirms
  **zero new errors anywhere**.
- `vitest` on the 9 colocated page tests (DesignSystem,
  Notifications, Profiles, Risk, Scribe, Settings, Templates,
  Workers, Workspaces): **9/9 files, 245/245 tests pass**. Behaviour
  unchanged.

## Risk

- All edits are minimal: dropped dead code (MissingPrimitive helper)
  or unused imports; replaced invalid variant strings with valid
  enums whose visual rendering is either identical (Sparkline default
  was the implicit render anyway, BadgeCounter numeric is the
  count-showing mode the page already needed) or removed inert noise
  props (Textarea autoResize/maxRows, IconButton variant, RadioGroup
  name) that were being silently dropped at runtime.
- The two `?? 'system'` and `?? ''` fallbacks in `Notifications.tsx`
  are unreachable in practice (the loop indexes always land in
  bounds) but make the types narrow correctly.
