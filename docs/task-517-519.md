# 작업 지시서: 5.17 + 5.18 + 5.19

## 5.17 c4 task --repo 옵션 구현

### 문제
현재 c4 task는 c4 프로젝트의 worktree만 만든다. 다른 프로젝트에서 작업하려면 --repo 옵션이 필요.

### 수정 1: src/cli.js (task case, 약 103번 줄)
for 루프 안에 --repo 파싱 추가:
```js
else if (args[i] === '--repo' && args[i + 1]) { body.projectRoot = args[++i]; }
```

### 수정 2: src/daemon.js (/task POST 라우트)
body에서 projectRoot를 꺼내서 sendTask options에 전달. 현재 /task 라우트를 확인하고 projectRoot가 전달되도록 보장.

### 수정 3: src/pty-manager.js sendTask() (약 2065번 줄)
이미 options.projectRoot를 _createAndSendTask에 전달하고 있음. _createAndSendTask(약 888번 줄)에서도 entry.projectRoot를 _detectRepoRoot에 전달하고 있음. 그러므로 CLI->daemon->sendTask 경로만 연결하면 됨.

### 수정 4: 5.16 항목도 같은 내용이므로 둘 다 done 표시

## 5.18 c4 send 자동 Enter

이미 구현되어 있음 (pty-manager.js send() 메서드 2777번 줄에서 `input + '\r'`). TODO.md에서 5.18을 done으로 변경하고 설명에 "이미 구현됨 - send()에서 자동 \r 추가" 추가.

## 5.19 PreToolUse 복합 명령 차단 실효성

### 문제
worker가 `cd X && git Y` 같은 복합 명령을 계속 사용. PreToolUse hook이 실제로 차단하지 못하는 원인 조사 필요.

### 조사 대상
1. src/pty-manager.js의 _buildCompoundBlockCommand() 함수 확인
2. _buildWorkerSettings()에서 PreToolUse hook이 정상 주입되는지 확인
3. 실제 .claude/settings.json 파일 내용 확인
4. hook command가 올바른 형식인지 검증

### 수정
원인을 찾아서 수정. 가능한 원인:
- hook command가 정상 동작하지 않음
- matcher 패턴이 Bash 도구를 못 잡음
- exit code 2 반환이 안 됨
- 경로/이스케이프 문제

## 문서
- TODO.md: 5.16, 5.17, 5.18, 5.19 항목 상태 업데이트
- CHANGELOG.md: 최상단에 새 버전 추가
- patches/ 디렉토리에 패치 노트 추가

## npm test 후 커밋
메시지: `feat: add --repo option to c4 task, fix PreToolUse compound blocking (5.17, 5.19)`
