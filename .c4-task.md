cd /root/c4-worktree-auto-w86 로 이동해서 작업해줘. 현재 브랜치는 c4/auto-lib-push-2야. 작업 단위마다 커밋해줘.

[C4 규칙 — 반드시 준수]
- 복합 명령(&&, |, ;) 사용 금지 → 단일 명령으로 분리
- IMPORTANT: git -C <path> 형태만 허용. cd 후 git 절대 금지 (cd X && git Y, cd X; git Y 모두 불가)
- sleep 대신 c4 wait <name> 사용
- /model 등 슬래시 명령: MSYS_NO_PATHCONV=1 c4 send 사용
- main 직접 커밋 금지 → 브랜치에서 작업
- 작업 루틴: 구현 → 테스트 → 문서 업데이트 → 커밋
- Respond in English only. Do not use non-ASCII characters in any output.

Lib hooks coverage push 2 (TODO 11.95, v1.11.113)

Continue the lib hook coverage push from v1.11.96. Discover untested use-*.ts files under web/src/lib/, batch 6 of them (smallest first), and add vitest RTL/jsdom tests covering the standard contract (idle / setters / validation / happy / error / busy-gate).

Note: queue named auto-w83 (already used). Your worker is auto-w86. Working dir /root/c4-worktree-auto-w86.

Approach:
1. Discover untested hooks:
   - ls /root/c4-worktree-auto-w86/web/src/lib/use-*.ts
   - Compare against existing /root/c4-worktree-auto-w86/web/src/lib/use-*.test.ts and use-*.test.tsx
   - Pick 6 hooks with the smallest implementation first.
2. Mirror the v1.11.96 batch pattern: renderHook + act, vi.mock for IO, contract coverage (idle / setters / validation / happy / error / busy-gate).
3. Target 8-15 cases per hook.

Verification:
- env -C /root/c4-worktree-auto-w86/web /root/c4/web/node_modules/.bin/vitest run --project unit <new test files>
- All new pass; full suite green (note pre-existing failures separately).

Docs / version:
- CHANGELOG.md [1.11.113] entry: 6 hooks tested + case count.
- docs/autonomous-queue-v10.md: mark 11.95 done with Shipped: summary including hook names.
- web/package.json: bump 1.11.112 -> 1.11.113.

Rules:
- single-command only (no &&, |, ;)
- git -C /root/c4-worktree-auto-w86 form
- ASCII English commits/docs
- Commit on c4/auto-lib-push-2 branch
- Do NOT merge to main - manager merges
- env -C for vite/vitest OK when prompted

Routine: discover -> pick 6 smallest -> tests in lockstep -> verify -> docs/version -> commit. Be autonomous, no clarifying questions.