cd /root/c4-worktree-auto-w87 로 이동해서 작업해줘. 현재 브랜치는 c4/auto-cmp-workerlist야. 작업 단위마다 커밋해줘.

[C4 규칙 — 반드시 준수]
- 복합 명령(&&, |, ;) 사용 금지 → 단일 명령으로 분리
- IMPORTANT: git -C <path> 형태만 허용. cd 후 git 절대 금지 (cd X && git Y, cd X; git Y 모두 불가)
- sleep 대신 c4 wait <name> 사용
- /model 등 슬래시 명령: MSYS_NO_PATHCONV=1 c4 send 사용
- main 직접 커밋 금지 → 브랜치에서 작업
- 작업 루틴: 구현 → 테스트 → 문서 업데이트 → 커밋
- Respond in English only. Do not use non-ASCII characters in any output.

WorkerList RTL (TODO 11.96, v1.11.116)

Add vitest RTL/jsdom tests for the WorkerList container component. Same pattern as the v1.11.104-112 batches.

Note: queue named auto-w84 (already used). Your worker is auto-w87. Working dir /root/c4-worktree-auto-w87.

Note on version: queue said 1.11.114 but main is now at 1.11.115 (the 11.95 WIP merge consumed 1.11.115). Use 1.11.116 instead.

Approach:
- Read web/src/components/WorkerList.tsx to understand its prop surface.
- Look at existing WorkerListGroupHeader.test.tsx + SessionsListSection.test.tsx for mirror patterns.
- Mock useWorkerList + any other hooks via vi.mock at the top.
- Cover: empty state, loading, fetch-error, list rendering (1 worker, N workers), group expansion / collapse, callback flow (onSelect / onClose / etc), locale flip if relevant.
- Target: 15-25 cases (WorkerList is a large container; spend coverage on the branching logic).

Output file:
- web/src/components/WorkerList.test.tsx

Verification:
- env -C /root/c4-worktree-auto-w87/web /root/c4/web/node_modules/.bin/vitest run --project unit src/components/WorkerList.test.tsx
- All new cases pass.

Docs / version:
- CHANGELOG.md [1.11.116] entry: WorkerList coverage + case count.
- docs/autonomous-queue-v10.md: mark 11.96 done with Shipped: summary, note 1.11.116 (not 1.11.114) per the version bump above.
- web/package.json: bump from current (1.11.115) to 1.11.116.

Rules:
- single-command only (no &&, |, ;)
- git -C /root/c4-worktree-auto-w87 form
- ASCII English commits/docs
- Commit on c4/auto-cmp-workerlist branch
- Do NOT merge to main - manager merges
- env -C for vite/vitest OK when prompted

Routine: read WorkerList -> draft test -> verify -> docs/version -> commit. Be autonomous, no clarifying questions.