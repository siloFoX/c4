cd /root/c4-worktree-auto-w90 로 이동해서 작업해줘. 현재 브랜치는 c4/auto-lib-utils야. 작업 단위마다 커밋해줘.

[C4 규칙 — 반드시 준수]
- 복합 명령(&&, |, ;) 사용 금지 → 단일 명령으로 분리
- IMPORTANT: git -C <path> 형태만 허용. cd 후 git 절대 금지 (cd X && git Y, cd X; git Y 모두 불가)
- sleep 대신 c4 wait <name> 사용
- /model 등 슬래시 명령: MSYS_NO_PATHCONV=1 c4 send 사용
- main 직접 커밋 금지 → 브랜치에서 작업
- 작업 루틴: 구현 → 테스트 → 문서 업데이트 → 커밋
- Respond in English only. Do not use non-ASCII characters in any output.

Lib utility coverage push (TODO 11.99, v1.11.121)

Cover four non-hook lib utility modules: chat-helpers.ts, snippet.ts, i18n.ts, dispatch-event.ts. Add vitest tests covering input variants, edge cases, error paths.

Note: queue named auto-w87 (already used). Your worker is auto-w90. Working dir /root/c4-worktree-auto-w90.
Note on version: queue said 1.11.117 but main is at 1.11.120. Use 1.11.121.

Approach:
- Read each module to identify the exported functions and their input shapes.
- For each function: cover happy path, edge cases (empty string, undefined, null, oversize, weird Unicode), and error paths (throw / return undefined / fallback).
- For i18n.ts: cover the t() function with template interpolation, plural, missing-key fallback, locale flip. Also setLocale, getLocale, subscribe.
- For dispatch-event.ts: cover the custom event dispatch helper plus the addEventListener cleanup contract.
- For snippet.ts: cover the existing snippet utility (codepath probably already partially covered - check first).
- For chat-helpers.ts: cover whichever helpers it exports.

Per-file caps:
- Aim for 8-20 cases per module depending on surface area.
- Commit per-module so partial progress is preserved if you hit a time limit.

Output files:
- web/src/lib/chat-helpers.test.ts
- web/src/lib/snippet.test.ts (or extend existing if present - check first)
- web/src/lib/i18n.test.ts (or extend existing if present)
- web/src/lib/dispatch-event.test.ts

Verification:
- env -C /root/c4-worktree-auto-w90/web /root/c4/web/node_modules/.bin/vitest run --project unit <new files>

Docs / version:
- CHANGELOG.md [1.11.121] entry: 4 modules + case count.
- docs/autonomous-queue-v10.md: mark 11.99 done with Shipped: summary.
- web/package.json: bump to 1.11.121.

Rules:
- single-command only (no &&, |, ;)
- git -C /root/c4-worktree-auto-w90 form
- ASCII English commits/docs
- Commit on c4/auto-lib-utils branch (per-module commits OK)
- Do NOT merge to main - manager merges
- env -C for vite/vitest OK when prompted

Routine: read each module -> tests + commit in lockstep -> verify -> docs/version -> commit. Be autonomous, no clarifying questions.